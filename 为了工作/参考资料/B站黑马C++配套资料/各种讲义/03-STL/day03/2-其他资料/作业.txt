1 vector容器存放对象指针，并打印
2 容器嵌套容器，vector容器存放vector容器作为元素，并打印
3 string容器 email验证案例
//用户邮箱地址验证
// 1 判断邮箱有效性 是否包含@和. 并且. 在@之后
// 2 判断用户输入的用户名中是否包含除了小写字母之外字符(ASCII 范围 97~122)
// 3 判断用户输入的邮箱地址是否正确(zhaosi@itcast.cn)

作业发送邮箱 mbl114@126.com

作业放到文件夹内，文件夹为你的姓名
文件夹内每一个作业一个cpp文件
