# 5、前端开发

## 前情提要

项目提供者主要学习的是后端开发，这次也是零基础入手前端开发，一点点学习，简单得跟着后端实现了一版前端，完成了这个前后端分离的项目。可能在前端代码的能力上来讲不如大多数前端录友的coding能力，如有疏漏之处，还请见谅！

该项目开发文档主要针对前后端功能调用整体流程进行解析，如果对某些技术栈不太熟悉的，可以在边学习项目的同时，通过搜索或者ai来拓宽技术栈。在这里就不对技术栈基础进行剖析了。

参考文献有：

[设计 | Element Plus](https://cn.element-plus.org/zh-CN/guide/design.html)

[Vue：Vuex-Store使用指南\_vue store-CSDN博客](https://blog.csdn.net/ChinaDragon10/article/details/141143969?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522db69d100a4a6f059ded616abae1e861a%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=db69d100a4a6f059ded616abae1e861a\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-141143969-null-null.142^v101^pc_search_result_base3\&utm_term=vue%20store\&spm=1018.2226.3001.4187)

[一文详解:Vue3中使用Vue Router\_vue3 router-CSDN博客](https://blog.csdn.net/w137160164/article/details/131152766?ops_request_misc=%257B%2522request%255Fid%2522%253A%25227bea6d754a62b33489d03a9460bbedb1%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=7bea6d754a62b33489d03a9460bbedb1\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_click~default-4-131152766-null-null.142^v101^pc_search_result_base3\&utm_term=vue%20router\&spm=1018.2226.3001.4187)

[多对多音视频通话demo完成啦？i\_哔哩哔哩\_bilibili](https://www.bilibili.com/video/BV1sG411B7oV?spm_id_from=333.788.videopod.sections\&vd_source=9b66f25927e6cc60ec8cd4e08340a677)

## 项目技术栈

vue、element-plus、store、router等。

## 项目设计

### element-plus

用到的组件包括：

ElMessage，比如ElMessage.error，ElMessage.warning，ElMessage.success，用于向前端展示事件触发的结果和返回的消息。

el-button element版本的按键。

el-form element版本的表单，用于登录注册、创建群聊、修改用户信息等这种输入+提交型的事件。

el-input element版本的输入框，用于输入信息的事件。

el-scrollbar element版本的滚动条，用于可能高度超过当前页面的模块。

el-dropdown element版本的下拉菜单，用于button下面多重选项。

### store

```javascript
import { createStore } from 'vuex'

export default createStore({
  state: {
    // web服务器地址
    // backendUrl: 'https://192.168.2.114:8000',
    // wsUrl: 'wss://192.168.2.114:8000',
    backendUrl: 'https://127.0.0.1:8000',
    wsUrl: 'wss://127.0.0.1:8000',
    userInfo: (sessionStorage.getItem('userInfo') && JSON.parse(sessionStorage.getItem('userInfo'))) || {},
    socket: null,
  },
  getters: {
  },
  mutations: {
    setUserInfo(state, userInfo) {
      state.userInfo = userInfo;
      sessionStorage.setItem('userInfo', JSON.stringify(userInfo));
    },
    cleanUserInfo(state) {
      state.userInfo = {};
      sessionStorage.removeItem('userInfo');
    }
  },
  actions: {
  },
  modules: {
  }
})
```

前端项目使用store作为状态管理模块，存储一些全局使用的变量，比如说backendUrl就是后端https服务器的ip端口地址，wsUrl就是后端websocket连接的ip端口地址，以及userInfo存储当前前端的用户信息，比如昵称、头像、性别、电话、邮箱、生日等（这里使用sessionStorage，在浏览器中按照会话为单位来存储userInfo，在浏览器中按F12打开开发者工具，点击应用程序，可以看到此时的会话存储），

![1739239678015-1d6295d0-3cee-4e60-9313-c9ecd77037ac.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239678015-1d6295d0-3cee-4e60-9313-c9ecd77037ac-587954.png)

最后还包括该前端与后端建立的websocket连接。

在mutations中，提供两个函数setUserInfo和cleanUserInfo。内容是对向会话设置当前用户的userInfo，以及清楚当前会话的userInfo。vue界面通过useStore能够访问到store中的内容，包括state以及mutations中的方法。不太了解store的同学可以看看这篇博客[Vue：Vuex-Store使用指南\_vue store-CSDN博客](https://blog.csdn.net/ChinaDragon10/article/details/141143969?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522db69d100a4a6f059ded616abae1e861a%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=db69d100a4a6f059ded616abae1e861a\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-2-141143969-null-null.142^v101^pc_search_result_base3\&utm_term=vue%20store\&spm=1018.2226.3001.4187)。

### router

<font style="color:rgba(0, 0, 0, 0.75);">Vue Router是一个官方的Vue.js路由管理器，它与 Vue.js 核心深度集成，通过它可以轻松地为单页应用程序（SPA）提供路由管理和导航功能。</font>

> <font style="color:rgba(0, 0, 0, 0.75);">Vue Router有一些功能。</font>
>
> <font style="color:rgba(0, 0, 0, 0.75);">路由器：Vue Router 提供了一个路由器，用于管理应用程序中的路由。Vue Router 实例化一个 Vue Router 对象，注册路由规则，并以它为中心连接其他组件。</font>
>
> <font style="color:rgba(0, 0, 0, 0.75);"></font>
>
> <font style="color:rgba(0, 0, 0, 0.75);">路由：路由是分发到不同组件的 URL 地址。在 Vue Router 中，路由通常是由 path 规则和相应的组件定义的。当浏览器的 URL 匹配到路由的 path 后，相应的组件将会被加载到页面中。路由的信息可以从 route 对象中获取。</font>
>
> <font style="color:rgba(0, 0, 0, 0.75);"></font>
>
> <font style="color:rgba(0, 0, 0, 0.75);">路由规则：路由规则是由 path、component、name、meta、props 等属性组成的。其中，path 表示 URL 的路径，component 表示要渲染的组件，name 表示路由名称，meta 表示路由的元数据，props 表示路由 props 数据。路由规则可以注册到 Vue Router 中。</font>
>
> <font style="color:rgba(0, 0, 0, 0.75);"></font>
>
> <font style="color:rgba(0, 0, 0, 0.75);">导航守卫：导航守卫是在路由跳转时执行的钩子函数，用于控制路由的访问权限、处理路由跳转前后的逻辑等。在 Vue Router 中，对于选项式 API，常用的导航守卫有 beforeRouteEnter、beforeRouteUpdate、beforeRouteLeave 等；对于使用组合 API 和 setup 函数来编写组件的，可以通过 onBeforeRouteUpdate 和 onBeforeRouteLeave 分别添加 update 和 leave 守卫。</font>

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import store from '../store/index.js'

const routes = [
  {
    path: '/',
    redirect: { name: 'Login' }
  },
  {
    path: '/login',
    name: 'Login',
    component: () => import('../views/access/Login.vue')
  },
  {
    path: '/smsLogin',
    name: 'smsLogin',
    component: () => import('../views/access/SmsLogin.vue')
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('../views/access/Register.vue')
  },
  {
    path: '/chat/owninfo',
    name: 'OwnInfo',
    component: () => import('../views/chat/user/OwnInfo.vue')
  },
  {
    path: '/chat/contactlist',
    name: 'ContactList',
    component: () => import('../views/chat/contact/ContactList.vue')
  },
  {
    path: '/chat/:id',
    name: 'ContactChat',
    component: () => import('../views/chat/contact/ContactChat.vue')
  },
  {
    path: '/chat/sessionList',
    name: 'SessionList',
    component: () => import('../views/chat/session/SessionList.vue')
  },
  {
    path: '/manager',
    name: 'Manager',
    component: () => import('../views/manager/Manager.vue')
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
});

router.beforeEach((to, from, next) => {
  if (!store.state.userInfo.uuid) {
    if (to.path === '/login' || to.path === '/register' || to.path === '/smsLogin') {
      next()
      return
    }
    next('/login')
  } else {
    next()
  }
})

export default router
```

这里创建了所有router链路。并且在每次跳转下一个页面时，都事先判断该会话是否已经登录了，即是否存在用户信息。如果不存在，得判断下一跳的页面是否是登录页面、注册页面或者验证码登录页面，如果是就直接跳转，否则都会先跳转到登录页面进行登录，并让会话存入用户信息。如果用户信息存在的话，也是直接跳转就可以了。

当某一个页面要跳转到另一个页面时，只需要引入useRouter，并且在js函数中使用router.push('/xxxx')即可完成跳转。

更详细的对router的介绍可以看[一文详解:Vue3中使用Vue Router\_vue3 router-CSDN博客](https://blog.csdn.net/w137160164/article/details/131152766?ops_request_misc=%257B%2522request%255Fid%2522%253A%25227bea6d754a62b33489d03a9460bbedb1%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=7bea6d754a62b33489d03a9460bbedb1\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_click~default-4-131152766-null-null.142^v101^pc_search_result_base3\&utm_term=vue%20router\&spm=1018.2226.3001.4187)

### ssl加密

后端已经有一套自签名证书和密钥了，直接使用该证书和密钥即可。

```javascript
const { defineConfig } = require('@vue/cli-service')
const fs = require('fs')
const path = require('path')
module.exports = defineConfig({
  transpileDependencies: true,
  lintOnSave: false,
  devServer: {
    host: '0.0.0.0',
    // 此处开启 https,并加载本地证书（否则浏览器左上角会提示不安全）
    https: {
      cert: fs.readFileSync(path.join(__dirname, 'src/assets/cert/127.0.0.1+2.pem')),
      key: fs.readFileSync(path.join(__dirname, 'src/assets/cert/127.0.0.1+2-key.pem')),
    },
    // 注意： https的端口必须是443
    port: 443,
  }
})
```

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">transpileDepe</font>\*\*\*\*<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">ndencies</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 设置为</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">true</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">，这表示将自动处理依赖的转译。在Vue CLI项目中，这通常用于确保那些不包含预编译ES6+代码的依赖能够在旧版浏览器中运行。</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">lintOnSave</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 设置为</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">false</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">，这表示在保存文件时不会执行ESLint代码检查。ESLint是一个静态代码分析工具，用于识别和报告JavaScript代码中的模式，但在某些情况下，希望关闭这个特性以加快开发速度。</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">devServer</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 这是开发服务器的配置对象，用于定义如何启动和运行本地开发服务器。</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">host</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 设置为</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">'0.0.0.0'</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">，这意味着开发服务器将接受来自任何IPv4地址的连接。这对于需要在同一网络中的其他设备上访问本地开发服务器的场景非常有用。</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">https</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 这个对象定义了如何配置HTTPS。包括：</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">cert</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 指定了SSL证书的完整路径。使用</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">fs.readFileSync</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">同步读取证书文件的内容。证书文件位于项目的</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">src/assets/cert/127.0.0.1+2.pem</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">路径下。</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">key</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 指定了SSL密钥的完整路径。同样使用</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">fs.readFileSync</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">同步读取密钥文件的内容。密钥文件位于</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">src/assets/cert/127.0.0.1+2-key.pem</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">路径下。\ </font><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">注意：为了使用HTTPS，你需要有有效的SSL证书和密钥。这里的示例路径和文件名看起来像是为本地开发生成的自签名证书。</font>

**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">port</font>**<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">: 设置为</font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">443</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">，这是HTTPS服务的标准端口。使用443端口意味着在访问开发服务器时不需要在URL中指定端口号（假设用户是在浏览器中直接访问而非通过代理等工具）。</font>

### NAT穿透公网视频通话

![1742818132722-138bc830-d71a-4500-885c-9c750e87747e.png](https://cdn.davidingplus.cn/images/2026/09/11/1742818132722-138bc830-d71a-4500-885c-9c750e87747e-831018.png)

打开前端web/chat-server/src/views/chat/contact/ContactChat.vue，找到ICE\_CFG配置，更新对应的turn服务器的相关配置。turn服务器就是你的云服务器。即修改your\_server\_ip，这个ip是云服务器公网ip。底层逻辑就是两个不同地方的ip通过访问这个云服务器的公网ip能够建立起视频通话。

![1742818632256-8c73c913-b408-49c8-b922-e10269e617c7.png](https://cdn.davidingplus.cn/images/2026/09/11/1742818632256-8c73c913-b408-49c8-b922-e10269e617c7-956978.png)

同时记得替换自己的用户名和密码！！！即云服务器的用户名和密码。

如果不需要公网通信，只需要局域网或者localhost通信的话，就需要把iceServers删掉，让ICE\_CFG置空。整个项目一开始默认为空。

## 业务开发

### 登录注册

#### /login

![1739239687990-8c3cd9d7-f1e9-40fb-a505-8b511b98d15e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239687990-8c3cd9d7-f1e9-40fb-a505-8b511b98d15e-990560.png)

```vue
// Login.vue
<div class="login-wrap">
  <div
    class="login-window"
    :style="{
      boxShadow: `var(${'--el-box-shadow-dark'})`,
    }"
  >
    <h2 class="login-item">登录</h2>
    <el-form
      :model="loginData"
      label-width="70px"
    >
      <el-form-item
        prop="telephone"
        label="账号"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input v-model="loginData.telephone" />
      </el-form-item>
      <el-form-item
        prop="password"
        label="密码"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input type="password" v-model="loginData.password" />
      </el-form-item>
    </el-form>
    <div class="login-button-container">
      <el-button type="primary" class="login-btn" @click="handleLogin"
        >登录</el-button
      >
    </div>

    <div class="go-register-button-container">
      <button class="go-register-btn" @click="handleRegister">注册</button>
      <button class="go-sms-btn" @click="handleSmsLogin">验证码登录</button>
    </div>
  </div>
</div>
```

设置一个el-form表单，数据对象是loginData。通过键入用户的账号和密码，点击登录即可用户登录，而注册button和验证码登录button分别可以跳转到注册界面和验证码登录界面。

```vue
const checkTelephoneValid = () => {
  const regex = /^1([38][0-9]|14[579]|5[^4]|16[6]|7[1-35-8]|9[189])\d{8}$/;
  return regex.test(data.loginData.telephone);
};
```

对账号（手机号进行前端校验）。

```vue
const handleLogin = async () => {
  try {
    if (!data.loginData.telephone || !data.loginData.password) {
      ElMessage.error("请填写完整登录信息。");
      return;
    }
    if (!checkTelephoneValid()) {
      ElMessage.error("请输入有效的手机号码。");
      return;
    }
    const response = await axios.post(
      store.state.backendUrl + "/login",
      data.loginData
    );
    console.log(response);
    if (response.data.code == 200) {
      if (response.data.data.status == 1) {
        ElMessage.error("该账号已被封禁，请联系管理员。");
        return;
      }
      try {
        ElMessage.success(response.data.message);
        if (!response.data.data.avatar.startsWith("http")) {
          response.data.data.avatar =
            store.state.backendUrl + response.data.data.avatar;
        }
        store.commit("setUserInfo", response.data.data);
        
        // 创建websocket连接
        
        router.push("/chat/sessionlist");
      } catch (error) {
        console.log(error);
      }
    } else {
      ElMessage.error(response.data.message);
    }
  } catch (error) {
    ElMessage.error(error);
  }
};
```

前端判断是否完整填写了账号密码，是否输入了有效的手机号，然后通过axios的post方法调用后端api接口/login。获取后端返回的response，先判断response.data.status是否为正常，如果不正常为禁用，则通过ElMessage给前端通知结果。

```vue
response.data.data.avatar = store.state.backendUrl + response.data.data.avatar;
```

这条语句，store.state.backendUrl存储的是后端服务器的ip和port地址。而通过调用后端api接口/login所返回的response里avatar的格式如下：

![1738762330061-96210d3b-508e-49a0-ae68-92e5eb92e768.png](https://cdn.davidingplus.cn/images/2026/09/11/1738762330061-96210d3b-508e-49a0-ae68-92e5eb92e768-076401.png)

如果是自己上传的图片，默认是按照后端项目根目录来锁定到具体的图片的。这时候需要加上store.state.backendUrl才能访问到后端的图片。

#### /user/sendSmsCode

![1739239706591-f7593a12-80f6-4b3b-9dd8-c236eaf84554.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239706591-f7593a12-80f6-4b3b-9dd8-c236eaf84554-130564.png)

```vue
const sendSmsCode = async () => {
  if (
    !data.registerData.telephone ||
    !data.registerData.nickname ||
    !data.registerData.password
  ) {
    ElMessage.error("请填写完整注册信息。");
    return;
  }
  if (!checkTelephoneValid()) {
    ElMessage.error("请输入有效的手机号码。");
    return;
  }
  const req = {
    telephone: data.registerData.telephone,
  };
  const rsp = await axios.post(
    store.state.backendUrl + "/user/sendSmsCode",
    req
  );
  console.log(rsp);
  if (rsp.data.code == 200) {
    ElMessage.success(rsp.data.message);
  } else if (rsp.data.code == 400) {
    ElMessage.warning(rsp.data.message);
  } else {
    ElMessage.error(rsp.data.message);
  }
};
```

当点击“点击发送”验证码时，前端会调用该方法，这个时候会检验是否完整填写相关信息，以及是否已输入有效的手机号。如果没问题，就会向后端api接口/user/sendSmsCode发送请求数据。

#### /register

![1739239716401-00df46c0-b92b-45e7-8028-988294f7b8d5.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239716401-00df46c0-b92b-45e7-8028-988294f7b8d5-557794.png)

```vue
<div class="register-wrap">
  <div
    class="register-window"
    :style="{
      boxShadow: `var(${'--el-box-shadow-dark'})`,
    }"
  >
    <h2 class="register-item">注册</h2>
    <el-form
      :model="registerData"
      label-width="70px"
    >
      <el-form-item
        prop="nickname"
        label="昵称"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
          {
            min: 3,
            max: 10,
            message: '昵称长度在 3 到 10 个字符',
            trigger: 'blur',
          },
        ]"
      >
        <el-input v-model="registerData.nickname" />
      </el-form-item>
      <el-form-item
        prop="telephone"
        label="账号"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input v-model="registerData.telephone" />
      </el-form-item>
      <el-form-item
        prop="password"
        label="密码"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input type="password" v-model="registerData.password" />
      </el-form-item>
      <el-form-item
        prop="sms_code"
        label="验证码"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input v-model="registerData.sms_code" style="max-width: 200px">
          <template #append>
            <el-button
              @click="sendSmsCode"
              style="background-color: rgb(229, 132, 132); color: #ffffff"
              >点击发送</el-button
            >
          </template>
        </el-input>
      </el-form-item>
    </el-form>
    <div class="register-button-container">
      <el-button type="primary" class="register-btn" @click="handleRegister"
        >注册</el-button
      >
    </div>
    <div class="go-login-button-container">
      <button class="go-sms-login-btn" @click="handleSmsLogin">
        验证码登录
      </button>
      <button class="go-password-login-btn" @click="handleLogin">
        密码登录
      </button>
    </div>
  </div>
</div>
```

设置一个el-form表单，通过填写昵称，手机号，密码来注册账号。等三个信息都填写完毕了，点击验证码发送，然后验证码发送到填写的手机号上，当用户获取到验证码了之后，在验证码输入框输入获取的验证码，之后点击注册即可。底部有跳转登录和验证码登录的按钮。

```vue
const handleRegister = async () => {
  try {
    if (
      !data.registerData.nickname ||
      !data.registerData.telephone ||
      !data.registerData.password ||
      !data.registerData.sms_code
    ) {
      ElMessage.error("请填写完整注册信息。");
      return;
    }
    if (
      data.registerData.nickname.length < 3 ||
      data.registerData.nickname.length > 10
    ) {
      ElMessage.error("昵称长度在 3 到 10 个字符。");
      return;
    }
    if (!checkTelephoneValid()) {
      ElMessage.error("请输入有效的手机号码。");
      return;
    }
    const response = await axios.post(
      store.state.backendUrl + "/register",
      data.registerData
    ); // 发送POST请求
    if (response.data.code == 200) {
      ElMessage.success(response.data.message);
      console.log(response.data.message);
      // 查看avatar前缀有没有http
      if (!response.data.data.avatar.startsWith("http")) {
        response.data.data.avatar =
          store.state.backendUrl + response.data.data.avatar;
      }
      store.commit("setUserInfo", response.data.data);

      // 创建websocket连接
      
      router.push("/chat/sessionlist");
    } else {
      ElMessage.error(response.data.message);
      console.log(response.data.message);
    }
  } catch (error) {
    ElMessage.error(error);
    console.log(error);
  }
};
```

点击注册后，需要先判断是否所有信息都填写好了，然后检查昵称是否在3到10个字之间，最后检查是否输入了有效的手机号。都没有问题的话，向后端api接口/register请求数据，后端返回response，前端调用store存储response中的userInfo。

#### /user/smsLogin

![1739239722593-e9ad5ce1-73ee-4ac5-9403-bf37abcd8f0f.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239722593-e9ad5ce1-73ee-4ac5-9403-bf37abcd8f0f-909379.png)

```vue
<div class="login-wrap">
  <div
    class="login-window"
    :style="{
      boxShadow: `var(${'--el-box-shadow-dark'})`,
    }"
  >
    <h2 class="login-item">登录</h2>
    <el-form
      ref="formRef"
      :model="loginData"
      label-width="70px"
      class="demo-dynamic"
    >
      <el-form-item
        prop="telephone"
        label="账号"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input v-model="loginData.telephone" />
      </el-form-item>
      <el-form-item
        prop="sms_code"
        label="验证码"
        :rules="[
          {
            required: true,
            message: '此项为必填项',
            trigger: 'blur',
          },
        ]"
      >
        <el-input v-model="loginData.sms_code" style="max-width: 200px">
          <template #append>
            <el-button
              @click="sendSmsCode"
              style="background-color: rgb(229, 132, 132); color: #ffffff"
              >点击发送</el-button
            >
          </template>
        </el-input>
      </el-form-item>
    </el-form>
    <div class="login-button-container">
      <el-button type="primary" class="login-btn" @click="handleSmsLogin"
        >登录</el-button
      >
    </div>

    <div class="go-register-button-container">
      <button class="go-register-btn" @click="handleRegister">注册</button>
      <button class="go-sms-btn" @click="handleLogin">密码登录</button>
    </div>
  </div>
</div>
```

验证码登录的前端界面。

```vue
const handleSmsLogin = async () => {
  try {
    if (!data.loginData.telephone || !data.loginData.sms_code) {
      ElMessage.error("请填写完整登录信息。");
      return;
    }
    if (!checkTelephoneValid()) {
      ElMessage.error("请输入有效的手机号码。");
      return;
    }
    const response = await axios.post(
      store.state.backendUrl + "/user/smsLogin",
      data.loginData
    );
    console.log(response);
    if (response.data.code == 200) {
      if (response.data.data.status == 1) {
        ElMessage.error("该账号已被封禁，请联系管理员。");
        return;
      }
      try {
        ElMessage.success(response.data.message);
        if (!response.data.data.avatar.startsWith("http")) {
          response.data.data.avatar =
            store.state.backendUrl + response.data.data.avatar;
        }
        store.commit("setUserInfo", response.data.data);
        
        // 准备创建websocket连接
        
        router.push("/chat/sessionlist");
      } catch (error) {
        console.log(error);
      }
    } else {
      ElMessage.error(response.data.message);
    }
  } catch (error) {
    ElMessage.error(error);
  }
};
```

验证码登录界面需要输入账号和验证码即可登录，通过“点击发送”就会向输入的手机号发送验证码。最后点击登录就可以验证通过登录。

### 用户功能

![1739239726586-73067ec9-ae3f-431e-bba9-98a9dbfa108b.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239726586-73067ec9-ae3f-431e-bba9-98a9dbfa108b-185947.png)

![1739239728333-4190fbd1-71d7-444f-b51a-b5a3a088e99c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239728333-4190fbd1-71d7-444f-b51a-b5a3a088e99c-993595.png)

这个是OwnInfo.vue页面，最左侧是导航栏，组件是NavigationModal。

![1739239732233-4f2087fa-dd90-48b9-8755-c39c53553bc2.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239732233-4f2087fa-dd90-48b9-8755-c39c53553bc2-097116.png)

第一个图标是会话聊天，跳转到/chat/sessionlist。

![1739239742216-e69f6387-4eb8-4b98-860f-4a0cd1762275.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239742216-e69f6387-4eb8-4b98-860f-4a0cd1762275-935971.png)

第二个图标是通讯录管理，跳转到/chat/contactlist。

![1739239746635-85b36dea-15a6-492d-a1ea-9168e13c54f0.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239746635-85b36dea-15a6-492d-a1ea-9168e13c54f0-834223.png)

这个图标是我的主页，跳转到/chat/owninfo。

![1739239750175-2ee673bc-5554-4985-be19-6402fcb5b4ec.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239750175-2ee673bc-5554-4985-be19-6402fcb5b4ec-383819.png)

这个图标是设置，对于非管理员来说可以退出登录，对于管理员来说还有进入管理员模式。

```vue
<div class="chat-wrap">
  <div
    class="chat-window"
    :style="{
      boxShadow: `var(${'--el-box-shadow-dark'})`,
    }"
  >
    <el-container class="chat-window-container">
      <el-aside class="aside-container">
        <NavigationModal></NavigationModal>
        <ContactListModal></ContactListModal>
      </el-aside>
      <div class="owner-info-window">
        <div class="my-homepage-title"><h2>我的主页</h2></div>

        <p class="owner-prefix">用户id：{{ userInfo.uuid }}</p>
        <p class="owner-prefix">昵称：{{ userInfo.nickname }}</p>
        <p class="owner-prefix">电话：{{ userInfo.telephone }}</p>
        <p class="owner-prefix">邮箱：{{ userInfo.email }}</p>
        <p class="owner-prefix">
          性别：{{ userInfo.gender === 0 ? "男" : "女" }}
        </p>
        <p class="owner-prefix">生日：{{ userInfo.birthday }}</p>
        <p class="owner-prefix">个性签名：{{ userInfo.signature }}</p>
        <p class="owner-prefix">
          加入kama chat server的时间：{{ userInfo.created_at }}
        </p>
        <div class="owner-opt">
          <p class="owner-prefix">头像：</p>
          <img style="width: 40px; height: 40px" :src="userInfo.avatar" />
        </div>
      </div>
      <div class="edit-window">
        <el-button class="edit-btn" @click="showMyInfoModal">编辑</el-button>
      </div>
    </el-container>
  </div>
</div>
```

左边部分是NavigationModal，中间部分是ContactListModal组件。

![1739239755778-1b56ce33-55e7-4a15-9d96-1b1b8dbb0335.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239755778-1b56ce33-55e7-4a15-9d96-1b1b8dbb0335-866898.png)

![1739239757743-b0105929-b231-490a-a84f-c9ce1e653cf0.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239757743-b0105929-b231-490a-a84f-c9ce1e653cf0-133318.png)

点击搜索框旁边的图标，下拉菜单弹出3项，包括“创建群聊”，“添加用户/群聊”,“新的好友”。

![1739239762256-333dce0f-a719-4a9c-992b-cd7059600f75.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239762256-333dce0f-a719-4a9c-992b-cd7059600f75-689272.png)

点击联系人，会弹出用户类型的联系人列表。

![1739239765475-d233a2ea-64b7-4b22-be60-3442fb76924c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239765475-d233a2ea-64b7-4b22-be60-3442fb76924c-952177.png)

点击我创建的群聊，会弹出我创建的所有群聊。

![1739239769059-d0da2093-fc07-4084-b5cf-346d26340466.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239769059-d0da2093-fc07-4084-b5cf-346d26340466-481069.png)

点击我加入的群聊，会弹出啊我加入的所有群聊。

```vue
<div class="contactlist-container">
  <div class="contactlist-header">
    <el-input
      v-model="contactSearch"
      class="contact-search-input"
      placeholder="搜索联系人/群聊"
      size="small"
      suffix-icon="Search"
    />
    <div class="contactlist-header-right">
      <el-dropdown placement="bottom" trigger="click">
        <button class="create-group-btn">
          <svg
            t="1733664667695"
            class="create-group-icon"
            viewBox="0 0 1024 1024"
            version="1.1"
            xmlns="http://www.w3.org/2000/svg"
            p-id="2875"
            width="128"
            height="128"
          >
            <path
              d="M488.021333 96a248.021333 248.021333 0 1 1-17.92 495.36l-1.749333 0.341333-4.352 0.298667A304 304 0 0 0 160 896a32 32 0 1 1-64 0 368.170667 368.170667 0 0 1 250.026667-348.672A247.978667 247.978667 0 0 1 488.021333 96z m288 528a32 32 0 0 1 32 32l-0.042666 87.978667H896a32 32 0 0 1 31.701333 27.690666l0.298667 4.352a32 32 0 0 1-32 32l-88.021333-0.042666V896a32 32 0 0 1-27.648 31.701333l-4.352 0.298667a32 32 0 0 1-32-32v-88.021333h-87.978667a32 32 0 0 1-31.701333-27.648l-0.298667-4.352a32 32 0 0 1 32-32h87.978667v-87.978667a32 32 0 0 1 27.690666-31.701333zM488.021333 160a184.021333 184.021333 0 1 0 0 368 184.021333 184.021333 0 0 0 0-368z"
              fill="#2c2c2c"
              p-id="2876"
            ></path>
          </svg>
        </button>
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item @click="showCreateGroupModal">
              创建群聊
            </el-dropdown-item>
            <el-dropdown-item @click="showApplyContactModal">
              添加用户/群聊
            </el-dropdown-item>
            <el-dropdown-item @click="showNewContactModal">
              新的好友
            </el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>
      
    </div>
  </div>
  <div class="contactlist-body">
    <div class="contactlist-user">
      <el-menu
        router
        unique-opened
        @open="handleShowUserList"
        @close="handleHideUserList"
      >
        <el-sub-menu index="1">
          <template #title>
            <span class="contactlist-user-title">联系人</span>
          </template>
        </el-sub-menu>
        <el-menu-item
          v-for="user in contactUserList"
          :key="user.user_id"
          @click="handleToChatUser(user)"
          class="contactlist-user-menu-item"
        >
          <el-dropdown
            trigger="contextmenu"
            class="contactlist-dropdown"
            placement="right"
          >
            <div class="contactlist-user-item">
              <img :src="user.avatar" class="contactlist-user-avatar" />
              {{ user.user_name }}
            </div>
            <template #dropdown>
              <el-dropdown-menu>
                <el-dropdown-item @click="handleCancelBlack(user)"
                  >解除拉黑</el-dropdown-item
                >
                <!-- 其他菜单项 -->
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </el-menu-item>
      </el-menu>

      <el-menu
        router
        unique-opened
        @open="handleShowMyGroupList"
        @close="handleHideMyGroupList"
      >
        <el-sub-menu index="1">
          <template #title>
            <span class="contactlist-user-title">我创建的群聊</span>
          </template>
        </el-sub-menu>
        <el-menu-item
          v-for="group in myGroupList"
          :key="group.group_id"
          @click="handleToChatGroup(group)"
        >
          <img :src="group.avatar" class="contactlist-avatar" />
          {{ group.group_name }}
        </el-menu-item>
      </el-menu>
      <el-menu
        router
        unique-opened
        @open="handleShowMyJoinedGroupList"
        @close="handleHideMyJoinedGroupList"
      >
        <el-sub-menu index="1">
          <template #title>
            <span class="contactlist-user-title">我加入的群聊</span>
          </template>
        </el-sub-menu>
        <el-menu-item
          v-for="group in myJoinedGroupList"
          :key="group.group_id"
          @click="handleToChatGroup(group)"
        >
          <img :src="group.avatar" class="contactlist-avatar" />
          {{ group.group_name }}
        </el-menu-item>
      </el-menu>
    </div>
  </div>
</div>
```

右边是我的主页整个页面。上面显示了登录用户的个人信息。

#### /user/updateUserInfo

![1739239779287-42eb107a-95a6-4033-b7c6-04f800f20f2e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239779287-42eb107a-95a6-4033-b7c6-04f800f20f2e-620148.png)

![1739239781421-936d4f8b-8a41-4f5d-b7d5-ce89947f1383.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239781421-936d4f8b-8a41-4f5d-b7d5-ce89947f1383-509883.png)

通过点击OwnInfo页面右下角的编辑按钮，可以进入到“修改主页”。

```vue
<Modal :isVisible="isMyInfoModalVisible">
  <template v-slot:header>
    <div class="modal-header">
      <div class="modal-quit-btn-container">
        <button class="modal-quit-btn" @click="quitMyInfoModal">
          <el-icon><Close /></el-icon>
        </button>
      </div>
      <div class="modal-header-title">
        <h3>修改主页</h3>
      </div>
    </div>
  </template>
  <template v-slot:body>
    <el-scrollbar
      max-height="300px"
      style="
        width: 400px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-top: 20px;
      "
    >
      <div class="modal-body">
        <el-form ref="formRef" :model="updateInfo" label-width="70px">
          <el-form-item
            prop="nickname"
            label="昵称"
            :rules="[
              {
                min: 3,
                max: 10,
                message: '昵称长度在 3 到 10 个字符',
                trigger: 'blur',
              },
            ]"
          >
            <el-input
              v-model="updateInfo.nickname"
              placeholder="选填"
            />
          </el-form-item>
          <el-form-item prop="email" label="邮箱">
            <el-input v-model="updateInfo.email" placeholder="选填" />
          </el-form-item>
          <el-form-item prop="birthday" label="生日">
            <el-input
              v-model="updateInfo.birthday"
              placeholder="选填，格式为2024.1.1"
            />
          </el-form-item>
          <el-form-item prop="signature" label="个性签名">
            <el-input
              v-model="updateInfo.signature"
              placeholder="选填"
            />
          </el-form-item>
          <el-form-item prop="avatar" label="头像">
            <el-upload
              v-model:file-list="fileList"
              ref="uploadRef"
              :auto-upload="false"
              :action="uploadPath"
              :on-success="handleUploadSuccess"
              :before-upload="beforeFileUpload"
            >
              <template #trigger>
                <el-button
                  style="background-color: rgb(252, 210.9, 210.9)"
                  >上传图片</el-button
                >
              </template>
            </el-upload>
          </el-form-item>
        </el-form>
      </div>
    </el-scrollbar>
  </template>
  <template v-slot:footer>
    <div class="modal-footer">
      <el-button class="modal-close-btn" @click="closeMyInfoModal">
        完成
      </el-button>
    </div>
  </template>
</Modal>
```

```vue
<template>

  <div class="modal-overlay" v-if="isVisible">
    <div class="modal-content">
      <slot name="header">
      </slot>
      <slot name="body">
      </slot>
      <slot name="footer">
      </slot>
    </div>
  </div>
</template>

<script>
export default {
  name: "Modal",
  props: {
    isVisible: false,
  },
};
</script>
```

这个Modal组件在后面会经常出现，主要分为三部分，header，body以及footer，在这个修改主页功能中，header就代表了标题“修改主页”，body就代表了中间的表单（昵称，邮箱，生日，个性签名，头像），footer就代表了“完成”按钮。

Modal有一个isVisible属性，可以通过属性将Modal内部的变量传递到父页面去，即父页面通过外部变量能控制到Modal内部的变量，:isVisble="isMyInfoModalVisible"。只要外部变量isMyInfoModalVisible为false，则该组件就不会显现出来，如果isMyInfoModalVisible为true，该组件就出现了，通过定义z-index，能够把该组件指定，其他的地方处于无法选中的状态。

![1738838779302-561a8a25-137d-400f-bc7d-e10225e0424c.png](https://cdn.davidingplus.cn/images/2026/09/11/1738838779302-561a8a25-137d-400f-bc7d-e10225e0424c-915217.png)

![1739239788963-a7d33e2d-4614-4db3-998e-4093f7f674eb.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239788963-a7d33e2d-4614-4db3-998e-4093f7f674eb-193247.png)

修改主页Modal前几个项都是输入框，使用的都是el-input。最后用到上传功能，这个功能的前端介绍会放在/message/uploadAvatar来讲，这里先跳过。

```vue
const closeMyInfoModal = async () => {
  if (
    data.updateInfo.nickname == "" &&
    data.fileList.length == 0 &&
    data.updateInfo.email == "" &&
    data.updateInfo.birthday == "" &&
    data.updateInfo.signature == ""
  ) {
    ElMessage("请至少修改一项");
    return;
  }
  if (data.updateInfo.nickname != "") {
    if (
      data.updateInfo.nickname.length < 3 ||
      data.updateInfo.nickname.length > 10
    ) {
      return;
    }
  }
  if (data.updateInfo.email != "") {
    if (!checkEmailValid(data.updateInfo.email)) {
      ElMessage("请输入有效的邮箱。");
      return;
    }
  }
  if (data.updateInfo.nickname != "") {
    data.userInfo.nickname = data.updateInfo.nickname;
  }
  if (data.updateInfo.email != "") {
    data.userInfo.email = data.updateInfo.email;
  }
  if (data.fileList.length != 0) {
    try {
      // 处理上传头像
    } catch (error) {
      console.log(error);
    }
  }

  if (data.updateInfo.birthday != "") {
    data.userInfo.birthday = data.updateInfo.birthday;
  }
  if (data.updateInfo.signature != "") {
    data.userInfo.signature = data.updateInfo.signature;
  }
  data.isMyInfoModalVisible = false;
  data.fileList = [];
  data.cnt = 0;
  data.updateInfo.uuid = data.userInfo.uuid;
  store.commit("setUserInfo", data.userInfo);
  try {
    const rsp = await axios.post(
      store.state.backendUrl + "/user/updateUserInfo",
      data.updateInfo
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
    } else if (rsp.data.code == 400) {
      ElMessage.error(rsp.data.message);
    } else if (rsp.data.code == 500) {
      ElMessage.error(rsp.data.message);
    }
  } catch (error) {
    console.log(error);
  }
  router.go(0);
};
```

当点击footer中的“完成”按钮时，会触发/user/updateUserInfo接口的调用。

先检查是否所有项都没有修改，如果都没有修改，此时点击完成是会提示“请至少修改一项”的。此外，对于邮箱，会检查是否符合邮箱的格式。最后，会将目前更新的数据打包然后发送给后端api接口/user/updateUserInfo。

#### /user/getUserInfo

```vue
<!-- App.vue -->
const getUserInfo = async () => {
  try {
    const req = {
      uuid: store.state.userInfo.uuid,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/user/getUserInfo",
      req
    );
    if (rsp.data.code == 200) {
      if (!rsp.data.data.avatar.startsWith("http")) {
        rsp.data.data.avatar = store.state.backendUrl + rsp.data.data.avatar;
      }
      store.commit("setUserInfo", rsp.data.data);
    } else {
      console.error(rsp.data.message);
    }
    console.log(rsp);
  } catch (error) {
    console.log(error);
  }
};
```

该接口在App.vue的OnMounted函数中调用，当每刷新一次，都会调用OnMounted函数，也就是每次刷新页面，都会调用/user/getUserInfo，获取目前的用户信息，确保实时的用户信息时正确的，而且实时判断用户有没有被禁用，被禁用了需要立即登出。

### 群组功能

#### /group/createGroup

![1739239798399-7bae22b2-b36d-490b-9231-30c72e1d1342.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239798399-7bae22b2-b36d-490b-9231-30c72e1d1342-441185.png)

![1739239800210-fd209ee8-b7c8-4fb2-bec6-c58d6e208d8e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239800210-fd209ee8-b7c8-4fb2-bec6-c58d6e208d8e-023722.png)

当点击如图的图标中的“创建群聊”时，会打开创建群聊的组件。

```vue
<Modal :isVisible="isCreateGroupModalVisible">
  <template v-slot:header>
    <div class="modal-header">
      <div class="modal-quit-btn-container">
        <button class="modal-quit-btn" @click="quitCreateGroupModal">
          <el-icon><Close /></el-icon>
        </button>
      </div>
      <div class="modal-header-title">
        <h3>创建群聊</h3>
      </div>
    </div>
  </template>
  <template v-slot:body>
    <el-scrollbar
      max-height="300px"
      style="
        width: 400px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-top: 20px;
      "
    >
      <div class="creatgroup-modal-body">
        <el-form
          ref="formRef"
          :model="createGroupReq"
          label-width="80px"
          class="demo-dynamic"
        >
          <el-form-item
            prop="name"
            label="群名称"
            :rules="[
              {
                required: true,
                message: '此项为必填项',
                trigger: 'blur',
              },
            ]"
          >
            <el-input
              v-model="createGroupReq.name"
              placeholder="必填"
            />
          </el-form-item>
          <el-form-item prop="notice" label="群公告">
            <el-input
              v-model="createGroupReq.notice"
              type="textarea"
              show-word-limit
              maxlength="500"
              :autosize="{ minRows: 3, maxRows: 3 }"
              placeholder="选填"
            />
          </el-form-item>
          <el-form-item
            prop="add_mode"
            label="加群方式"
            :rules="[
              {
                required: true,
                message: 'Please select activity resource',
                trigger: 'change',
              },
            ]"
          >
            <el-radio-group v-model="createGroupReq.add_mode">
              <el-radio :value="0">直接加入</el-radio>
              <el-radio :value="1">群主审核</el-radio>
            </el-radio-group>
          </el-form-item>
          <el-form-item prop="avatar" label="群头像">
            <el-upload
              v-model:file-list="fileList"
              ref="uploadRef"
              :auto-upload="false"
              :action="uploadPath"
              :on-success="handleUploadSuccess"
              :before-upload="beforeFileUpload"
            >
              <template #trigger>
                <el-button
                  style="background-color: rgb(252, 210.9, 210.9)"
                  >上传图片</el-button
                >
              </template>
            </el-upload>
          </el-form-item>
        </el-form>
      </div>
    </el-scrollbar>
  </template>
  <template v-slot:footer>
    <div class="creategroup-modal-footer">
      <el-button class="modal-close-btn" @click="closeCreateGroupModal">
        完成
      </el-button>
    </div>
  </template>
</Modal>

const handleCreateGroup = async () => {
  try {
    data.createGroupReq.owner_id = data.userInfo.uuid;
    if (data.fileList.length > 0) {
      // 处理上传头像
    }
    const response = await axios.post(
      store.state.backendUrl + "/group/createGroup",
      data.createGroupReq
    );
  } catch (error) {
    console.error(error);
  }
};
```

通过设置el-form的model属性，能够让整个表单输入的数据绑定到createGroupReq变量中，当向输入框键入后，createGroupReq的字段也发生了相应的变化。向创建群聊组件中键入群基础信息后，点击完成，会触发该创建群聊的方法。

#### /group/loadMyGroup

![1739239805572-b1ae6535-e063-4c6f-bcdf-087858ae4016.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239805572-b1ae6535-e063-4c6f-bcdf-087858ae4016-831403.png)

```vue
const handleShowMyGroupList = async () => {
  try {
    data.ownListReq.owner_id = data.userInfo.uuid;
    const loadMyGroupRsp = await axios.post(
      store.state.backendUrl + "/group/loadMyGroup",
      data.ownListReq
    );
    if (loadMyGroupRsp.data.data) {
      for (let i = 0; i < loadMyGroupRsp.data.data.length; i++) {
        if (!loadMyGroupRsp.data.data[i].avatar.startsWith("http")) {
          loadMyGroupRsp.data.data[i].avatar =
            store.state.backendUrl + loadMyGroupRsp.data.data[i].avatar;
        }
      }
    }
    data.myGroupList = loadMyGroupRsp.data.data;
  } catch (error) {
    console.error(error);
  }
};
```

当点击“我创建的群聊”时，会触发handleShowMyGroupList，会调用后端api接口/group/loadMyGroup获取到我创建的群聊列表。最后赋值给myGroupList变量。

```vue
<el-menu
  router
  unique-opened
  @open="handleShowMyGroupList"
  @close="handleHideMyGroupList"
>
  <el-sub-menu index="1">
    <template #title>
      <span class="contactlist-user-title">我创建的群聊</span>
    </template>
  </el-sub-menu>
  <el-menu-item
    v-for="group in myGroupList"
    :key="group.group_id"
    @click="handleToChatGroup(group)"
  >
    <img :src="group.avatar" class="contactlist-avatar" />
    {{ group.group_name }}
  </el-menu-item>
</el-menu>
```

前端会通过myGroupList回显到页面上，这时候通过el-menu-item的v-for把所有的群聊遍历地展现到菜单中。

![1739239809426-aee12091-cb83-4fbe-82f3-3d08b0cfed5a.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239809426-aee12091-cb83-4fbe-82f3-3d08b0cfed5a-776636.png)

#### /group/checkGroupAddMode

![1739239812140-c687d577-8a2f-4d17-806f-00f725c3879d.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239812140-c687d577-8a2f-4d17-806f-00f725c3879d-211426.png)

![1739239816258-f1d92726-40a3-4975-bb2c-34da1c9801ae.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239816258-f1d92726-40a3-4975-bb2c-34da1c9801ae-845303.png)

```vue
<SmallModal :isVisible="isApplyContactModalVisible">
          <template v-slot:header>
            <div class="modal-header">
              <div class="modal-quit-btn-container">
                <button class="modal-quit-btn" @click="quitApplyContactModal">
                  <el-icon><Close /></el-icon>
                </button>
              </div>
              <div class="modal-header-title">
                <h3>添加用户/群聊</h3>
              </div>
            </div>
          </template>
          <template v-slot:body>
            <div class="modal-body">
              <el-form
                ref="formRef"
                :model="applyContactReq"
                label-width="100px"
                class="apply-contact-form"
              >
                <el-form-item
                  prop="name"
                  label="用户/群聊id"
                  :rules="[
                    {
                      required: true,
                      message: '此项为必填项',
                      trigger: 'blur',
                    },
                  ]"
                >
                  <el-input
                    v-model="applyContactReq.contact_id"
                    placeholder="请填写申请的用户/群聊id"
                  />
                </el-form-item>
                <el-form-item prop="message" label="申请消息">
                  <el-input
                    v-model="applyContactReq.message"
                    placeholder="选填，填写更容易通过"
                    type="textarea"
                    show-word-limit
                    maxlength="100"
                    :autosize="{ minRows: 3, maxRows: 3 }"
                  />
                </el-form-item>
              </el-form>
            </div>
          </template>
          <template v-slot:footer>
            <div class="modal-footer">
              <el-button
                class="modal-close-btn"
                @click="closeApplyContactModal"
              >
                完成
              </el-button>
            </div>
          </template>
        </SmallModal>
```

```vue
const handleApplyGroup = async () => {
  try {
    let req = {
      group_id: data.applyContactReq.contact_id,
    };
    let rsp = await axios.post(
      store.state.backendUrl + "/group/checkGroupAddMode",
      req
    );
    if (rsp.data.code == 200) {
      if (rsp.data.data == 0) {
        // 直接加入
        handleEnterDirectly(data.applyContactReq.contact_id);
        return;
      }
    } else {
      ElMessage.error("申请失败");
      return;
    }
    data.applyContactReq.owner_id = data.userInfo.uuid;
    rsp = await axios.post(
      store.state.backendUrl + "/contact/applyContact",
      data.applyContactReq
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      if (rsp.data.message == "申请成功") {
        data.isApplyContactModalVisible = false;
        ElMessage.success("申请成功");
        return;
      } else {
        ElMessage.error(rsp.data.message);
      }
    } else {
      ElMessage.error("申请失败");
    }
  } catch (error) {
    console.error(error);
  }
};
```

当处理的是群聊类型的添加时，点击“完成”，会实现调用后端api接口/group/checkGroupAddMode。检查是直接进群还是群主审核。针对检查结果看是否需要调用api接口/contact/applyContact。

#### /group/enterGroupDirectly

```vue
const handleEnterDirectly = async (groupId) => {
  try {
    const req = {
      owner_id: groupId,
      contact_id: data.userInfo.uuid,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/group/enterGroupDirectly",
      req
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      data.isApplyContactModalVisible = false;
    } else {
      ElMessage.error(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

在handleApplyGroup函数中，如果是检查是直接进群的话，会进入到handleEnterDirectly。

直接进群，需要的req是群组id和登录用户的uuid，获取之后调用后端api接口/group/enterGroupDirectly。针对返回结果判断，如果状态码为200，说明正常完成进群。data.isApplyContactModalVisible = false; 即关掉“添加用户/群聊”组件。

#### /group/leaveGroup

```vue
const handleLeaveGroup = async () => {
  try {
    const req = {
      user_id: data.userInfo.uuid,
      group_id: data.contactInfo.contact_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/group/leaveGroup",
      req
    );
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      console.log(rsp.data.message);
      router.push("/chat/sessionlist");
    } else if (rsp.data.code == 400) {
      ElMessage.warning(rsp.data.message);
      console.log(rsp.data.message);
    } else {
      ElMessage.error(rsp.data.message);
      console.error(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

当点击“退出群聊”后，会触发handleLeaveGroup函数，req需要登录用户id和群组id，调用后端api接口/group/leaveGroup，后端执行相关的离群数据管理。

#### /group/dismissGroup

![1739239822317-f9f8450b-b6a5-4bc9-9b69-f3ebe4f2acb6.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239822317-f9f8450b-b6a5-4bc9-9b69-f3ebe4f2acb6-971302.png)

```vue
const handleDismissGroup = async () => {
  try {
    const req = {
      group_id: data.contactInfo.contact_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/group/dismissGroup",
      req
    );
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      console.log(rsp.data.message);
      router.push("/chat/sessionlist");
    } else if (rsp.data.code == 400) {
      ElMessage.warning(rsp.data.message);
      console.log(rsp.data.message);
    } else {
      ElMessage.error(rsp.data.message);
      console.error(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

点击图片中的“解散群聊”，会触发handleDismissGroup，会调用后端api接口/group/dismissGroup，相关的群聊记录，联系人记录，群聊申请记录，会话记录，以及redis缓存中的相关数据都会被清除掉。

#### /group/updateGroupInfo

![1739239829200-6b9dd7f6-5fb2-41da-8fda-8328fcc1341b.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239829200-6b9dd7f6-5fb2-41da-8fda-8328fcc1341b-893303.png)![1739239829204-c4786879-c975-43c8-9fe8-7e6c250ff059.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239829204-c4786879-c975-43c8-9fe8-7e6c250ff059-461541.png)

```vue
<Modal :isVisible="isUpdateGroupInfoModalVisible">
  <template v-slot:header>
    <div class="updategroupinfo-modal-quit-btn-container">
      <button
        class="updategroupinfo-modal-quit-btn"
        @click="quitUpdateGroupInfoModal"
      >
        <el-icon><Close /></el-icon>
      </button>
    </div>
    <div class="updategroupinfo-modal-header-title">
      <h3>修改群聊信息</h3>
    </div>
  </template>
  <template v-slot:body>
    <el-scrollbar
      max-height="255px"
      style="
        width: 400px;
        height: 255px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-top: 20px;
      "
    >
      <div class="modal-body">
        <el-form
          ref="formRef"
          :model="updateGroupInfo"
          label-width="80px"
        >
          <el-form-item
            prop="name"
            label="群名称"
            :rules="[
              {
                min: 3,
                max: 10,
                message: '群名称长度在 3 到 10 个字符',
                trigger: 'blur',
              },
            ]"
          >
            <el-input
              v-model="updateGroupInfo.name"
              placeholder="选填"
            />
          </el-form-item>
          <el-form-item prop="add_mode" label="入群方式">
            <el-radio-group v-model="updateGroupInfo.add_mode">
              <el-radio :value="0">直接加入</el-radio>
              <el-radio :value="1">群主审核</el-radio>
            </el-radio-group>
          </el-form-item>
          <el-form-item prop="notice" label="群公告">
            <el-input
              v-model="updateGroupInfo.notice"
              type="textarea"
              show-word-limit
              maxlength="500"
              :autosize="{ minRows: 3, maxRows: 3 }"
              placeholder="选填"
            />
          </el-form-item>
          <el-form-item prop="avatar" label="群头像">
            <el-upload
              v-model:file-list="avatarList"
              ref="uploadAvatarRef"
              :auto-upload="false"
              :action="uploadAvatarPath"
              :on-success="handleAvatarUploadSuccess"
              :before-upload="beforeAvatarUpload"
            >
              <template #trigger>
                <el-button
                  style="background-color: rgb(252, 210.9, 210.9)"
                  >上传图片</el-button
                >
              </template>
            </el-upload>
          </el-form-item>
        </el-form>
      </div>
    </el-scrollbar>
  </template>
  <template v-slot:footer>
    <div class="updategroupinfo-modal-footer">
      <el-button
        style="background-color: rgb(252, 210.9, 210.9)"
        @click="closeUpdateGroupInfoModal"
      >
        完成
      </el-button>
    </div>
  </template>
</Modal>
```

```vue
const handleUpdateGroupInfo = async () => {
  try {
    if (
      data.updateGroupInfo.name == "" &&
      data.updateGroupInfo.notice == "" &&
      data.updateGroupInfo.add_mode == -1 &&
      data.avatarList.length == 0
    ) {
      ElMessage.error("请至少修改一项");
      return;
    }
    if (data.avatarList.length > 0) {
      // 处理上传头像
    }
    data.updateGroupInfo.uuid = data.contactInfo.contact_id;
    const rsp = await axios.post(
      store.state.backendUrl + "/group/updateGroupInfo",
      data.updateGroupInfo
    );
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      data.isUpdateGroupInfoModalVisible = false;
    } else {
      ElMessage.error(rsp.data.message);
      console.log(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

更新群聊信息，点击“修改群聊信息”之后，“修改群聊信息”的组件的isVisible属性会变成true。此时该组件会显示出来。通过向组件中对应的群名称、入群方式、群公告、群头像传值。点击“完成”时，会检查是否已经修改了至少一项，最后会向后端api接口/group/updateGroupInfo传入准备好的req。

#### /group/getGroupMemberList

![1739239838441-2660014b-f1cc-4d84-a969-0b62d1b764cb.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239838441-2660014b-f1cc-4d84-a969-0b62d1b764cb-646082.png)

```vue
const getGroupMemberList = async () => {
  const req = {
    group_id: data.contactInfo.contact_id,
  };
  try {
    const rsp = await axios.post(
      store.state.backendUrl + "/group/getGroupMemberList",
      req
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      for (let i = 0; i < rsp.data.data.length; i++) {
        if (!rsp.data.data[i].avatar.startsWith("http")) {
          rsp.data.data[i].avatar =
            store.state.backendUrl + rsp.data.data[i].avatar;
        }
      }
      data.groupMemberList = rsp.data.data;
      console.log(data.groupMemberList);
    } else {
      ElMessage.error(rsp.data.message);
      console.log(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};

const showRemoveGroupMemberModal = async () => {
  await getGroupMemberList();
  data.isRemoveGroupMemberModalVisible = true;
};
```

在点击“移除群组人员”时，会触发showRemoveGroupMemberModal函数，这个函数会先调用getGroupMemberList，获取群组成员列表。只要当获取到群组成员列表时，才能展示群组成员列表，并移除部分群组成员。

当进入到getGroupMemberList中时，req需要用到群组id，之后会调用后端api接口/group/getGroupMemberList，将req传入给后端，随后获取到该群聊成员列表。

#### /group/removeGroupMembers

![1739239845700-83f2a6b3-105a-4eb9-be03-aaba5251c711.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239845700-83f2a6b3-105a-4eb9-be03-aaba5251c711-223440.png)![1739239845689-3975d700-c3cb-48fb-8974-0ecd1094fff0.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239845689-3975d700-c3cb-48fb-8974-0ecd1094fff0-979124.png)

```vue
<Modal :isVisible="isRemoveGroupMemberModalVisible">
  <template v-slot:header>
    <div class="removegroupmember-modal-quit-btn-container">
      <button
        class="removegroupmember-modal-quit-btn"
        @click="quitRemoveGroupMemberModal"
      >
        <el-icon><Close /></el-icon>
      </button>
    </div>
    <div class="removegroupmember-modal-header-title">
      <h3>移除群组人员</h3>
    </div>
  </template>
  <template v-slot:body>
    <span
      style="
        font-size: 14px;
        font-weight: bold;
        font-family: Arial, Helvetica, sans-serif;
        color: rgb(57, 57, 57);
        width: 270px;
        display: flex;
        justify-content: left;
        margin-bottom: 5px;
      "
      >群组成员：</span
    >
    <el-scrollbar
      max-height="400px"
      style="height: 300px; width: 350px"
    >
      <div class="modal-body">
        <ul
          style="list-style-type: none"
          class="removegroupmembers-list"
        >
          <li
            v-for="groupMember in groupMemberList"
            :key="groupMember.user_id"
            class="removegroupmembers-item"
          >
            <div style="display: flex; align-items: center">
              <el-image
                :src="groupMember.avatar"
                class="removegroupmembers-item-avatar"
              />
              <span class="removegroupmembers-item-name">{{
                groupMember.nickname
              }}</span>
            </div>
            <input
              type="checkbox"
              :value="groupMember.user_id"
              v-model="selectedGroupMembers"
              @change="handleCheckboxChange"
            />
          </li>
        </ul>
      </div>
    </el-scrollbar>
  </template>
  <template v-slot:footer>
    <div
      style="
        height: 50px;
        width: 300px;
        display: flex;
        justify-content: right;
      "
    >
      <el-button
        class="removegroupmembers-button"
        @click="handleRemoveGroupMembers"
        >移除所选人员</el-button
      >
    </div>
  </template>
</Modal>
```

每项群成员由el-image，span，input三个部分组成。第一个是头像，第二个是该成员昵称，第三个就是勾选框，给input设置checkbox类型，将input变成勾选框。当勾选的群成员都会存入selectedGroupMembers列表中，每次勾选或者取消勾选都会触发handleCheckboxChange。

```vue
const handleCheckboxChange = () => {
  data.removeGroupMembersList = data.selectedGroupMembers;
  console.log(data.removeGroupMembersList);
};
```

该函数会将勾选的列表存入到data.removeGroupMembersList中。该列表就是需要移除的群成员列表。

```vue
const handleRemoveGroupMembers = async () => {
  const req = {
    group_id: data.contactInfo.contact_id,
    owner_id: data.contactInfo.contact_owner_id,
    uuid_list: data.removeGroupMembersList,
  };
  console.log(data.contactInfo);
  try {
    const rsp = await axios.post(
      store.state.backendUrl + "/group/removeGroupMembers",
      req
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      getGroupMemberList();
    } else if (rsp.data.code == 400) {
      ElMessage.warning(rsp.data.message);
    } else {
      ElMessage.error(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
const showAVContainerModal = () => {
  data.isAVContainerModalVisible = true;
};
```

当点击“移除所选人员”时，会触发handleRemoveGroupMembers。req需要群组id，登录用户id，以及移除人员id列表。需要登录用户id的原因是，防止移除群主，当选到群主的时候，需要提示“不能移除群主”。随后将req传给后端api接口/group/removeGroupMembers。

### 联系人功能

#### /contact/getUserList

![1739239857284-ba2f0a61-1b05-4d12-a70c-b8ee1d0e518e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239857284-ba2f0a61-1b05-4d12-a70c-b8ee1d0e518e-493903.png)![1739239857286-99924ab0-67cf-4be6-80ff-6d0b29ddd488.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239857286-99924ab0-67cf-4be6-80ff-6d0b29ddd488-879926.png)

```vue
<el-menu
  router
  unique-opened
  @open="handleShowUserList"
  @close="handleHideUserList"
>
  <el-sub-menu index="1">
    <template #title>
      <span class="contactlist-user-title">联系人</span>
    </template>
  </el-sub-menu>
  <el-menu-item
    v-for="user in contactUserList"
    :key="user.user_id"
    @click="handleToChatUser(user)"
    class="contactlist-user-menu-item"
  >
    <el-dropdown
      trigger="contextmenu"
      class="contactlist-dropdown"
      placement="right"
    >
      <div class="contactlist-user-item">
        <img :src="user.avatar" class="contactlist-user-avatar" />
        {{ user.user_name }}
      </div>
      <template #dropdown>
        <el-dropdown-menu>
          <el-dropdown-item @click="handleCancelBlack(user)"
            >解除拉黑</el-dropdown-item
          >
          <!-- 其他菜单项 -->
        </el-dropdown-menu>
      </template>
    </el-dropdown>
  </el-menu-item>
</el-menu>
```

```vue
const handleShowUserList = async () => {
  try {
    data.ownListReq.owner_id = data.userInfo.uuid;
    const getUserListRsp = await axios.post(
      store.state.backendUrl + "/contact/getUserList",
      data.ownListReq
    );
    if (getUserListRsp.data.data) {
      for (let i = 0; i < getUserListRsp.data.data.length; i++) {
        if (!getUserListRsp.data.data[i].avatar.startsWith("http")) {
          getUserListRsp.data.data[i].avatar =
            store.state.backendUrl + getUserListRsp.data.data[i].avatar;
        }
      }
    }
    data.contactUserList = getUserListRsp.data.data;
  } catch (error) {
    console.error(error);
  }
};
```

当点击ContactListModal中的联系人菜单时，会触发handleShowUserList函数，这里的req就是登录用户id，将req传给后端api接口/contact/getUserList，会获取到该用户的联系人列表（用户）。将该rsp赋值给data.contactUserList。该list通过el-menu中的el-menu-item展现出来，通过遍历该list，就成功展现出所有的用户（用户的头像，昵称为一项）。

#### /contact/loadMyJoinedGroup

![1739239866784-9d47ff00-b746-42cb-865f-c7c48133f917.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239866784-9d47ff00-b746-42cb-865f-c7c48133f917-417360.png)![1739239866775-b6c1475b-b82c-4af9-ba12-c573a6d2d53b.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239866775-b6c1475b-b82c-4af9-ba12-c573a6d2d53b-999753.png)

```vue
const handleShowMyJoinedGroupList = async () => {
      try {
        data.ownListReq.owner_id = data.userInfo.uuid;
        const loadMyJoinedGroupRsp = await axios.post(
          store.state.backendUrl + "/contact/loadMyJoinedGroup",
          data.ownListReq
        );
        if (loadMyJoinedGroupRsp.data.data) {
          for (let i = 0; i < loadMyJoinedGroupRsp.data.data.length; i++) {
            if (!loadMyJoinedGroupRsp.data.data[i].avatar.startsWith("http")) {
              loadMyJoinedGroupRsp.data.data[i].avatar =
                store.state.backendUrl +
                loadMyJoinedGroupRsp.data.data[i].avatar;
            }
          }
        }
        data.myJoinedGroupList = loadMyJoinedGroupRsp.data.data;
      } catch (error) {
        console.error(error);
      }
    };
```

当点击ContactListModal中的“我创建的群聊”菜单时，会触发handleShowMyJoinedGroupList函数，这里的req就是登录用户id，将req传给后端api接口/contact/loadMyJoinedGroup，会获取到该用户的“我创建的群聊”列表。将该rsp赋值给data.myJoinedGroupList。该list通过el-menu中的el-menu-item展现出来，通过遍历该list，就成功展现出所有的“我创建的群聊”（群聊的头像，群名称为一项）。

#### /contact/getContactInfo

![1739239880035-c81440ba-6db1-4b76-967d-65a81b440724.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239880035-c81440ba-6db1-4b76-967d-65a81b440724-148510.png)![1739239880307-f24d9d42-ff36-4769-ba93-11c64036c573.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239880307-f24d9d42-ff36-4769-ba93-11c64036c573-621101.png)

```vue
<Modal :isVisible="isUserContactInfoModalVisible">
  <template v-slot:header>
    <div class="userinfo-modal-quit-btn-container">
      <button
        class="userinfo-modal-quit-btn"
        @click="quitUserContactInfoModal"
      >
        <el-icon><Close /></el-icon>
      </button>
    </div>
    <div class="userinfo-modal-header-title">
      <h3>个人主页</h3>
    </div>
  </template>
  <template v-slot:body>
    <el-descriptions
      direction="vertical"
      border
      class="modal-list"
      size="small"
    >
      <el-descriptions-item
        :rowspan="2"
        :width="120"
        label="头像"
        align="center"
      >
        <el-image
          style="width: 100px; height: 100px"
          :src="contactInfo.contact_avatar"
        />
      </el-descriptions-item>
      <el-descriptions-item label="Id" :width="140">{{
        contactInfo.contact_id
      }}</el-descriptions-item>
      <el-descriptions-item label="性别">{{
        contactInfo.contact_gender == 0 ? "男" : "女"
      }}</el-descriptions-item>
      <el-descriptions-item label="电话号码">{{
        contactInfo.contact_phone
      }}</el-descriptions-item>
      <el-descriptions-item label="昵称">{{
        contactInfo.contact_name
      }}</el-descriptions-item>

      <el-descriptions-item label="邮箱" :span="2">
        <div style="height: 30px">
          {{ contactInfo.contact_email }}
        </div></el-descriptions-item
      >

      <el-descriptions-item label="生日" :span="1" :width="140"
        >{{ contactInfo.contact_birthday }}
      </el-descriptions-item>
      <el-descriptions-item label="个性签名">
        <div style="height: 70px">
          {{ contactInfo.contact_signature }}
        </div>
      </el-descriptions-item>
    </el-descriptions>
  </template>
</Modal>
```

![1739239889701-9308937d-d02c-43c1-8fc2-02a4173decfc.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239889701-9308937d-d02c-43c1-8fc2-02a4173decfc-940788.png)![1739239889807-42c0ba30-6eea-4465-92bc-5c90d6ab20b2.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239889807-42c0ba30-6eea-4465-92bc-5c90d6ab20b2-670590.png)

```vue
<Modal :isVisible="isGroupContactInfoModalVisible">
  <template v-slot:header>
    <div class="groupcontactinfo-modal-quit-btn-container">
      <button
        class="groupcontactinfo-modal-quit-btn"
        @click="quitGroupContactInfoModal"
      >
        <el-icon><Close /></el-icon>
      </button>
    </div>
    <div class="groupcontactinfo-modal-header-title">
      <h3>群聊主页</h3>
    </div>
  </template>
  <template v-slot:body>
    <el-descriptions
      direction="vertical"
      border
      class="modal-list"
      size="small"
    >
      <el-descriptions-item
        :rowspan="2"
        :width="120"
        label="头像"
        align="center"
      >
        <el-image
          style="width: 100px; height: 100px"
          :src="contactInfo.contact_avatar"
        />
      </el-descriptions-item>
      <el-descriptions-item label="Id" :width="140">{{
        contactInfo.contact_id
      }}</el-descriptions-item>
      <el-descriptions-item label="群人数">{{
        contactInfo.contact_member_cnt
      }}</el-descriptions-item>
      <el-descriptions-item label="群主id">{{
        contactInfo.contact_owner_id
      }}</el-descriptions-item>
      <el-descriptions-item label="入群方式" :width="140"
        >{{
          contactInfo.contact_add_mode == 0
            ? "直接加入"
            : "群主审核"
        }}
      </el-descriptions-item>
      <el-descriptions-item label="群名称" :span="3">{{
        contactInfo.contact_name
      }}</el-descriptions-item>
      <el-descriptions-item label="群公告" :span="3">
        <div style="height: 70px">
          {{ contactInfo.contact_notice }}
        </div>
      </el-descriptions-item>
    </el-descriptions>
  </template>
</Modal>
```

群聊主页和个人主页使用了el-descriptions组件，首先是头像el-descriptions-item，有rowspan属性，设置为占两行。对于群名称和群公告el-descriptions-item，有span属性，意为占多少列的宽度。

```vue
const getChatContactInfo = async (id) => {
  try {
    data.getContactInfoReq.contact_id = id;
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/getContactInfo",
      data.getContactInfoReq
    );
    if (!rsp.data.data.contact_avatar.startsWith("http")) {
      rsp.data.data.contact_avatar =
        store.state.backendUrl + rsp.data.data.contact_avatar;
    }
    data.contactInfo = rsp.data.data;
    console.log(data.contactInfo);
  } catch (error) {
    console.log(error);
  }
};
```

当点开某个会话时，或者当自己是群主，在会话设置中更新群聊信息时，会触发getChatContactInfo，获取到会话的联系人信息（用户、群聊）。这里req需要联系人id。并将req传给后端api接口/contact/getContactInfo。返回的rsp是联系人信息。将rsp数据给到data.contactInfo。此时成功获取到该联系人信息。

#### /contact/deleteContact

![1739239902140-2ba24ccf-5feb-49b6-93fb-3ea4ee599c62.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239902140-2ba24ccf-5feb-49b6-93fb-3ea4ee599c62-517111.png)![1739239902110-91a358ea-bc19-469c-a637-7ef1c2550cb7.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239902110-91a358ea-bc19-469c-a637-7ef1c2550cb7-150715.png)

```vue
const preToDeleteContact = () => {
  try {
    ElMessageBox.confirm("确认删除该联系人？", "Warning", {
      confirmButtonText: "确认",
      cancelButtonText: "取消",
      type: "warning",
    })
      .then(() => {
        deleteContact();
        ElMessage({
          type: "success",
          message: "成功删除",
        });
      })
      .catch(() => {
        ElMessage({
          type: "info",
          message: "取消删除",
        });
      });
  } catch (error) {
    console.error(error);
  }
};
```

当点击和某个用户的会话设置时，会有“删除联系人”的菜单项。点击该项会自动触发preToDeleteContact函数。该函数使用了ElMessageBox消息提示框，confirm函数来判断是否确认要删除该联系人，如果确认删除就触发deleteContact方法，如果取消就提示取消。

```vue
const deleteContact = async () => {
  try {
    const req = {
      owner_id: data.userInfo.uuid,
      contact_id: data.contactInfo.contact_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/deleteContact",
      req
    );
    console.log(rsp.data);
  } catch (error) {
    console.error(error);
  }
  router.push("/chat/sessionlist");
};
```

当触发deleteContact方法后，req需要登录用户id，联系人id。将req传给后端api接口/contact/deleteContact。完成调用。

#### /contact/applyContact

![1739239908540-1f30980f-9262-491d-bfad-6212e762e522.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239908540-1f30980f-9262-491d-bfad-6212e762e522-304798.png)

```vue
const closeApplyContactModal = () => {
  if (data.applyContactReq.contact_id == "") {
    ElMessage.error("请输入申请用户/群组id");
    return;
  }
  if (data.applyContactReq.contact_id[0] == "G") {
    handleApplyGroup();
  } else {
    handleApplyContact();
  }
};

const handleApplyGroup = async () => {
  try {
    let req = {
      group_id: data.applyContactReq.contact_id,
    };
    let rsp = await axios.post(
      store.state.backendUrl + "/group/checkGroupAddMode",
      req
    );
    if (rsp.data.code == 200) {
      if (rsp.data.data == 0) {
        // 直接加入
        handleEnterDirectly(data.applyContactReq.contact_id);
        return;
      }
    } else {
      ElMessage.error("申请失败");
      return;
    }
    data.applyContactReq.owner_id = data.userInfo.uuid;
    rsp = await axios.post(
      store.state.backendUrl + "/contact/applyContact",
      data.applyContactReq
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      if (rsp.data.message == "申请成功") {
        data.isApplyContactModalVisible = false;
        ElMessage.success("申请成功");
        return;
      } else {
        ElMessage.error(rsp.data.message);
      }
    } else {
      ElMessage.error("申请失败");
    }
  } catch (error) {
    console.error(error);
  }
};
  
const handleApplyContact = async () => {
  try {
    data.applyContactReq.owner_id = data.userInfo.uuid;
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/applyContact",
      data.applyContactReq
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      if (rsp.data.message == "申请成功") {
        data.isApplyContactModalVisible = false;
        ElMessage.success("申请成功");
        return;
      }
    }
    ElMessage.error(rsp.data.message);
  } catch (error) {
    console.error(error);
  }
};
```

当点击“添加用户/群聊”的完成按钮时，会触发closeApplyContactModal方法。检查到输入框中的id是群聊id就会触发handleApplyGroup，如果是用户id就会触发handleApplyContact。

当触发handleApplyGroup时，会实现检查是否是直接加群，如果不是直接加群的话，就是群主审核。req包括登录用户id，申请用户id。将req传给后端api接口/contact/applyContact。

当触发handleApplyContact时，也会将req传给后端api接口/contact/applyContact。

后端根据添加相关的申请记录，并返回结果。前端根据rsp的状态码来进行相应信息的回显。

#### /contact/getNewContactList

![1739239917445-8e2334ae-1006-4a0d-b7fa-54f507507f5d.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239917445-8e2334ae-1006-4a0d-b7fa-54f507507f5d-240380.png)![1739239917467-9afdc699-477b-49b0-9d34-d2018cb8a7ad.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239917467-9afdc699-477b-49b0-9d34-d2018cb8a7ad-466448.png)![1739239917429-2aa7fd8b-089d-4f02-9e6e-9cb770b37f43.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239917429-2aa7fd8b-089d-4f02-9e6e-9cb770b37f43-044429.png)

```vue
<SmallModal :isVisible="isNewContactModalVisible">
  <template v-slot:header>
    <div class="modal-header">
      <div class="modal-quit-btn-container">
        <button class="modal-quit-btn" @click="quitNewContactModal">
          <el-icon><Close /></el-icon>
        </button>
      </div>
      <div class="modal-header-title">
        <h3>新的朋友</h3>
      </div>
    </div>
  </template>
  <template v-slot:body>
    <div class="newcontact-modal-body">
      <el-scrollbar max-height="400px">
        <ul class="newcontact-list" style="list-style-type: none">
          <li
            v-for="newContact in newContactList"
            :key="newContact.contact_id"
            class="newcontact-item"
          >
            <div
              style="
                display: flex;
                align-items: center;
                justify-content: center;
              "
            >
              <img
                :src="newContact.contact_avatar"
                style="width: 30px; height: 30px; margin-right: 10px"
              />

              <el-tooltip
                effect="customized"
                :content="newContact.message"
                placement="top"
                hide-after="0"
                enterable="false"
              >
                <div>
                  {{ newContact.contact_name }}
                </div>
              </el-tooltip>
            </div>
            <el-dropdown placement="right" trigger="click">
              <el-button class="action-btn"> 去处理 </el-button>
              <template #dropdown>
                <el-dropdown-menu>
                  <el-dropdown-item
                    @click="handleAgree(newContact.contact_id)"
                    >同意</el-dropdown-item
                  >
                  <el-dropdown-item
                    @click="handleReject(newContact.contact_id)"
                  >
                    拒绝
                  </el-dropdown-item>
                  <el-dropdown-item
                    @click="handleBlack(newContact.contact_id)"
                  >
                    拉黑
                  </el-dropdown-item>
                </el-dropdown-menu>
              </template>
            </el-dropdown>
          </li>
        </ul>
      </el-scrollbar>
    </div>
  </template>
  <template v-slot:footer>
    <div class="newcontact-modal-footer"></div>
  </template>
</SmallModal>
```

```vue
const handleNewContactList = async () => {
  try {
    data.ownListReq.owner_id = data.userInfo.uuid;
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/getNewContactList",
      data.ownListReq
    );
    console.log(rsp);
    data.newContactList = rsp.data.data;
    if (data.newContactList == null) {
      ElMessage.warning("没有新的好友申请");
      return;
    }
    for (let i = 0; i < data.newContactList.length; i++) {
      if (!data.newContactList[i].contact_avatar.startsWith("http")) {
        data.newContactList[i].contact_avatar =
          store.state.backendUrl + data.newContactList[i].contact_avatar;
      }
    }
    data.isNewContactModalVisible = true;
    console.log(rsp);
  } catch (error) {
    console.error(error);
  }
};
```

当点击“新的好友”时，会触发handleNewContactList，req是登录用户id，将req传入后端api接口/contact/getNewContactList。rsp是新的好友申请列表，如果该列表为空，就会在前端提示“没有新的好友申请”。如果不为空，就打开“新的好友”组件。

#### /contact/passContactApply

![1739239927289-66fa6f7a-408d-4c76-9925-c5f04c98351f.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239927289-66fa6f7a-408d-4c76-9925-c5f04c98351f-888559.png)

```vue
const handleAgree = async (contactId) => {
  try {
    const req = {
      owner_id: data.userInfo.uuid,
      contact_id: contactId,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/passContactApply",
      req
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      data.newContactList = data.newContactList.filter(
        (c) => c.contact_id !== contactId
      );
    } else {
      ElMessage.error(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

当打开了“新的好友”组件后，点击某个用户的“去处理”按钮，会弹出来“同意”“拒绝”“拉黑”三个操作。如果点击同意，会触发handleAgree方法，该rep是登录用户id和联系人id，req传入后端api接口/contact/passContactApply。后端完成申请状态切换和联系人记录更新。前端newContactList过滤掉已经处理的用户。

#### /contact/blackContact

![1739239935177-26879e8b-66ad-4a08-8b83-fa362ae47ce9.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239935177-26879e8b-66ad-4a08-8b83-fa362ae47ce9-294721.png)![1739239935172-059a421d-5dba-4fde-8c1f-2a0cff915826.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239935172-059a421d-5dba-4fde-8c1f-2a0cff915826-153659.png)

```vue
const preToBlackContact = () => {
  try {
    ElMessageBox.confirm("确认拉黑该联系人？", "Warning", {
      confirmButtonText: "确认",
      cancelButtonText: "取消",
      type: "warning",
    })
      .then(() => {
        blackContact();
        ElMessage({
          type: "success",
          message: "成功拉黑",
        });
      })
      .catch(() => {
        ElMessage({
          type: "info",
          message: "取消拉黑",
        });
      });
  } catch (error) {
    console.error(error);
  }
};
```

当点击和某个用户的会话设置时，会有“拉黑联系人”的菜单项。点击该项会自动触发preToBlackContact函数。该函数使用了ElMessageBox消息提示框，confirm函数来判断是否确认要拉黑该联系人，如果确认拉黑就触发blackContact方法，如果取消就提示取消。

```vue
const blackContact = async () => {
  try {
    const req = {
      owner_id: data.userInfo.uuid,
      contact_id: data.contactInfo.contact_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/blackContact",
      req
    );
    console.log(rsp.data);
  } catch (error) {
    console.error(error);
  }
  router.push("/chat/sessionlist");
};
```

当触发blackContact方法后，req需要登录用户id，联系人id。将req传给后端api接口/contact/blackContact。完成调用。

#### /contact/cancelBlackContact

![1739239947928-1399983b-8dd8-4c2c-a0e9-7ca71e19eb68.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239947928-1399983b-8dd8-4c2c-a0e9-7ca71e19eb68-268267.png)![1739239948054-e4ab7c42-3391-46f3-8022-35e2c7b3abe4.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239948054-e4ab7c42-3391-46f3-8022-35e2c7b3abe4-370752.png)![1739239948130-e7db4275-d3a4-42eb-bb0e-d9f8cba16cd1.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239948130-e7db4275-d3a4-42eb-bb0e-d9f8cba16cd1-162505.png)

```vue
const handleCancelBlack = async (user) => {
  try {
    const req = {
      owner_id: data.userInfo.uuid,
      contact_id: user.user_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/cancelBlackContact",
      req
    );
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      console.log(rsp.data.message);
    } else if (rsp.data.code == 400) {
      ElMessage.warning(rsp.data.message);
      console.log(rsp.data.message);
    } else if (rsp.data.code == 500) {
      ElMessage.error(rsp.data.message);
      console.log(rsp.data.message);
    }
  } catch (error) {
    ElMessage.error(error);
    console.error(error);
  }
};
```

当点击“拉黑联系人”之后，会拉黑该联系人并删除对应的会话记录。此时再次打开联系人列表，点击该联系人发现，弹出提示“已拉黑对方，先解除拉黑状态才能发起对话”。

此时右键点击该用户，弹出右键菜单，里面有解除拉黑。点击解除拉黑触发handleCancelBlack。该req包括登录用户id和取消拉黑用户id，将req传给后端api接口/contact/cancelBlackContact。返回rsp进行结果回显。

#### /contact/getAddGroupList

![1739239957198-c1d67ce3-ed29-4d7f-ab42-74eb7cb88a3d.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239957198-c1d67ce3-ed29-4d7f-ab42-74eb7cb88a3d-283787.png)![1739239957227-cf0ae1ab-b445-4d8c-9ca5-15f043073a7c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239957227-cf0ae1ab-b445-4d8c-9ca5-15f043073a7c-546532.png)![1739239957180-ac1318da-42e7-4b0c-9b94-30a52f415a10.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239957180-ac1318da-42e7-4b0c-9b94-30a52f415a10-912073.png)

```vue
const handleAddGroupList = async () => {
  try {
    const req = {
      group_id: data.contactInfo.contact_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/getAddGroupList",
      req
    );
    if (rsp.data.code == 200) {
      data.addGroupList = rsp.data.data;
      console.log(rsp.data.data);
      if (data.addGroupList == null) {
        ElMessage.warning("没有新的加群申请");
        return;
      } else {
        for (let i = 0; i < data.addGroupList.length; i++) {
          if (!data.addGroupList[i].contact_avatar.startsWith("http")) {
            data.addGroupList[i].contact_avatar =
              store.state.backendUrl + data.addGroupList[i].contact_avatar;
          }
        }
        data.isAddGroupModalVisible = true;
        console.log(rsp);
      }
    }
  } catch (error) {
    console.log(error);
  }
};
```

当点击“加群申请”时，会触发handleAddGroupList，req是群组id，将req传入后端api接口/contact/getAddGroupList。rsp是新的加群申请列表，如果该列表为空，就会在前端提示“没有新的加群申请”。如果不为空，就打开“加群申请”组件。

#### /contact/refuseContactApply

![1739239962100-85232193-9060-4234-86b3-561e847ef0b4.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239962100-85232193-9060-4234-86b3-561e847ef0b4-851841.png)

```vue
const handleReject = async (contactId) => {
  try {
    const req = {
      owner_id: data.contactInfo.contact_id,
      contact_id: contactId,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/refuseContactApply",
      req
    );
    console.log(rsp);
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      data.addGroupList = data.addGroupList.filter(
        (c) => c.contact_id !== contactId
      );
    } else {
      ElMessage.error(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

当打开了“新的好友”组件后，点击某个用户的“去处理”按钮，会弹出来“同意”“拒绝”“拉黑”三个操作。如果点击拒绝，会触发handleReject方法，该rep是登录用户id和联系人id，req传入后端api接口/contact/refuseContactApply。后端完成申请状态切换和联系人记录更新。前端newContactList过滤掉已经处理的用户。

#### /contact/blackApply

![1739239966999-eb6e805f-39cf-4758-a835-0e4f9155b47e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239966999-eb6e805f-39cf-4758-a835-0e4f9155b47e-145313.png)

```vue
const handleBlack = async (contactId) => {
  try {
    const req = {
      owner_id: data.userInfo.uuid,
      contact_id: contactId,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/contact/blackApply",
      req
    );
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      console.log(rsp.data.message);
      data.newContactList = data.newContactList.filter(
        (c) => c.contact_id !== contactId
      );
    } else if (rsp.data.code == 400) {
      ElMessage.warning(rsp.data.message);
      console.log(rsp.data.message);
    } else if (rsp.data.code == 500) {
      ElMessage.error(rsp.data.message);
      console.log(rsp.data.message);
    }
  } catch (error) {
    ElMessage.error(error);
    console.error(error);
  }
};
```

当打开了“新的好友”组件后，点击某个用户的“去处理”按钮，会弹出来“同意”“拒绝”“拉黑”三个操作。如果点击拉黑，会触发handleBlack方法，该rep是登录用户id和联系人id，req传入后端api接口/contact/blackApply。后端完成申请状态切换和联系人记录更新。前端newContactList过滤掉已经处理的用户。

### 会话功能

#### /session/openSession

![1739239974788-7463195d-76bb-4925-b4f5-65eadeae40cf.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239974788-7463195d-76bb-4925-b4f5-65eadeae40cf-904707.png)![1739239974786-e7f4715e-1c41-455b-8d83-1596c1fffed3.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239974786-e7f4715e-1c41-455b-8d83-1596c1fffed3-894686.png)

```vue
const getSessionId = async (contactId) => {
  try {
    const req = {
      send_id: data.userInfo.uuid,
      receive_id: contactId,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/session/openSession",
      req
    );
    data.sessionId = rsp.data.data;
    console.log(rsp);
  } catch (error) {
    console.error(error);
  }
};

onBeforeRouteUpdate(async (to, from, next) => {
  await getChatContactInfo(to.params.id);
  await getSessionId(router.currentRoute.value.params.id);
  if (data.contactInfo.contact_id[0] == "U") {
    await getMessageList();
  } else {
    await getGroupMessageList();
  }
  console.log(data.sessionId);
  next();
});
// 这是刚渲染/chat/:id页面的时候会调用
onMounted(async () => {
  try {
    /*  */
    console.log(router.currentRoute.value.params.id);
    await getChatContactInfo(router.currentRoute.value.params.id);
    await getSessionId(router.currentRoute.value.params.id);
    console.log(data.contactInfo);
    if (data.contactInfo.contact_id[0] == "U") {
      await getMessageList();
    } else {
      await getGroupMessageList();
    }
    console.log(data.sessionId);
  } catch (error) {
    console.error(error);
  }
});
```

在打开ContactChat.vue界面时，默认与某个联系人发起会话。

![1738931689189-2219a05f-c78d-49b4-a041-3b7abdb7952e.png](https://cdn.davidingplus.cn/images/2026/09/11/1738931689189-2219a05f-c78d-49b4-a041-3b7abdb7952e-713025.png)

当前是和U2025011516051218598发起的会话。

在onBeforeRouteUpdate路径变化前或者onMounted刷新页面时，会先getChatContactInfo获取联系人信息，getSessionId获取会话id，getMessageList获取与用户的聊天记录或者getGroupMessageList获取群聊聊天记录。

特别是当触发了getSessionId获取会话Id时，该req是登录用户id和联系人id。将req传给后端api接口/session/openSession打开会话，返回该会话id。

#### /session/getUserSessionList

![1739239993098-21356140-8a3d-452e-856b-5f6dfb5e60b8.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239993098-21356140-8a3d-452e-856b-5f6dfb5e60b8-054798.png)![1739239993415-7a1a1f54-9c6b-4f5e-b7b9-8baf001b4e93.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239993415-7a1a1f54-9c6b-4f5e-b7b9-8baf001b4e93-842128.png)

```vue
<el-menu
  router
  unique-opened
  @open="handleShowUserSessionList"
  @close="handleHideUserSessionList"
>
  <el-sub-menu index="1">
    <template #title>
      <span class="sessionlist-title">用户</span>
    </template>
  </el-sub-menu>
  <el-menu-item
    v-for="user in userSessionList"
    :key="user.user_id"
    @click="handleToChatUser(user)"
  >
    <img :src="user.avatar" class="sessionlist-avatar" />
    {{ user.user_name }}
  </el-menu-item>
</el-menu>
```

```vue
const handleShowUserSessionList = async () => {
  try {
    data.ownListReq.owner_id = data.userInfo.uuid;
    const userSessionListRsp = await axios.post(
      store.state.backendUrl + "/session/getUserSessionList",
      data.ownListReq
    );
    if (userSessionListRsp.data.data) {
      for (let i = 0; i < userSessionListRsp.data.data.length; i++) {
        if (!userSessionListRsp.data.data[i].avatar.startsWith("http")) {
          userSessionListRsp.data.data[i].avatar =
            store.state.backendUrl + userSessionListRsp.data.data[i].avatar;
        }
      }
    }
    data.userSessionList = userSessionListRsp.data.data;
  } catch (error) {
    console.error(error);
  }
};
```

当点击会话列表中的用户菜单时，会触发handleShowUserSessionList函数，这里的req就是登录用户id，将req传给后端api接口/contact/getUserSessionList，会获取到该用户的用户会话列表。将该rsp赋值给data.userSessionList。该list通过el-menu中的el-menu-item展现出来，通过遍历该list，就成功展现出所有的用户（用户的头像，昵称为一项）。

#### /session/getGroupSessionList

![1739240004134-61e1dc01-5e84-4d8c-8f97-ae709a070006.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240004134-61e1dc01-5e84-4d8c-8f97-ae709a070006-995832.png)![1739240004125-4664603b-218f-49bc-81c4-6e0e9e48acc7.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240004125-4664603b-218f-49bc-81c4-6e0e9e48acc7-427585.png)

```vue
<el-menu
  router
  unique-opened
  @open="handleShowGroupSessionList"
  @close="handleHideGroupSessionList"
>
  <el-sub-menu index="1">
    <template #title>
      <span class="sessionlist-title">群聊</span>
    </template>
  </el-sub-menu>
  <el-menu-item
    v-for="group in groupSessionList"
    :key="group.group_id"
    @click="handleToChatGroup(group)"
  >
    <img :src="group.avatar" class="sessionlist-avatar" />
    {{ group.group_name }}
  </el-menu-item>
</el-menu>
```

```vue
const handleShowGroupSessionList = async () => {
  try {
    data.ownListReq.owner_id = data.userInfo.uuid;
    const groupSessionListRsp = await axios.post(
      store.state.backendUrl + "/session/getGroupSessionList",
      data.ownListReq
    );
    if (groupSessionListRsp.data.data) {
      for (let i = 0; i < groupSessionListRsp.data.data.length; i++) {
        if (!groupSessionListRsp.data.data[i].avatar.startsWith("http")) {
          groupSessionListRsp.data.data[i].avatar =
            store.state.backendUrl +
            groupSessionListRsp.data.data[i].avatar;
        }
      }
    }
    data.groupSessionList = groupSessionListRsp.data.data;
  } catch (error) {
    console.error(error);
  }
};
```

当点击会话列表中的群聊菜单时，会触发handleShowGroupSessionList函数，这里的req就是登录用户id，将req传给后端api接口/contact/getGroupSessionList，会获取到该用户的群聊会话列表。将该rsp赋值给data.groupSessionList。该list通过el-menu中的el-menu-item展现出来，通过遍历该list，就成功展现出所有的群聊（群聊头像，群名称为一项）。

#### /session/deleteSession

![1739240010328-a8a41fd4-6839-4f36-adf1-5627dcfeda4a.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240010328-a8a41fd4-6839-4f36-adf1-5627dcfeda4a-565159.png)![1739240010321-7ff87d75-c9d2-4a72-a54e-4122ffbec557.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240010321-7ff87d75-c9d2-4a72-a54e-4122ffbec557-599247.png)

```vue
const preToDeleteSession = () => {
  try {
    ElMessageBox.confirm("确认删除该会话以及其聊天记录？", "Warning", {
      confirmButtonText: "确认",
      cancelButtonText: "取消",
      type: "warning",
    })
      .then(() => {
        deleteSession();
        ElMessage({
          type: "success",
          message: "成功删除",
        });
      })
      .catch(() => {
        ElMessage({
          type: "info",
          message: "取消删除",
        });
      });
  } catch (error) {
    console.error(error);
  }
};
```

当点击和某个用户的会话设置时，会有“删除该会话”的菜单项。点击该项会自动触发preToDeleteSession函数。该函数使用了ElMessageBox消息提示框，confirm函数来判断是否确认要删除该会话，如果确认删除就触发deleteSession方法，如果取消就提示取消。

```vue
const deleteSession = async () => {
  try {
    const req = {
      owner_id: data.userInfo.uuid,
      session_id: data.sessionId,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/session/deleteSession",
      req
    );
    console.log(rsp.data);
  } catch (error) {
    console.error(error);
  }
  router.push("/chat/sessionlist");
};
```

当触发deleteSession方法后，req需要登录用户id，会话id。将req传给后端api接口/contact/deleteSession。完成调用。

#### /session/checkOpenSessionAllowed

![1739240018838-8de21810-2ec7-4498-bb2d-a58cdfb5f57f.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240018838-8de21810-2ec7-4498-bb2d-a58cdfb5f57f-882796.png)![1739240018800-3f33f5b1-2879-4ee5-9c74-d8bfbc451968.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240018800-3f33f5b1-2879-4ee5-9c74-d8bfbc451968-984871.png)

```vue
const handleToChatUser = async (user) => {
  try {
    const req = {
      send_id: data.userInfo.uuid,
      receive_id: user.user_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/session/checkOpenSessionAllowed",
      req
    );
    if (rsp.data.code == 200) {
      if (rsp.data.data == true) {
        router.push("/chat/" + user.user_id);
      } else {
        ElMessage.warning(rsp.data.message);
        console.error(rsp.data.message);
      }
    } else {
      ElMessage.error(rsp.data.message);
      console.error(rsp.data.message);
    }
  } catch (error) {
    ElMessage.error(error);
    console.error(error);
  }
};
```

```vue
const handleToChatGroup = async (group) => {
  try {
    const req = {
      send_id: data.userInfo.uuid,
      receive_id: group.group_id,
    };
    const rsp = await axios.post(
      store.state.backendUrl + "/session/checkOpenSessionAllowed",
      req
    );
    if (rsp.data.code == 200) {
      if (rsp.data.data == true) {
        router.push("/chat/" + group.group_id);
      } else {
        ElMessage.warning(rsp.data.message);
        console.error(rsp.data.message);
      }
    } else {
      if (rsp.data.code == 400) {
        ElMessage.warning(rsp.data.message);
        console.error(rsp.data.message);
      } else {
        ElMessage.error(rsp.data.message);
        console.error(rsp.data.message);
      }
    }
  } catch (error) {
    console.error(error);
  }
};
```

当点击用户列表和ContactListModal组件中的用户项或者群聊项时，都会触发handleToChatUser或者handleToChatGroup方法，他们在跳转到对应的会话页面时都会先判断是否允许跳转到该会话。该req是登录用户id和联系人id（用户、群聊），req传给后端api接口/session/checkOpenSessionAllowed。后端判断该联系人是否已经被管理员禁用，如果禁用了，就会提示“该联系人已经禁用，无法发起会话”；如果没有禁用，则可以正常跳转。

### 消息功能

#### /message/getMessageList

```vue
const getMessageList = async () => {
  try {
    console.log(data.contactInfo);
    const req = {
      user_one_id: data.userInfo.uuid,
      user_two_id: data.contactInfo.contact_id,
    };
    console.log(req);
    const rsp = await axios.post(
      store.state.backendUrl + "/message/getMessageList",
      req
    );
    if (rsp.data.data) {
      for (let i = 0; i < rsp.data.data.length; i++) {
        if (!rsp.data.data[i].send_avatar.startsWith("http")) {
          rsp.data.data[i].send_avatar =
            store.state.backendUrl + rsp.data.data[i].send_avatar;
        }
      }
    }
    data.messageList = rsp.data.data;
    console.log(data.messageList);
    console.log(rsp);
  } catch (error) {
    console.error(error);
  }
};

onBeforeRouteUpdate(async (to, from, next) => {
  await getChatContactInfo(to.params.id);
  await getSessionId(router.currentRoute.value.params.id);
  if (data.contactInfo.contact_id[0] == "U") {
    await getMessageList();
  } else {
    await getGroupMessageList();
  }
  console.log(data.sessionId);
  next();
});
// 这是刚渲染/chat/:id页面的时候会调用
onMounted(async () => {
  try {
    /*  */
    console.log(router.currentRoute.value.params.id);
    await getChatContactInfo(router.currentRoute.value.params.id);
    await getSessionId(router.currentRoute.value.params.id);
    console.log(data.contactInfo);
    if (data.contactInfo.contact_id[0] == "U") {
      await getMessageList();
    } else {
      await getGroupMessageList();
    }
    console.log(data.sessionId);
  } catch (error) {
    console.error(error);
  }
});
```

在打开ContactChat.vue界面时，默认与某个联系人发起会话。

![1738931689189-2219a05f-c78d-49b4-a041-3b7abdb7952e.png](https://cdn.davidingplus.cn/images/2026/09/11/1738931689189-2219a05f-c78d-49b4-a041-3b7abdb7952e-713025.png)

当前是和U2025011516051218598发起的会话。

在onBeforeRouteUpdate路径变化前或者onMounted刷新页面时，会先getChatContactInfo获取联系人信息，getSessionId获取会话id，getMessageList获取与用户的聊天记录或者getGroupMessageList获取群聊聊天记录。

特别是当触发了getMessageList获取与用户的聊天记录时，该req是登录用户id和聊天用户id。将req传给后端api接口/message/getMessageList，后端返回rsp，rsp是聊天记录，将rsp赋值给data.getMessageList。

#### /message/getGroupMessageList

```vue
const getMessageList = async () => {
  try {
    console.log(data.contactInfo);
    const req = {
      user_one_id: data.userInfo.uuid,
      user_two_id: data.contactInfo.contact_id,
    };
    console.log(req);
    const rsp = await axios.post(
      store.state.backendUrl + "/message/getMessageList",
      req
    );
    if (rsp.data.data) {
      for (let i = 0; i < rsp.data.data.length; i++) {
        if (!rsp.data.data[i].send_avatar.startsWith("http")) {
          rsp.data.data[i].send_avatar =
            store.state.backendUrl + rsp.data.data[i].send_avatar;
        }
      }
    }
    data.messageList = rsp.data.data;
    console.log(data.messageList);
    console.log(rsp);
  } catch (error) {
    console.error(error);
  }
};

onBeforeRouteUpdate(async (to, from, next) => {
  await getChatContactInfo(to.params.id);
  await getSessionId(router.currentRoute.value.params.id);
  if (data.contactInfo.contact_id[0] == "U") {
    await getMessageList();
  } else {
    await getGroupMessageList();
  }
  console.log(data.sessionId);
  next();
});
// 这是刚渲染/chat/:id页面的时候会调用
onMounted(async () => {
  try {
    /*  */
    console.log(router.currentRoute.value.params.id);
    await getChatContactInfo(router.currentRoute.value.params.id);
    await getSessionId(router.currentRoute.value.params.id);
    console.log(data.contactInfo);
    if (data.contactInfo.contact_id[0] == "U") {
      await getMessageList();
    } else {
      await getGroupMessageList();
    }
    console.log(data.sessionId);
  } catch (error) {
    console.error(error);
  }
});
```

在打开ContactChat.vue界面时，默认与某个联系人发起会话。

![1738931689189-2219a05f-c78d-49b4-a041-3b7abdb7952e.png](https://cdn.davidingplus.cn/images/2026/09/11/1738931689189-2219a05f-c78d-49b4-a041-3b7abdb7952e-713025.png)

当前是和U2025011516051218598发起的会话。

在onBeforeRouteUpdate路径变化前或者onMounted刷新页面时，会先getChatContactInfo获取联系人信息，getSessionId获取会话id，getMessageList获取与用户的聊天记录或者getGroupMessageList获取群聊聊天记录。

特别是当触发了getGroupMessageList获取与群聊的聊天记录时，该req是登录用户id和聊天群聊id。将req传给后端api接口/message/getGroupMessageList，后端返回rsp，rsp是聊天记录，将rsp赋值给data.getGroupMessageList。

### 管理员功能

#### /user/getUserInfoList

![1739240029672-4effd5e0-9ead-4b0f-af93-29b02e975b05.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240029672-4effd5e0-9ead-4b0f-af93-29b02e975b05-806586.png)![1739240029616-426091eb-60c2-4fca-b038-e5f4581458db.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240029616-426091eb-60c2-4fca-b038-e5f4581458db-552553.png)

```vue
onMounted(() => {
  getUserList();
});

const getUserList = async () => {
  try {
    const req = {
      owner_id: store.state.userInfo.uuid,
    }
    const rsp = await axios.post(
      store.state.backendUrl + "/user/getUserInfoList", req
    );
    // 存入对应的变量中
    console.log(rsp);
  } catch (error) {
    console.log(error);
  }
};
```

当登录用户信息中admin为1，即该用户为管理员时。点击导航栏的设置，有“管理员模式”菜单项。进入到管理员界面，在渲染时会调用getUserList。该方法的req为登录用户id，将req传给后端api接口/user/getUserInfoList，rsp为用户信息列表，即全部用户的信息。该信息会用于DeleteUserModal.vue，DisableUserModal.vue，SetAdminModal.vue三个界面中。

#### /user/ableUsers

![1739240036613-7d750de1-a0e3-467c-b23e-e086f9674c45.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240036613-7d750de1-a0e3-467c-b23e-e086f9674c45-669621.png)

```vue
<el-table
  :data="disableUserTableData"
  style="width: 100%; height: 90%"
  @selection-change="selectUsers"
>
  <el-table-column type="selection" width="55" />
  <el-table-column prop="uuid" label="Uuid" width="200" />
  <el-table-column
    prop="nickname"
    label="昵称"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column
    prop="telephone"
    label="电话"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column prop="is_admin" label="管理员" width="80" >
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_admin == 0"
        >否</el-button
      >
      <el-button type="primary" v-if="scope.row.is_admin == 1"
        >是</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="删除状态" width="90">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_deleted == false"
        >正常</el-button
      >
      <el-button type="danger" v-if="scope.row.is_deleted == true"
        >已删除</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="禁用状态" width="90">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.status == 0"
        >正常</el-button
      >
      <el-button type="success" v-if="scope.row.status == 1"
        >禁用</el-button
      >
    </template>
  </el-table-column>
</el-table>
```

```vue
const ableUsers = async () => {
  try {
    const req = {
      uuid_list: data.uuidList,
    }
    const rsp = await axios.post(
      store.state.backendUrl + "/user/ableUsers", req);
    console.log(rsp);
    // router.go(0);
    getUserList();
  } catch (error) {
    console.log(error);
  }
};

const selectUsers = (val) => {
  data.uuidList = val.map((item) => item.uuid);
  console.log(data.uuidList);
};
```

点击用户列表，点击“启用/禁用”，会显示用户是否禁用的信息列表。通过勾选左侧的勾选框，会触发selectUsers函数，更新uuidList。当点击“启用/全部启用”时，会触发ableUsers函数，该函数的req是用户id列表，将req传给后端api接口/user/ableUsers。

#### /user/disableUsers

![1739240043486-1de252d2-203b-4b28-af75-706eaca613a6.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240043486-1de252d2-203b-4b28-af75-706eaca613a6-702297.png)

```vue
const disableUsers = async () => {
  try {
    const req = {
      uuid_list: data.uuidList,
    }
    const rsp = await axios.post(
      store.state.backendUrl + "/user/disableUsers", req);
    console.log(rsp);
    // router.go(0);
    getUserList();
  } catch (error) {
    console.log(error);
  }
}

const selectUsers = (val) => {
  data.uuidList = val.map((item) => item.uuid);
  console.log(data.uuidList);
};
```

点击用户列表，点击“启用/禁用”，会显示用户是否禁用的信息列表。通过勾选左侧的勾选框，会触发selectUsers函数，更新uuidList。当点击“禁用/全部禁用”时，会触发disableUsers函数，该函数的req是用户id列表，将req传给后端api接口/user/disableUsers。

#### /user/deleteUsers

![1739240051162-893e9fa0-873e-4de6-abdb-cd33c31a7069.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240051162-893e9fa0-873e-4de6-abdb-cd33c31a7069-390801.png)

```vue

<el-table
  :data="disableUserTableData"
  style="width: 100%; height: 90%"
  @selection-change="selectUsers"
>
  <el-table-column type="selection" width="55" />
  <el-table-column prop="uuid" label="Uuid" width="200" />
  <el-table-column
    prop="nickname"
    label="昵称"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column
    prop="telephone"
    label="电话"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column prop="is_admin" label="管理员" width="80" >
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_admin == 0"
        >否</el-button
      >
      <el-button type="primary" v-if="scope.row.is_admin == 1"
        >是</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="删除状态" width="90">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_deleted == false"
        >正常</el-button
      >
      <el-button type="danger" v-if="scope.row.is_deleted == true"
        >已删除</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="禁用状态" width="90">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.status == 0"
        >正常</el-button
      >
      <el-button type="success" v-if="scope.row.status == 1"
        >禁用</el-button
      >
    </template>
  </el-table-column>
</el-table>
```

```vue
const selectUsers = (val) => {
  data.uuidList = val.map((item) => item.uuid);
  console.log(data.uuidList);
};

const deleteUsers = async () => {
  try {
    const req = {
      uuid_list: data.uuidList,
    };
    const rsp = await axios.post(store.state.backendUrl + "/user/deleteUsers", req);
    console.log(rsp);
    getUserList();
  } catch (error) {
    console.log(error);
  }
};
```

点击用户列表，点击“删除”，会显示用户是否删除的信息列表。通过勾选左侧的勾选框，会触发selectUsers函数，更新uuidList。当点击“删除或全部删除”时，会触发deleteUsers函数，该函数的req是用户id列表，将req传给后端api接口/user/deleteUsers。

#### /user/setAdmin

![1739240067251-d503ef49-0012-4648-9de6-e56264b39f14.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240067251-d503ef49-0012-4648-9de6-e56264b39f14-661369.png)![1739240067207-9ce8a43b-fb40-4b6e-9760-0eb70ce992cf.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240067207-9ce8a43b-fb40-4b6e-9760-0eb70ce992cf-499619.png)

```vue
<el-table
  :data="setAdminTableData"
  style="width: 100%; height: 90%"
  @selection-change="selectUsers"
>
  <el-table-column type="selection" width="55" />
  <el-table-column prop="uuid" label="Uuid" width="200" />
  <el-table-column
    prop="nickname"
    label="昵称"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column
    prop="telephone"
    label="电话"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column prop="is_admin" label="管理员" width="120" >
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_admin == 0"
        >否</el-button
      >
      <el-button type="primary" v-if="scope.row.is_admin == 1"
        >是</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="删除状态" width="120">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_deleted == false"
        >正常</el-button
      >
      <el-button type="danger" v-if="scope.row.is_deleted == true"
        >已删除</el-button
      >
    </template>
  </el-table-column>
</el-table>
```

```vue
const selectUsers = (val) => {
  data.uuidList = val.map((item) => item.uuid);
  console.log(data.uuidList);
};

const setAdmin = async (isAdmin) => {
  try {
    const req = {
      uuid_list: data.uuidList,
      is_admin: isAdmin,
    }
    const rsp = await axios.post(
      store.state.backendUrl + "/user/setAdmin", req);
    console.log(rsp);
    // router.go(0);
    getUserList();
  } catch (error) {
    console.log(error);
  }
};
```

点击用户列表，点击“设置为管理员”，会显示用户是否为管理员的信息列表。通过勾选左侧的勾选框，会触发selectUsers函数，更新uuidList。当点击“取消/全部取消管理员”时，会触发setAdmin函数，此时函数入参isAdmin为0，该函数的req是用户id列表，将req传给后端api接口/user/setAdmin。当点击“设置/全部设置管理员”时，会触发setAdmin函数，此时函数入参isAdmin为1，该函数的req是用户id列表，将req传给后端api接口/user/setAdmin。

#### /group/getGroupInfoList

![1739240077095-7e0ae761-e779-4866-8883-2653e3246ad8.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240077095-7e0ae761-e779-4866-8883-2653e3246ad8-543252.png)![1739240077061-d52c11d2-7064-461a-b9b5-f57cef4ea090.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240077061-d52c11d2-7064-461a-b9b5-f57cef4ea090-187271.png)

```vue
onMounted(() => {
  getGroupInfoList();
});

  const getGroupInfoList = async () => {
  try {
    const rsp = await axios.post(
      store.state.backendUrl + "/group/getGroupInfoList"
    );
    data.deleteGroupTableData = rsp.data.data;
    console.log(rsp);
  } catch (error) {
    console.log(error);
  }
};
```

当登录用户信息中admin为1，即该用户为管理员时。点击导航栏的设置，有“管理员模式”菜单项。进入到管理员界面，在渲染时会调用getGroupInfoList。该方法的没有req，调用后端api接口/user/getUserInfoList，rsp为群聊信息列表，即全部群聊的信息。该信息会用于DeleteGroupModal.vue，DisableGroupModal.vue二个界面中。

#### /group/deleteGroups

![1739240081657-99e257b5-1109-4303-8fc4-415749fc9903.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240081657-99e257b5-1109-4303-8fc4-415749fc9903-027618.png)

```vue
<el-table
  :data="deleteGroupTableData"
  style="width: 100%; height: 90%"
  @selection-change="selectGroups"
>
  <el-table-column type="selection" width="55" />
  <el-table-column prop="uuid" label="Uuid" width="200" />
  <el-table-column
    prop="name"
    label="群名称"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column
    prop="owner_id"
    label="群主id"
    width="200"
    show-overflow-tooltip
  />
  <el-table-column prop="status" label="禁用状态" width="80" >
    <template #default="scope">
      <el-button type="default" v-if="scope.row.status == 0"
        >否</el-button
      >
      <el-button type="primary" v-if="scope.row.status == 1"
        >是</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="删除状态" width="100">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_deleted == false"
        >正常</el-button
      >
      <el-button type="danger" v-if="scope.row.is_deleted == true"
        >已删除</el-button
      >
    </template>
  </el-table-column>
</el-table>
```

```vue
const selectGroups = (val) => {
  data.uuidList = val.map((item) => item.uuid);
  console.log(data.uuidList);
};

const deleteGroups = async () => {
  try {
    const req = {
      uuid_list: data.uuidList,
    };
    const rsp = await axios.post(store.state.backendUrl + "/group/deleteGroups", req);
    console.log(rsp);
    getGroupInfoList();
  } catch (error) {
    console.log(error);
  }
};
```

点击群聊列表，点击“删除/解散群聊”，会显示群聊是否删除的信息列表。通过勾选左侧的勾选框，会触发selectGroups函数，更新uuidList。当点击“删除或全部删除”时，会触发deleteGroups函数，该函数的req是群聊id列表，将req传给后端api接口/user/deleteGroups。

#### /group/setGroupsStatus

![1739240116471-994f85df-13a3-4698-bff7-2324d2bd47da.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240116471-994f85df-13a3-4698-bff7-2324d2bd47da-053309.png)![1739240116488-cd416a4d-2fd2-419d-9d7a-e3238c96c505.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240116488-cd416a4d-2fd2-419d-9d7a-e3238c96c505-676510.png)

```vue
<el-table
  :data="disableGroupTableData"
  style="width: 100%; height: 90%"
  @selection-change="selectGroups"
>
  <el-table-column type="selection" width="55" />
  <el-table-column prop="uuid" label="Uuid" width="200" />
  <el-table-column
    prop="name"
    label="群名称"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column
    prop="owner_id"
    label="群主id"
    width="120"
    show-overflow-tooltip
  />
  <el-table-column prop="status" label="禁用状态" width="120" >
    <template #default="scope">
      <el-button type="default" v-if="scope.row.status == 0"
        >否</el-button
      >
      <el-button type="primary" v-if="scope.row.status == 1"
        >是</el-button
      >
    </template>
  </el-table-column>
  <el-table-column label="删除状态" width="120">
    <template #default="scope">
      <el-button type="default" v-if="scope.row.is_deleted == false"
        >正常</el-button
      >
      <el-button type="danger" v-if="scope.row.is_deleted == true"
        >已删除</el-button
      >
    </template>
  </el-table-column>
</el-table>
```

```vue
const selectGroups = (val) => {
  data.uuidList = val.map((item) => item.uuid);
  console.log(data.uuidList);
};

const setGroupsStatus = async (status) => {
  try {
    const req = {
      uuid_list: data.uuidList,
      status: status,
    };
    const rsp = await axios.post(store.state.backendUrl + "/group/setGroupsStatus", req);
    console.log(rsp);
    getGroupInfoList();
  } catch (error) {
    console.log(error);
  }
};
```

点击群聊列表，点击“启用/禁用”，会显示群聊是否禁用的信息列表。通过勾选左侧的勾选框，会触发selectGroups函数，更新uuidList。当点击“启用/全部启用”时，会触发setGroupsStatus函数，此时函数入参status为0，该函数的req是群聊id列表、状态，将req传给后端api接口/group/setGroupsStatus。当点击“禁用/全部禁用”时，会触发setGroupsStatus函数，此时函数入参status为1，该函数的req是群聊id列表、状态，将req传给后端api接口/group/setGroupsStatus。

### 文件上传下载

#### /message/uploadAvatar

![1739240126745-c2d59c10-747f-4d09-aaf5-5620ba4b0319.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240126745-c2d59c10-747f-4d09-aaf5-5620ba4b0319-933277.png)![1739240126763-ad612d68-ac98-4f23-b0fa-6711170ffda0.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240126763-ad612d68-ac98-4f23-b0fa-6711170ffda0-853189.png)![1739240127261-3bef6673-7167-4dcc-b3f1-146a92f8b5b2.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240127261-3bef6673-7167-4dcc-b3f1-146a92f8b5b2-759383.png)

```vue
<el-form-item prop="avatar" label="群头像">
  <el-upload
    v-model:file-list="avatarList"
    ref="uploadAvatarRef"
    :auto-upload="false"
    :action="uploadAvatarPath"
    :on-success="handleAvatarUploadSuccess"
    :before-upload="beforeAvatarUpload"
  >
    <template #trigger>
      <el-button
        style="background-color: rgb(252, 210.9, 210.9)"
        >上传图片</el-button
      >
    </template>
  </el-upload>
</el-form-item>
```

整个项目有三处用到了头像上传，这里使用表单项el-form-item存储一个el-upload。<font style="color:#262626;background-color:transparent;">v-model:file-list绑定到avatarList，是用来存储上传的文件列表。</font><font style="color:#262626;">auto-upload设为false，意味着选择文件后不会自动上传，需要手动触发，比如在表单提交时调用上传方法。action属性指定了上传的API地址，这里用的是uploadAvatarPath变量。然后有两个事件处理函数：on-success对应上传成功后的处理函数handleAvatarUploadSuccess，before-upload是在上传前执行的函数beforeAvatarUpload，用于文件校验，比如检查文件类型、大小等。</font>

```vue
const data = reactive({
  uploadAvatarRef: null,
  uploadAvatarPath: store.state.backendUrl + "/message/uploadAvatar",
  avatarList: [],
});
```

响应式数据中定义了这三个变量。

比如说在更新群聊信息时：

```vue
const handleUpdateGroupInfo = async () => {
  try {
    if (
      data.updateGroupInfo.name == "" &&
      data.updateGroupInfo.notice == "" &&
      data.updateGroupInfo.add_mode == -1 &&
      data.avatarList.length == 0
    ) {
      ElMessage.error("请至少修改一项");
      return;
    }
    if (data.avatarList.length > 0) {
      data.updateGroupInfo.avatar =
        "/static/avatars/" + data.avatarList[0].name;
      data.uploadAvatarRef.submit();
    }
    data.updateGroupInfo.uuid = data.contactInfo.contact_id;
    const rsp = await axios.post(
      store.state.backendUrl + "/group/updateGroupInfo",
      data.updateGroupInfo
    );
    if (rsp.data.code == 200) {
      ElMessage.success(rsp.data.message);
      data.isUpdateGroupInfoModalVisible = false;
      await getChatContactInfo(router.currentRoute.value.params.id);
    } else {
      ElMessage.error(rsp.data.message);
      console.log(rsp.data.message);
    }
  } catch (error) {
    console.error(error);
  }
};
```

当判断到群头像时，如果data.avatarList.length > 0即头像上传列表不为空，则会获取第一张头像的名字，该req.avatar传入相应的值，通过data.uploadAvatarRef.submit()能够操作对应的el-upload进行上传提交操作。

```vue
const handleAvatarUploadSuccess = () => {
  ElMessage.success("头像上传成功");
  data.avatarList = [];
};
```

这个函数是当头像上传成功的触发函数，会在前端显示“头像上传成功”，并且把上传列表清空。

```vue
const beforeAvatarUpload = (avatar) => {
  console.log("上传前avatar====>", avatar);
  console.log(data.avatarList);
  console.log(avatar);
  if (data.avatarList.length > 1) {
    ElMessage.error("只能上传一张头像");
    return false;
  }
  const isLt50M = avatar.size / 1024 / 1024 < 50;
  if (!isLt50M) {
    ElMessage.error("上传头像图片大小不能超过 50MB!");
    return false;
  }
};
```

这个函数是在上传头像之前的预处理函数，先判断是否有大于1张图片被上传，如果大于1张图片被上传就会提示“只能上传一张头像”。并且需要确定该头像图片大小不能超过50MB。（你也可以设置自己的规定的头像图片大小）

#### /message/uploadFile

![1739240138782-c72e9fa3-903f-411c-94f7-47e648f23719.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240138782-c72e9fa3-903f-411c-94f7-47e648f23719-553932.png)![1739240138884-7a5dc471-3cdc-4e08-9485-11eade73e280.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240138884-7a5dc471-3cdc-4e08-9485-11eade73e280-196926.png)

```vue
<el-tooltip
  effect="customized"
  content="文件上传"
  placement="top"
  hide-after="0"
  enterable="false"
>
  <button class="image-button">
    <el-upload
      v-model:file-list="fileList"
      ref="uploadRef"
      :auto-upload="true"
      :show-file-list="false"
      :action="uploadPath"
      :on-success="handleUploadSuccess"
      :before-upload="beforeFileUpload"
      style="
        display: flex;
        align-items: center;
        justify-content: center;
      "
    >
      <svg
        t="1733503065264"
        class="upload-icon"
        viewBox="0 0 1024 1024"
        version="1.1"
        xmlns="http://www.w3.org/2000/svg"
        p-id="2430"
        width="128"
        height="128"
      >
        <path
          d="M543.7 157v534c0 16.6-13.4 30-30 30s-30-13.4-30-30V157c0-16.6 13.4-30 30-30 16.5 0 30 13.4 30 30z"
          fill=""
          p-id="2431"
        ></path>
        <path
          d="M323.1 331c11.8 11.8 30.7 11.8 42.5 0l119.9-119.9c15.6-15.6 40.9-15.6 56.6 0L662 331c11.7 11.7 30.7 11.7 42.4 0s11.7-30.7 0-42.4L541.7 126.1c-15.6-15.6-41-15.6-56.6 0L323 288.6c-11.6 11.8-11.6 30.7 0.1 42.4zM853.7 913h-680c-33.1 0-60-26.9-60-60V583.7c0-16.4 12.8-30.2 29.2-30.7 16.9-0.4 30.8 13.2 30.8 30v240c0 16.6 13.4 30 30 30h620c16.6 0 30-13.4 30-30V583.7c0-16.4 12.8-30.2 29.2-30.7 16.9-0.4 30.8 13.2 30.8 30v270c0 33.1-26.9 60-60 60z"
          fill=""
          p-id="2432"
        ></path>
      </svg>
    </el-upload>
  </button>
</el-tooltip>
```

这里auto-upload自动上传是true，也就是当点击文件上传时，弹出选择文件窗口，选择文件后，会自动上传，不用再通过upload.submit()函数进行触发式上传。另外show-file-list为false表示不会展现文件列表。

#### /static/files/xxxx

![1739240151194-68f49cb2-bd09-45bc-aed2-3eb6d3d187b7.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240151194-68f49cb2-bd09-45bc-aed2-3eb6d3d187b7-503434.png)![1739240151374-9bf56345-b5e7-4f3c-803a-09ca2324366e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240151374-9bf56345-b5e7-4f3c-803a-09ca2324366e-388489.png)![1739240151558-e636de40-dbb7-4632-afe9-baa063600ed9.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240151558-e636de40-dbb7-4632-afe9-baa063600ed9-861773.png)

```vue
const downloadFile = async (fileName) => {
  try {
    const rsp = await axios.get(
      store.state.backendUrl + "/static/files/" + fileName,
      {
        responseType: "blob",
      }
    );
    console.log(rsp);
    const blob = new Blob([rsp.data], {
      type: rsp.headers["content-type"] || "application/octet-stream",
    });
    const link = document.createElement("a");
    link.href = window.URL.createObjectURL(blob);
    link.download = fileName;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  } catch (error) {
    console.error(error);
  }
};
```

当点击“下载”时，会触发downloadFile方法，会调用后端api接口/static/files/xxx。返回类型是二进制"blob"，<font style="color:rgb(6, 7, 31);background-color:transparent;">意味着服务器响应的内容会被处理为 Blob 对象，这对于处理二进制文件（如图片、视频等）是必要的。</font>

<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">使用从响应中接收到的数据（</font><code><font style="color:rgb(6, 7, 31);">rsp.data</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">）创建一个新的 Blob 对象。Blob 对象表示一个不可变的原始数据的类文件对象。这里还设置了 Blob 的 MIME 类型，如果响应头中没有提供 </font><code><font style="color:rgb(6, 7, 31);">content-type</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">，则默认为 </font><code><font style="color:rgb(6, 7, 31);">"application/octet-stream"</font></code><font style="color:rgb(6, 7, 31);">。</font>

<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">创建一个 </font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"><a></font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> 元素，用于触发文件下载。</font>

<code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">link.href = window.URL.createObjectURL(blob)</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">：为</font><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> </font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"><a></font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> </font><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">元素的</font><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> </font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">href</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> </font><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">属性设置一个对象 URL，这个 URL 指向前面创建的 Blob 对象。</font>

<code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">link.download = fileName</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">：设置 </font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">download</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> 属性，这会提示浏览器以下载而不是导航的方式处理点击事件，同时指定下载的文件名。</font>

<code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">document.body.appendChild(link)</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">：将 </font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"><a></font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> 元素添加到文档的 body 中。这是必要的，因为元素必须存在于 DOM 中才能触发点击事件。</font>

<code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">link.click()</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">：程序化地触发点击事件，开始下载文件。</font>

<code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">document.body.removeChild(link)</font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">：下载开始后，立即从 DOM 中移除 </font><code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"><a></font></code><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"> 元素。</font>

### 聊天功能

#### /wss?client\_id=xxx

```vue
onMounted(() => {
  if (store.state.userInfo.uuid) {
    getUserInfo();
    if (store.state.userInfo.status == 1) {
      logout();
    }
    const wsUrl =
      store.state.wsUrl + "/wss?client_id=" + store.state.userInfo.uuid;
      console.log(wsUrl);
    store.state.socket = new WebSocket(wsUrl);
    store.state.socket.onopen = () => {
      console.log("WebSocket连接已打开");console.log("连接信令服务器成功");
    };
    store.state.socket.onmessage = (message) => {
      console.log("收到消息：", message.data);
    };
    store.state.socket.onclose = () => {
      console.log("WebSocket连接已关闭");
    console.log("连接信令服务器断开");
    };
    store.state.socket.onerror = () => {
      console.log("WebSocket连接发生错误");console.log("连接信令服务器失败，错误信息：", error);
    };
    console.log(store.state.socket);
  }
});

store.state.socket.send(JSON.stringify(chatMessageRequest));
```

在App.vue中，涉及到websocket的连接建立。这里<font style="color:#262626;background-color:rgb(253, 253, 254);">构造WebSocket的URL，该URL由基础WebSocket地址（</font><code><font style="color:#262626;background-color:rgb(253, 253, 254);">store.state.wsUrl</font></code><font style="color:#262626;background-color:rgb(253, 253, 254);">）和用户ID（</font><code><font style="color:#262626;background-color:rgb(253, 253, 254);">store.state.userInfo.uuid</font></code><font style="color:#262626;background-color:rgb(253, 253, 254);">）组成。随后</font><font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">创建一个新的WebSocket实例，并保存到Vuex的状态中，以便在其他地方使用。定义当WebSocket连接成功打开时的回调函数。打印了两条消息到控制台，表示WebSocket连接已打开并成功连接到信令服务器。定义当WebSocket接收到消息时的回调函数。打印接收到的消息内容。定义当WebSocket连接关闭时的回调函数。打印了两条消息到控制台，表示WebSocket连接已关闭并且与信令服务器的连接断开。定义当WebSocket连接发生错误时的回调函数。</font>

<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">通过调用socket.send函数就会发送消息。</font>

<font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);"></font>

#### <font style="color:rgb(6, 7, 31);background-color:rgb(253, 253, 254);">/user/wsLogout</font>

![1739240159040-14902eb7-2f33-4876-b15e-3bc4b9f1cd58.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240159040-14902eb7-2f33-4876-b15e-3bc4b9f1cd58-875289.png)

如果点击“退出登录”会触发下面的代码。

```vue
const logout = async () => {
      store.commit("cleanUserInfo");
      const req = {
        owner_id: data.userInfo.uuid,
      };
      const rsp = await axios.post(
        store.state.backendUrl + "/user/wsLogout",
        req
      );
      if (rsp.data.code == 200) {
        router.push("/login");
        ElMessage.success(rsp.data.message);
      } else {
        ElMessage.error(rsp.data.message);
      }
    };
```

删除Vuex状态管理中的登录用户信息，req是登录用户id，将req传给后端api接口/user/wsLogout。当调用成功后，会直接跳转到/login界面。

另外App.vue也做了logout的功能。

```vue
const logout = async () => {
  store.commit("cleanUserInfo");
  const req = {
    owner_id: data.userInfo.uuid,
  };
  const rsp = await axios.post(
    store.state.backendUrl + "/user/wsLogout",
    req
  );
  if (rsp.data.code == 200) {
    router.push("/login");
    ElMessage.success("账号被封禁，退出登录");
  } else {
    ElMessage.error(rsp.data.message);
  }
};
```

在App.vue，每次刷新会先判断当前用户信息是否已被禁用，如果禁用后，就会触发logout函数。删除Vuex状态管理中的登录用户信息，req是登录用户id，将req传给后端api接口/user/wsLogout。当调用成功后，会直接跳转到/login界面，并提示“账号被封禁，退出登录”。

### 音视频通话功能

#### 发起通话

目前音视频通话功能只支持单聊，不支持群聊。

![1739240163915-e24e1b06-d75c-4896-87d0-2229b60abfa2.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240163915-e24e1b06-d75c-4896-87d0-2229b60abfa2-806711.png)

通过打开和某用户的会话聊天，能够看到“音视频通话”的按钮。

![1739240167001-daf647de-2953-4a9a-abf7-193dccf52ced.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240167001-daf647de-2953-4a9a-abf7-193dccf52ced-063029.png)

```vue
<div
  class="video-modal-overlay"
  v-show="isAVContainerModalVisible"
>
  <div class="video-modal-content">
    <div class="video-modal-header">
      <h2>聊天室</h2>
    </div>
    <div class="video-modal-body">
      <video autoplay playsinline class="local-video"></video>
      <video autoplay playsinline class="remote-video"></video>
    </div>
    <div class="video-modal-footer">
      <el-button
        class="video-modal-footer-btn"
        @click="startCall(true)"
        >发起通话</el-button
      >
      <el-button
        class="video-modal-footer-btn"
        @click="startCall(false)"
        >接收通话</el-button
      >
      <el-button
        class="video-modal-footer-btn"
        @click="rejectCall"
        >拒绝通话</el-button
      >
      <el-button
        class="video-modal-footer-btn"
        @click="sendEndCall"
        >挂断通话</el-button
      >
      <el-button
        class="video-modal-footer-btn"
        @click="closeAVContainerModal"
        >退出聊天室</el-button
      >
    </div>
  </div>
</div>
```

点击音视频通话的按钮后，打开聊天室。

![1739240172379-5766fec6-928b-4e67-99f6-5e0b990722c3.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240172379-5766fec6-928b-4e67-99f6-5e0b990722c3-853507.png)

点击发起通话，会触发startCall方法。入参为true。

```vue
const startCall = async (isInitiator) => {
  console.log(data.localVideo);
  console.log(data.localStream);
  if (data.localVideo) {
    ElMessage.warning("已经在通话中，请勿重复发起");
    return;
  }
  if (isInitiator && !data.ableToStartCall) {
    ElMessage.warning(
      "对方已经发起通话，请先接收通话或拒绝通话，才能发起下一次通话"
    );
    return;
  }
  if (!isInitiator && !data.ableToReceiveOrRejectCall) {
    ElMessage.warning("对方没有发起通话或已在通话中，无法接收通话");
    return;
  }
  createRtcPeerConnection();
  data.localStream = await getLocalMediaStream();
  attachMediaStreamToLocalVideo();
  attachMediaStreamToPeerConnection();
  if (isInitiator) {
    var startCallMessage = {
      messageId: "PROXY",
      type: "start_call",
    };
    const rtcMessageRequest = {
      session_id: data.sessionId,
      type: 3,
      content: "",
      url: "",
      send_id: data.userInfo.uuid,
      send_name: data.userInfo.nickname,
      send_avatar: data.userInfo.avatar,
      receive_id: data.contactInfo.contact_id,
      file_size: "",
      file_name: "",
      file_type: "",
      av_data: JSON.stringify(startCallMessage),
    };
    store.state.socket.send(JSON.stringify(rtcMessageRequest));
  } else {
    var receiveCallMessage = {
      messageId: "PROXY",
      type: "receive_call",
    };
    const rtcMessageRequest = {
      session_id: data.sessionId,
      type: 3,
      content: "",
      url: "",
      send_id: data.userInfo.uuid,
      send_name: data.userInfo.nickname,
      send_avatar: data.userInfo.avatar,
      receive_id: data.contactInfo.contact_id,
      file_size: "",
      file_name: "",
      file_type: "",
      av_data: JSON.stringify(receiveCallMessage),
    };
    store.state.socket.send(JSON.stringify(rtcMessageRequest));
    data.ableToReceiveOrRejectCall = false;
  }
};
```

当入参为true时，说明是有人发起通话，此时先检查data.localVideo画面是否已经被渲染了，如果没有，说明没有发起过通话，则允许进一步发起通话，否则会提示“已经在通话中，请勿重复发起”。通过data.ableToStartCall来判断是否能够发起通话，如果不能发起，说明对方已经发起通话了，自己不用再次发起通话，则提示“对方已经发起通话，请先接收通话或拒绝通话，才能发起下一次通话”。随后触发createRtcPeerConnection。

![1739074362020-adb895c0-9af0-476c-85ce-21c4b448942e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739074362020-adb895c0-9af0-476c-85ce-21c4b448942e-828257.png)

```vue
const createRtcPeerConnection = () => {
  if (data.rtcPeerConn) {
    console.log("peer connection has already been created.");
    return;
  }
  data.rtcPeerConn = new RTCPeerConnection();
  data.rtcPeerConn.onicecandidate = (event) => {
    if (event.candidate) {
      var proxyCandidateMessage = {
        messageId: "PROXY",
        type: "candidate",
        messageData: {
          candidate: event.candidate,
        },
      };
      const rtcMessageRequest = {
        session_id: data.sessionId,
        type: 3,
        content: "",
        url: "",
        send_id: data.userInfo.uuid,
        send_name: data.userInfo.nickname,
        send_avatar: data.userInfo.avatar,
        receive_id: data.contactInfo.contact_id,
        file_size: "",
        file_name: "",
        file_type: "",
        av_data: JSON.stringify(proxyCandidateMessage),
      };
      console.log(rtcMessageRequest);
      store.state.socket.send(JSON.stringify(rtcMessageRequest));
    }
  };
  data.rtcPeerConn.oniceconnectionstatechange = (event) => {
    console.log(
      "oniceconnectionstatechange",
      data.rtcPeerConn.iceConnectionState
    );
  };
  // 对端传来媒体轨道
  data.rtcPeerConn.ontrack = (event) => {
    if (data.remoteStream === null) {
      data.remoteStream = new MediaStream();
      data.remoteVideo = document.querySelector("video.remote-video");
      data.remoteVideo.srcObject = data.remoteStream;
      data.remoteVideo.style.display = "inline-block";
    }
    data.remoteStream.addTrack(event.track);
  };
};
```

如果没有创建过RtcPeerConnection就会创建对应的rtc连接。并定义该rtc连接的onicecandidate触发候选者消息的回调函数。当候选者消息来了之后会新建一条PROXY代理消息，消息类型为“candidate”。该消息通过前端与后端client的websocket连接发出去。再定义一下oniceconnectionstatechange触发P2P连接状态改变的回调函数，打印出连接状态，用于调试。最后定义一下对端传来媒体轨道时的回调函数，如果远端媒体流不存在的话，会定义了远端媒体流和远端视频界面，远端视频界面的资源对象是远端媒体流。最后将该媒体轨道加入到媒体流中。

当执行为了创建rtc连接和基本的初始化之后，会进入到data.localStream = await getLocalMediaStream();获取本地的媒体流。

![1739074347454-8e15d533-bab2-4df7-b76f-159cf13ac04c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739074347454-8e15d533-bab2-4df7-b76f-159cf13ac04c-504147.png)

```vue
const getLocalMediaStream = () => {
  if (!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia) {
    console.log("getUserMedia is not supported!");
    return null;
  } else if (data.localStream) {
    console.log("localStream already exist.");
    return data.localStream;
  } else {
    var constraints = {
      video: true,
      audio: true,
    };
    return navigator.mediaDevices.getUserMedia(constraints);
  }
};
```

获取本地媒体流，先判断该浏览器是否支持获取到本地的媒体流，之后需要判断data.localStream本地媒体流是否存在，如果已经存在了说明获取了，则就不再获取了。否则这里默认为音视频都需要，将该设定作为参数，调用getUserMedia函数。此时浏览器会根据你的设备设定，看是否直接启用麦克风/摄像头。

```vue
const attachMediaStreamToLocalVideo = () => {
  data.localVideo = document.querySelector("video.local-video");
  data.localVideo.srcObject = data.localStream;
  data.localVideo.muted = true;
  data.localVideo.style.display = "inline-block";
};

const attachMediaStreamToPeerConnection = () => {
  data.localStream.getTracks().forEach((track) => {
    data.rtcPeerConn.addTrack(track);
  });
};
```

定义本地的视频界面元素，并将本地媒体流传给本地视频的资源对象。

并将本地媒体流中所有的媒体轨道加入到rtc连接中，传给对端。

之后如果入参是true，会发起一条PROXY代理消息，类型是start\_call。通过websocket连接发送给后端，并将接收通话和拒绝通话置为false，即此时处于发起通话状态，无法接收或拒绝通话。

```vue
store.state.socket.onmessage = (jsonMessage) => {
  const message = JSON.parse(jsonMessage.data);
  if (message.type != 3) {
    if (
      // 群聊过来的消息，且当前会话是该群聊
      (message.receive_id[0] == "G" &&
        message.receive_id == data.contactInfo.contact_id) ||
      // 其他用户过来的消息，且当前会话是该用户
      (message.receive_id[0] == "U" &&
        message.receive_id == data.userInfo.uuid) ||
      // 自己发送的消息
      message.send_id == data.userInfo.uuid
    ) {
      console.log("收到消息：", message);
      if (data.messageList == null) {
        data.messageList = [];
      }
      data.messageList.push(message);
      scrollToBottom();
    }
    // 其他接受的消息都不显示在messageList中，而是通过切换页面或刷新页面getMessageList来获取
  } else {
    var messageAVdata = JSON.parse(message.av_data); // 后端message的该字段命名为av_data
    if (messageAVdata.messageId === "PEER_LEAVE") {
      console.log("接收到PEER_LEAVE消息：", data.userInfo.uuid);
      receiveEndCall();
    } else if (messageAVdata.messageId === "PROXY") {
      console.log("接收到PROXY消息：", message);
      if (messageAVdata.type === "start_call") {
        ElNotification({
          title: "消息提示",
          message: `收到一条来自${message.send_name}的通话请求，请及时前往查看`,
          type: "warning",
        });
        data.ableToReceiveOrRejectCall = true;
        data.ableToStartCall = false;
      } else if (messageAVdata.type === "receive_call") {
        createOffer();
      } else if (messageAVdata.type === "reject_call") {
        endCall();
      } else if (messageAVdata.type === "sdp") {
        if (messageAVdata.messageData.sdp.type === "offer") {
          handleOfferSdp(messageAVdata.messageData.sdp);
        } else if (messageAVdata.messageData.sdp.type === "answer") {
          handleAnswerSdp(messageAVdata.messageData.sdp);
        } else {
          console.log("不支持的sdp类型");
        }
      } else if (messageAVdata.type === "candidate") {
        handleCandidate(messageAVdata.messageData.candidate);
      } else {
        console.log("不支持的proxy类型");
      }
    }
    console.log("收到消息：", message);
    if (data.messageList == null) {
      data.messageList = [];
    }
    data.messageList.push(message);
    scrollToBottom();
  }
};
```

当进入到会话聊天界面时，触发socket.onmessage回调函数，给socket.onmessage回调函数添加收到音视频通话消息时如何处理该消息。

首先如果接收到消息的类型为start\_call，则使用ElNotification来做一个通知，标题为消息提示：“收到一条来自apylee1的通话请求，请及时前往查看”。发起通话置为false。

![1739240181080-96119810-5fd1-48ae-976a-d511411c0d2a.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240181080-96119810-5fd1-48ae-976a-d511411c0d2a-478159.png)

#### 接收通话

![1739240192530-30728bf8-a4a7-4531-b69c-31dabc312941.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240192530-30728bf8-a4a7-4531-b69c-31dabc312941-853093.png)![1739240192641-628a407f-e853-434a-a253-bf2b6c479a9c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240192641-628a407f-e853-434a-a253-bf2b6c479a9c-093866.png)

此时接收方点击“接收通话”，触发start\_call方法，入参为false。

```vue
const startCall = async (isInitiator) => {
  console.log(data.localVideo);
  console.log(data.localStream);
  if (data.localVideo) {
    ElMessage.warning("已经在通话中，请勿重复发起");
    return;
  }
  if (isInitiator && !data.ableToStartCall) {
    ElMessage.warning(
      "对方已经发起通话，请先接收通话或拒绝通话，才能发起下一次通话"
    );
    return;
  }
  if (!isInitiator && !data.ableToReceiveOrRejectCall) {
    ElMessage.warning("对方没有发起通话或已在通话中，无法接收通话");
    return;
  }
  createRtcPeerConnection();
  data.localStream = await getLocalMediaStream();
  attachMediaStreamToLocalVideo();
  attachMediaStreamToPeerConnection();
  if (isInitiator) {
    var startCallMessage = {
      messageId: "PROXY",
      type: "start_call",
    };
    const rtcMessageRequest = {
      session_id: data.sessionId,
      type: 3,
      content: "",
      url: "",
      send_id: data.userInfo.uuid,
      send_name: data.userInfo.nickname,
      send_avatar: data.userInfo.avatar,
      receive_id: data.contactInfo.contact_id,
      file_size: "",
      file_name: "",
      file_type: "",
      av_data: JSON.stringify(startCallMessage),
    };
    store.state.socket.send(JSON.stringify(rtcMessageRequest));
  } else {
    var receiveCallMessage = {
      messageId: "PROXY",
      type: "receive_call",
    };
    const rtcMessageRequest = {
      session_id: data.sessionId,
      type: 3,
      content: "",
      url: "",
      send_id: data.userInfo.uuid,
      send_name: data.userInfo.nickname,
      send_avatar: data.userInfo.avatar,
      receive_id: data.contactInfo.contact_id,
      file_size: "",
      file_name: "",
      file_type: "",
      av_data: JSON.stringify(receiveCallMessage),
    };
    store.state.socket.send(JSON.stringify(rtcMessageRequest));
    data.ableToReceiveOrRejectCall = false;
  }
};
```

接收方也会创建rtc连接。

![1739074329883-c152525b-0905-4b63-9075-d2c55a48414d.png](https://cdn.davidingplus.cn/images/2026/09/11/1739074329883-c152525b-0905-4b63-9075-d2c55a48414d-699751.png)

接收方也会获取本地媒体流。

![1739074315910-809e9015-59ef-4f44-9e41-962b3635e071.png](https://cdn.davidingplus.cn/images/2026/09/11/1739074315910-809e9015-59ef-4f44-9e41-962b3635e071-136451.png)

也会定义本地的视频界面元素，并将本地媒体流传给本地视频的资源对象。

并将本地媒体流中所有的媒体轨道加入到rtc连接中，传给对端。

之后会创建一个PROXY代理消息，类型为receive\_call。将该消息通过websocket连接发送给后端，此时“发起通话”，“拒绝通话”，“接收通话”都置为false。

```vue
store.state.socket.onmessage = (jsonMessage) => {
  const message = JSON.parse(jsonMessage.data);
  if (message.type != 3) {
    if (
      // 群聊过来的消息，且当前会话是该群聊
      (message.receive_id[0] == "G" &&
        message.receive_id == data.contactInfo.contact_id) ||
      // 其他用户过来的消息，且当前会话是该用户
      (message.receive_id[0] == "U" &&
        message.receive_id == data.userInfo.uuid) ||
      // 自己发送的消息
      message.send_id == data.userInfo.uuid
    ) {
      console.log("收到消息：", message);
      if (data.messageList == null) {
        data.messageList = [];
      }
      data.messageList.push(message);
      scrollToBottom();
    }
    // 其他接受的消息都不显示在messageList中，而是通过切换页面或刷新页面getMessageList来获取
  } else {
    var messageAVdata = JSON.parse(message.av_data); // 后端message的该字段命名为av_data
    if (messageAVdata.messageId === "PEER_LEAVE") {
      console.log("接收到PEER_LEAVE消息：", data.userInfo.uuid);
      receiveEndCall();
    } else if (messageAVdata.messageId === "PROXY") {
      console.log("接收到PROXY消息：", message);
      if (messageAVdata.type === "start_call") {
        ElNotification({
          title: "消息提示",
          message: `收到一条来自${message.send_name}的通话请求，请及时前往查看`,
          type: "warning",
        });
        data.ableToReceiveOrRejectCall = true;
        data.ableToStartCall = false;
      } else if (messageAVdata.type === "receive_call") {
        createOffer();
      } else if (messageAVdata.type === "reject_call") {
        endCall();
      } else if (messageAVdata.type === "sdp") {
        if (messageAVdata.messageData.sdp.type === "offer") {
          handleOfferSdp(messageAVdata.messageData.sdp);
        } else if (messageAVdata.messageData.sdp.type === "answer") {
          handleAnswerSdp(messageAVdata.messageData.sdp);
        } else {
          console.log("不支持的sdp类型");
        }
      } else if (messageAVdata.type === "candidate") {
        handleCandidate(messageAVdata.messageData.candidate);
      } else {
        console.log("不支持的proxy类型");
      }
    }
    console.log("收到消息：", message);
    if (data.messageList == null) {
      data.messageList = [];
    }
    data.messageList.push(message);
    scrollToBottom();
  }
};
```

#### 拒绝通话

![1739240219187-0f9ed930-ff71-4791-bcd1-38f49e303de7.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240219187-0f9ed930-ff71-4791-bcd1-38f49e303de7-141245.png)![1739240219416-4018a601-9abe-4c33-b54a-99eecea9d4f1.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240219416-4018a601-9abe-4c33-b54a-99eecea9d4f1-391760.png)

```vue
const rejectCall = () => {
  if (!data.ableToReceiveOrRejectCall) {
    ElMessage.warning("对方没有发起通话或已在通话中，无法拒绝通话");
    return;
  }
  var rejectCallMessage = {
    messageId: "PROXY",
    type: "reject_call",
  };
  const rtcMessageRequest = {
    session_id: data.sessionId,
    type: 3,
    content: "",
    url: "",
    send_id: data.userInfo.uuid,
    send_name: data.userInfo.nickname,
    send_avatar: data.userInfo.avatar,
    receive_id: data.contactInfo.contact_id,
    file_size: "",
    file_name: "",
    file_type: "",
    av_data: JSON.stringify(rejectCallMessage),
  };
  store.state.socket.send(JSON.stringify(rtcMessageRequest));
  data.ableToReceiveOrRejectCall = false;
};
```

当点击“拒绝通话”时，会先判断是否已经处于通话中或正在发起通话，如果是，则提示“对方没有发起通话或已在通话中，无法拒绝通话”。

接着会创建一个PROXY代理消息，类型为reject\_call。前端通过websocket连接转发给后端。并将“接收通话”“拒绝通话”置为false。

```vue
const endCall = () => {
  if (data.localVideo) data.localVideo.style.display = "none";
  if (data.remoteVideo) data.remoteVideo.style.display = "none";
  closeLocalMediaStream();
  closeRtcPeerConnection();
  data.remoteStream = null;
  data.localStream = null;
  data.localVideo = null;
  data.remoteVideo = null;
  data.ableToReceiveOrRejectCall = false;
  data.ableToStartCall = true;
  ElMessage.warning("对方拒绝通话");
};

const closeLocalMediaStream = () => {
  if (data.localStream != null) {
    data.localStream.getTracks().forEach((track) => {
      track.stop();
    });
    data.localStream = null;
  }
};

const closeRtcPeerConnection = () => {
  if (data.rtcPeerConn) {
    data.rtcPeerConn.close();
    data.rtcPeerConn = null;
  }
};
```

此时进入endCall关闭通话方法，所有的本地媒体轨道都停止，远端连接关闭。判断本地视频和远端视频是否还存在，存在会置为null，本地和远端的媒体流也会置为null。此时允许“发起通话”，拒绝“接收通话”“拒绝通话”。

#### 创建offer

当websocket接收到receive\_call类型的消息时，会调用createOffer方法。

![1739074283855-df83f2e0-4382-44a3-88bd-bd3b3f30c824.png](https://cdn.davidingplus.cn/images/2026/09/11/1739074283855-df83f2e0-4382-44a3-88bd-bd3b3f30c824-071655.png)

```vue
const createOffer = () => {
  var offerOpts = {
    offerToReceiveAudio: true,
    offerToReceiveVideo: true,
  };
  data.rtcPeerConn
    .createOffer(offerOpts)
    .then((desc) => {
      data.rtcPeerConn.setLocalDescription(desc);
      var proxySdpMessage = {
        messageId: "PROXY",
        type: "sdp",
        messageData: {
          sdp: desc,
        },
      };
      console.log(desc);
      const rtcMessageRequest = {
        session_id: data.sessionId,
        type: 3,
        content: "",
        url: "",
        send_id: data.userInfo.uuid,
        send_name: data.userInfo.nickname,
        send_avatar: data.userInfo.avatar,
        receive_id: data.contactInfo.contact_id,
        file_size: "",
        file_name: "",
        file_type: "",
        av_data: JSON.stringify(proxySdpMessage),
      };
      store.state.socket.send(JSON.stringify(rtcMessageRequest));
    })
    .catch((err) => {
      console.log(
        `createOffer failed, error name: ${err.name}, error message: ${err.message}`
      );
    });
};
```

在createOffer方法中，首先设置offer默认包含音视频。随后使用rtc连接的createOffer方法，入参为默认设置，返回offer sdp的描述。对于该描述，将它设置为本地描述，并创建一个PROXY消息，类型为sdp，附带数据为offer描述。通过websocket连接发送给后端，如果报错会提示createOffer失败，报错信息等。

```vue
store.state.socket.onmessage = (jsonMessage) => {
  const message = JSON.parse(jsonMessage.data);
  if (message.type != 3) {
    if (
      // 群聊过来的消息，且当前会话是该群聊
      (message.receive_id[0] == "G" &&
        message.receive_id == data.contactInfo.contact_id) ||
      // 其他用户过来的消息，且当前会话是该用户
      (message.receive_id[0] == "U" &&
        message.receive_id == data.userInfo.uuid) ||
      // 自己发送的消息
      message.send_id == data.userInfo.uuid
    ) {
      console.log("收到消息：", message);
      if (data.messageList == null) {
        data.messageList = [];
      }
      data.messageList.push(message);
      scrollToBottom();
    }
    // 其他接受的消息都不显示在messageList中，而是通过切换页面或刷新页面getMessageList来获取
  } else {
    var messageAVdata = JSON.parse(message.av_data); // 后端message的该字段命名为av_data
    if (messageAVdata.messageId === "PEER_LEAVE") {
      console.log("接收到PEER_LEAVE消息：", data.userInfo.uuid);
      receiveEndCall();
    } else if (messageAVdata.messageId === "PROXY") {
      console.log("接收到PROXY消息：", message);
      if (messageAVdata.type === "start_call") {
        ElNotification({
          title: "消息提示",
          message: `收到一条来自${message.send_name}的通话请求，请及时前往查看`,
          type: "warning",
        });
        data.ableToReceiveOrRejectCall = true;
        data.ableToStartCall = false;
      } else if (messageAVdata.type === "receive_call") {
        createOffer();
      } else if (messageAVdata.type === "reject_call") {
        endCall();
      } else if (messageAVdata.type === "sdp") {
        if (messageAVdata.messageData.sdp.type === "offer") {
          handleOfferSdp(messageAVdata.messageData.sdp);
        } else if (messageAVdata.messageData.sdp.type === "answer") {
          handleAnswerSdp(messageAVdata.messageData.sdp);
        } else {
          console.log("不支持的sdp类型");
        }
      } else if (messageAVdata.type === "candidate") {
        handleCandidate(messageAVdata.messageData.candidate);
      } else {
        console.log("不支持的proxy类型");
      }
    }
    console.log("收到消息：", message);
    if (data.messageList == null) {
      data.messageList = [];
    }
    data.messageList.push(message);
    scrollToBottom();
  }
};
```

当接收到sdp类型的消息时，会判断，如果是offer，则会去处理offer类型的sdp消息。

```vue
const handleOfferSdp = (val) => {
  data.rtcPeerConn
    .setRemoteDescription(new RTCSessionDescription(val))
    .then(() => {
      createAnswer();
    })
    .catch((err) => {
      console.log("rtcPeerConn setRemoteDescription failed", err);
    });
};
```

#### 创建answer

此时调用远端连接的setRemoteDescription设置远端描述方法。并创建answer类型的sdp。

![1739074249995-929891d7-ca9a-4838-9e38-f260f025a15c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739074249995-929891d7-ca9a-4838-9e38-f260f025a15c-227715.png)

```vue
const createAnswer = () => {
      data.rtcPeerConn
        .createAnswer()
        .then((desc) => {
          data.rtcPeerConn.setLocalDescription(desc);
          var proxySdpMessage = {
            messageId: "PROXY",
            type: "sdp",
            messageData: {
              sdp: desc,
            },
          };
          console.log(desc);
          const rtcMessageRequest = {
            session_id: data.sessionId,
            type: 3,
            content: "",
            url: "",
            send_id: data.userInfo.uuid,
            send_name: data.userInfo.nickname,
            send_avatar: data.userInfo.avatar,
            receive_id: data.contactInfo.contact_id,
            file_size: "",
            file_name: "",
            file_type: "",
            av_data: JSON.stringify(proxySdpMessage),
          };
          store.state.socket.send(JSON.stringify(rtcMessageRequest));
        })
        .catch((err) => {
          console.log(
            `createAnswer failed, error name: ${err.name}, error message: ${err.message}`
          );
        });
    };
```

在createAnswer方法中，首先设置answer默认包含音视频。随后使用rtc连接的createAnswer方法，入参为默认设置，返回answer sdp的描述。对于该描述，将它设置为本地描述，并创建一个PROXY消息，类型为sdp，附带数据为answer描述。通过websocket连接发送给后端，如果报错会提示createAnswer失败，报错信息等。

```vue
store.state.socket.onmessage = (jsonMessage) => {
  const message = JSON.parse(jsonMessage.data);
  if (message.type != 3) {
    if (
      // 群聊过来的消息，且当前会话是该群聊
      (message.receive_id[0] == "G" &&
        message.receive_id == data.contactInfo.contact_id) ||
      // 其他用户过来的消息，且当前会话是该用户
      (message.receive_id[0] == "U" &&
        message.receive_id == data.userInfo.uuid) ||
      // 自己发送的消息
      message.send_id == data.userInfo.uuid
    ) {
      console.log("收到消息：", message);
      if (data.messageList == null) {
        data.messageList = [];
      }
      data.messageList.push(message);
      scrollToBottom();
    }
    // 其他接受的消息都不显示在messageList中，而是通过切换页面或刷新页面getMessageList来获取
  } else {
    var messageAVdata = JSON.parse(message.av_data); // 后端message的该字段命名为av_data
    if (messageAVdata.messageId === "PEER_LEAVE") {
      console.log("接收到PEER_LEAVE消息：", data.userInfo.uuid);
      receiveEndCall();
    } else if (messageAVdata.messageId === "PROXY") {
      console.log("接收到PROXY消息：", message);
      if (messageAVdata.type === "start_call") {
        ElNotification({
          title: "消息提示",
          message: `收到一条来自${message.send_name}的通话请求，请及时前往查看`,
          type: "warning",
        });
        data.ableToReceiveOrRejectCall = true;
        data.ableToStartCall = false;
      } else if (messageAVdata.type === "receive_call") {
        createOffer();
      } else if (messageAVdata.type === "reject_call") {
        endCall();
      } else if (messageAVdata.type === "sdp") {
        if (messageAVdata.messageData.sdp.type === "offer") {
          handleOfferSdp(messageAVdata.messageData.sdp);
        } else if (messageAVdata.messageData.sdp.type === "answer") {
          handleAnswerSdp(messageAVdata.messageData.sdp);
        } else {
          console.log("不支持的sdp类型");
        }
      } else if (messageAVdata.type === "candidate") {
        handleCandidate(messageAVdata.messageData.candidate);
      } else {
        console.log("不支持的proxy类型");
      }
    }
    console.log("收到消息：", message);
    if (data.messageList == null) {
      data.messageList = [];
    }
    data.messageList.push(message);
    scrollToBottom();
  }
};
```

当接收到sdp类型的消息时，会判断，如果是answer，则会去处理answer类型的sdp消息。

```vue
const handleAnswerSdp = (val) => {
      data.rtcPeerConn
        .setRemoteDescription(new RTCSessionDescription(val))
        .catch((err) => {
          console.log("rtcPeerConn setRemoteDescription failed", err);
        });
    };
```

此时调用远端连接的setRemoteDescription设置远端描述方法。

#### 创建candidate

![1739075549505-a0426e4c-2101-4ab9-9824-9a1d01a1e32b.png](https://cdn.davidingplus.cn/images/2026/09/11/1739075549505-a0426e4c-2101-4ab9-9824-9a1d01a1e32b-552621.png)

```vue
data.rtcPeerConn.onicecandidate = (event) => {
if (event.candidate) {
  var proxyCandidateMessage = {
    messageId: "PROXY",
    type: "candidate",
    messageData: {
      candidate: event.candidate,
    },
  };
  const rtcMessageRequest = {
    session_id: data.sessionId,
    type: 3,
    content: "",
    url: "",
    send_id: data.userInfo.uuid,
    send_name: data.userInfo.nickname,
    send_avatar: data.userInfo.avatar,
    receive_id: data.contactInfo.contact_id,
    file_size: "",
    file_name: "",
    file_type: "",
    av_data: JSON.stringify(proxyCandidateMessage),
  };
  console.log(rtcMessageRequest);
  store.state.socket.send(JSON.stringify(rtcMessageRequest));
}
```

当offer，answer都发放完毕之后，发起端会开始查询本端的后选择，将所有的候选者消息收集之后，会触发onIceCandidate方法。该方法会创建一个PROXY代理消息，类型为candidate。通过前端的websocket连接将候选者信息转发给后端。后端发送给对端用户。

```vue
const handleCandidate = (val) => {
  data.rtcPeerConn.addIceCandidate(new RTCIceCandidate(val));
};
```

当对端用户接收到本端的候选者信息会添加到远端连接中。

到此音视频通话连接成功，p2p连接完全建立。

#### 挂断通话

![1739240238313-1203ae1f-c3c9-4541-ac70-bf847daca34e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240238313-1203ae1f-c3c9-4541-ac70-bf847daca34e-719933.png)![1739240238332-fb037d37-9180-492e-9c67-26f758bd8fac.png](https://cdn.davidingplus.cn/images/2026/09/11/1739240238332-fb037d37-9180-492e-9c67-26f758bd8fac-750735.png)

```vue
const sendEndCall = () => {
  if (data.localVideo == null && data.remoteVideo == null) {
    ElMessage.warning("尚未开始通话，无法挂断");
    return;
  }
  if (data.localVideo) data.localVideo.style.display = "none";
  if (data.remoteVideo) data.remoteVideo.style.display = "none";
  closeLocalMediaStream();
  closeRtcPeerConnection();
  data.remoteStream = null;
  data.localStream = null;
  data.localVideo = null;
  data.remoteVideo = null;
  data.ableToReceiveOrRejectCall = false;
  data.ableToStartCall = true;
  var proxyPeerLeaveMessage = {
    messageId: "PEER_LEAVE",
  };
  const rtcMessageRequest = {
    session_id: data.sessionId,
    type: 3,
    content: "",
    url: "",
    send_id: data.userInfo.uuid,
    send_name: data.userInfo.nickname,
    send_avatar: data.userInfo.avatar,
    receive_id: data.contactInfo.contact_id,
    file_size: "",
    file_name: "",
    file_type: "",
    av_data: JSON.stringify(proxyPeerLeaveMessage),
  };
  store.state.socket.send(JSON.stringify(rtcMessageRequest));
};
```

当点击挂断通话时，会触发sendEndCall方法。所有的本地媒体轨道都停止，远端连接关闭。判断本地视频和远端视频是否还存在，存在会置为null，本地和远端的媒体流也会置为null。此时允许“发起通话”，拒绝“接收通话”“拒绝通话”。

随后会创建一个PEER\_LEAVE离开聊天室消息，通过前端websocket连接发送给后端。

```vue
store.state.socket.onmessage = (jsonMessage) => {
  const message = JSON.parse(jsonMessage.data);
  if (message.type != 3) {
    if (
      // 群聊过来的消息，且当前会话是该群聊
      (message.receive_id[0] == "G" &&
        message.receive_id == data.contactInfo.contact_id) ||
      // 其他用户过来的消息，且当前会话是该用户
      (message.receive_id[0] == "U" &&
        message.receive_id == data.userInfo.uuid) ||
      // 自己发送的消息
      message.send_id == data.userInfo.uuid
    ) {
      console.log("收到消息：", message);
      if (data.messageList == null) {
        data.messageList = [];
      }
      data.messageList.push(message);
      scrollToBottom();
    }
    // 其他接受的消息都不显示在messageList中，而是通过切换页面或刷新页面getMessageList来获取
  } else {
    var messageAVdata = JSON.parse(message.av_data); // 后端message的该字段命名为av_data
    if (messageAVdata.messageId === "PEER_LEAVE") {
      console.log("接收到PEER_LEAVE消息：", data.userInfo.uuid);
      receiveEndCall();
    } else if (messageAVdata.messageId === "PROXY") {
      console.log("接收到PROXY消息：", message);
      if (messageAVdata.type === "start_call") {
        ElNotification({
          title: "消息提示",
          message: `收到一条来自${message.send_name}的通话请求，请及时前往查看`,
          type: "warning",
        });
        data.ableToReceiveOrRejectCall = true;
        data.ableToStartCall = false;
      } else if (messageAVdata.type === "receive_call") {
        createOffer();
      } else if (messageAVdata.type === "reject_call") {
        endCall();
      } else if (messageAVdata.type === "sdp") {
        if (messageAVdata.messageData.sdp.type === "offer") {
          handleOfferSdp(messageAVdata.messageData.sdp);
        } else if (messageAVdata.messageData.sdp.type === "answer") {
          handleAnswerSdp(messageAVdata.messageData.sdp);
        } else {
          console.log("不支持的sdp类型");
        }
      } else if (messageAVdata.type === "candidate") {
        handleCandidate(messageAVdata.messageData.candidate);
      } else {
        console.log("不支持的proxy类型");
      }
    }
    console.log("收到消息：", message);
    if (data.messageList == null) {
      data.messageList = [];
    }
    data.messageList.push(message);
    scrollToBottom();
  }
};
```

当对端接收到PEER\_LEAVE退出聊天室消息时，会触发receiveEndCall方法。

```vue
const receiveEndCall = () => {
  if (data.localVideo) data.localVideo.style.display = "none";
  if (data.remoteVideo) data.remoteVideo.style.display = "none";
  closeLocalMediaStream();
  closeRtcPeerConnection();
  data.remoteStream = null;
  data.localStream = null;
  data.localVideo = null;
  data.remoteVideo = null;
  data.ableToReceiveOrRejectCall = false;
  data.ableToStartCall = true;
  ElMessage.warning("对方已挂断");
};
```

所有的本地媒体轨道都停止，远端连接关闭。判断本地视频和远端视频是否还存在，存在会置为null，本地和远端的媒体流也会置为null。此时允许“发起通话”，拒绝“接收通话”“拒绝通话”。提示“对方已挂断”。

以上就是前端部分的代码讲解，接下来可以看整个项目测试。


> 更新: 2025-03-24 23:12:40  
> 原文: <https://www.yuque.com/chengxuyuancarl/zbfw4l/ab5qmakuibfby9w2>