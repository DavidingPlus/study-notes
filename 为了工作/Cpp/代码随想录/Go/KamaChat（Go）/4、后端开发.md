# 4、后端开发

## 前情提要

参考文献有：

[从Go log库到Zap，怎么打造出好用又实用的Logger-CSDN博客](https://blog.csdn.net/qcrao/article/details/125863186?utm_medium=distribute.pc_relevant.none-task-blog-2~default~baidujs_utm_term~default-4-125863186-blog-139361554.235%5Ev43%5Epc_blog_bottom_relevance_base5\&spm=1001.2101.3001.4242.3\&utm_relevant_index=7)

[第 25 章 - Golang 项目结构\_golang项目结构-CSDN博客](https://blog.csdn.net/hummhumm/article/details/143949493?ops_request_misc=%257B%2522request%255Fid%2522%253A%252226901165bf05a031b18ebb8331db698f%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=26901165bf05a031b18ebb8331db698f\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-143949493-null-null.142^v101^pc_search_result_base3\&utm_term=go%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84\&spm=1018.2226.3001.4187)

[MVC 三层架构案例详细讲解\_c# mvc 分层-CSDN博客](https://blog.csdn.net/BASK2311/article/details/130760490?ops_request_misc=%257B%2522request%255Fid%2522%253A%2522175676639230040d831ecada7dc2e9f4%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=175676639230040d831ecada7dc2e9f4\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_click~default-2-130760490-null-null.142^v101^pc_search_result_base3\&utm_term=mvc\&spm=1018.2226.3001.4187)

[概念POJO、DTO、DAO、PO、BO、VO、QO、ENTITY详解-CSDN博客](https://blog.csdn.net/qq_40610003/article/details/109007539?ops_request_misc=%257B%2522request%255Fid%2522%253A%252290d50203024f2208ff3ba6d48dfac376%2522%252C%2522scm%2522%253A%252220140713.130102334..%2522%257D\&request_id=90d50203024f2208ff3ba6d48dfac376\&biz_id=0\&utm_medium=distribute.pc_search_result.none-task-blog-2~all~sobaiduend~default-1-109007539-null-null.142^v101^pc_search_result_base3\&utm_term=dao%EF%BC%8Cdto\&spm=1018.2226.3001.4187)

[multipart/form-data （一种POST 数据提交的方式）\_multipart form-data-CSDN博客](https://blog.csdn.net/dreamerrrrrr/article/details/111146763)

[【Kafka】Windows下安装Kafka（图文记录详细步骤）\_windows安装kafka-CSDN博客](https://blog.csdn.net/tttzzzqqq2018/article/details/132127105)

[使用mkcert工具生成受信任的SSL证书，解决局域网本地https访问问题\_mkcert局域网证书-CSDN博客](https://blog.csdn.net/qq_45392321/article/details/119676301)

## 项目技术栈

go（gin、gorm）、kafka、mysql、redis、wss、zap、json、tls/ssl等。

## 项目设计

### 建表

所有表都是用软删除的方式，软删除的好处是：通过一个DeletedAt字段修改是否删除，而不是调用delete删除该表项，维护表中索引的花销要更低一些。

#### UserInfo

```go
type UserInfo struct {
	Id            int64          `gorm:"column:id;primaryKey;comment:自增id"`
	Uuid          string         `gorm:"column:uuid;uniqueIndex;type:char(20);comment:用户唯一id"`
	Nickname      string         `gorm:"column:nickname;type:varchar(20);not null;comment:昵称"`
	Telephone     string         `gorm:"column:telephone;index;not null;type:char(11);comment:电话"`
	Email         string         `gorm:"column:email;type:char(30);comment:邮箱"`
	Avatar        string         `gorm:"column:avatar;type:char(255);default:https://cube.elemecdn.com/0/88/03b0d39583f48206768a7534e55bcpng.png;not null;comment:头像"`
	Gender        int8           `gorm:"column:gender;comment:性别，0.男，1.女"`
	Signature     string         `gorm:"column:signature;type:varchar(100);comment:个性签名"`
	Password      string         `gorm:"column:password;type:char(18);not null;comment:密码"`
	Birthday      string         `gorm:"column:birthday;type:char(8);comment:生日"`
	CreatedAt     time.Time      `gorm:"column:created_at;index;type:datetime;not null;comment:创建时间"`
	DeletedAt     gorm.DeletedAt `gorm:"column:deleted_at;type:datetime;comment:删除时间"`
	IsAdmin       int8           `gorm:"column:is_admin;not null;comment:是否是管理员，0.不是，1.是"`
	Status        int8           `gorm:"column:status;not null;comment:状态，0.正常，1.禁用"`
}
```

Uuid是唯一索引，用于唯一识别userinfo。

Telephone是普通索引，之所以不是唯一索引是因为软删除可能会删除某个Telephone，但该表项还保留在数据表中，再次用该Telephone新建时如果是唯一索引，就无法新建，所以也是用普通索引的方式来创建的。

gorm.DeletedAt是gorm框架里专门用于软删除的结构体。

```go
// soft_delete.go
type DeletedAt sql.NullTime

// sql.go
// NullTime represents a time.Time that may be null.
// NullTime implements the Scanner interface so
// it can be used as a scan destination, similar to NullString.
type NullTime struct {
	Time  time.Time
	Valid bool // Valid is true if Time is not NULL
}
```

根据注释可以看出，NullTime中的Time就是time中的Time结构体，还有一个Valid，用于判断该空时间是否有效。如果无效，说明当前时间为空，即还没删除；如果有效，说明当前时间不为空，已经被删除了。

#### GroupInfo

```go
type GroupInfo struct {
	Id        int64           `gorm:"column:id;primaryKey;comment:自增id"`
	Uuid      string          `gorm:"column:uuid;uniqueIndex;type:char(20);not null;comment:群组唯一id"`
	Name      string          `gorm:"column:name;type:varchar(20);not null;comment:群名称"`
	Notice    string          `gorm:"column:notice;type:varchar(500);comment:群公告"`
	Members   json.RawMessage `gorm:"column:members;type:json;comment:群组成员"`
	MemberCnt int             `gorm:"column:member_cnt;default:1;comment:群人数"` // 默认群主1人
	OwnerId   string          `gorm:"column:owner_id;type:char(20);not null;comment:群主uuid"`
	AddMode   int8            `gorm:"column:add_mode;default:0;comment:加群方式，0.直接，1.审核"`
	Avatar    string          `gorm:"column:avatar;type:char(255);default:https://cube.elemecdn.com/0/88/03b0d39583f48206768a7534e55bcpng.png;not null;comment:头像"`
	Status    int8            `gorm:"column:status;default:0;comment:状态，0.正常，1.禁用，2.解散"`
	CreatedAt time.Time       `gorm:"column:created_at;index;type:datetime;not null;comment:创建时间"`
	DeletedAt gorm.DeletedAt  `gorm:"column:deleted_at;index;comment:删除时间"`
}
```

Members是按理是一个\[]string类型的数组，其中存储着每个群成员的uuid。但是gorm不支持直接存入\[]string，所以使用到了json.RawMessage，将\[]string序列化成一个\[]byte进行存储，取出来的时候需要反序列化。

```go
// stream.go
// RawMessage is a raw encoded JSON value.
// It implements Marshaler and Unmarshaler and can
// be used to delay JSON decoding or precompute a JSON encoding.
type RawMessage []byte
```

#### UserContact

```go
type UserContact struct {
	Id          int64          `gorm:"column:id;primaryKey;comment:自增id"`
	UserId      string         `gorm:"column:user_id;index;type:char(20);not null;comment:用户唯一id"`
	ContactId   string         `gorm:"column:contact_id;index;type:char(20);not null;comment:对应联系id"`
	ContactType int8           `gorm:"column:contact_type;not null;comment:联系类型，0.用户，1.群聊"`
	Status      int8           `gorm:"column:status;not null;comment:联系状态，0.正常，1.拉黑，2.被拉黑，3.删除好友，4.被删除好友，5.被禁言，6.退出群聊，7.被踢出群聊"`
	CreatedAt   time.Time      `gorm:"column:created_at;type:datetime;not null;comment:创建时间"`
	DeletedAt   gorm.DeletedAt `gorm:"column:deleted_at;type:datetime;index;comment:删除时间"`
}
```

UserId是只有用户类型，ContactId包含了用户和群聊两种类型。

#### ContactApply

```go
type ContactApply struct {
	Id          int64          `gorm:"column:id;primaryKey;comment:自增id"`
	Uuid        string         `gorm:"column:uuid;uniqueIndex;type:char(20);comment:申请id"`
	UserId      string         `gorm:"column:user_id;index;type:char(20);not null;comment:申请人id"`
	ContactId   string         `gorm:"column:contact_id;index;type:char(20);not null;comment:被申请id"`
	ContactType int8           `gorm:"column:contact_type;not null;comment:被申请类型，0.用户，1.群聊"`
	Status      int8           `gorm:"column:status;not null;comment:申请状态，0.申请中，1.通过，2.拒绝，3.拉黑"`
	Message     string         `gorm:"column:message;type:varchar(100);comment:申请信息"`
	LastApplyAt time.Time      `gorm:"column:last_apply_at;type:datetime;not null;comment:最后申请时间"`
	DeletedAt   gorm.DeletedAt `gorm:"column:deleted_at;index;type:datetime;comment:删除时间"`
}
```

LastApplyAt字段支持多次申请，每次申请会更新申请时间。

#### Session

```go
type Session struct {
	Id            int64          `gorm:"column:id;primaryKey;comment:自增id"`
	Uuid          string         `gorm:"column:uuid;uniqueIndex;type:char(20);comment:会话uuid"`
	SendId        string         `gorm:"column:send_id;index;type:char(20);not null;comment:创建会话人id"`
	ReceiveId     string         `gorm:"column:receive_id;index;type:char(20);not null;comment:接受会话人id"`
	ReceiveName   string         `gorm:"column:receive_name;type:varchar(20);not null;comment:名称"`
	Avatar        string         `gorm:"column:avatar;type:char(255);default:default_avatar.png;not null;comment:头像"`
	CreatedAt     time.Time      `gorm:"column:created_at;index;type:datetime;comment:创建时间"`
	DeletedAt     gorm.DeletedAt `gorm:"column:deleted_at;index;type:datetime;comment:删除时间"`
}
```

SendId是用户类型，ReceiveId是用户或者群聊类型。

#### Message

```go
type Message struct {
	Id         int64     `gorm:"column:id;primaryKey;comment:自增id"`
	Uuid       string    `gorm:"column:uuid;uniqueIndex;type:char(20);not null;comment:消息uuid"`
	SessionId  string    `gorm:"column:session_id;index;type:char(20);not null;comment:会话uuid"`
	Type       int8      `gorm:"column:type;not null;comment:消息类型，0.文本，1.文件，2.通话"` // 通话不用存消息内容或者url
	Content    string    `gorm:"column:content;type:TEXT;comment:消息内容"`
	Url        string    `gorm:"column:url;type:char(255);comment:消息url"`
	SendId     string    `gorm:"column:send_id;index;type:char(20);not null;comment:发送者uuid"`
	SendName   string    `gorm:"column:send_name;type:varchar(20);not null;comment:发送者昵称"`
	SendAvatar string    `gorm:"column:send_avatar;type:varchar(255);not null;comment:发送者头像"`
	ReceiveId  string    `gorm:"column:receive_id;index;type:char(20);not null;comment:接受者uuid"`
	FileType   string    `gorm:"column:file_type;type:char(10);comment:文件类型"`
	FileName   string    `gorm:"column:file_name;type:varchar(50);comment:文件名"`
	FileSize   string    `gorm:"column:file_size;type:char(20);comment:文件大小"`
	Status     int8      `gorm:"column:status;not null;comment:状态，0.未发送，1.已发送"`
	CreatedAt  time.Time `gorm:"column:created_at;not null;comment:创建时间"`
	AVdata     string    `gorm:"column:av_data;comment:通话传递数据"`
}
```

根据不同的消息类型，使用到的表字段也不同，比如如果是文本的话，会使用到Content；如果是文件的话，会使用到Url、FileType、FileName、FileSize；如果是通话的话，会使用到AVdata。

上述表设计是比较基础的表设计，如果各位同学有个性化的想法比如通过会话列表可以看到最新消息的话，可以向Session添加“最新消息”“最近接收的时间”；

```go
LastMessage   string `gorm:"column:last_message;type:TEXT;comment:最新的消息"`
LastMessageAt time.Time `gorm:"column:last_message_at;type:datetime;comment:最近接收时间"`
```

或者可以尝试给消息类型添加一个语音，并实现具体的功能。

### config全局配置

```go
[mainConfig]
appName = "your app name"
host = "127.0.0.1"
port = 8000

[mysqlConfig]
host = "127.0.0.1"
port = 3306
user = "root"
password = "123456"
databaseName = "your database name"

[redisConfig]
host = "127.0.0.1"
port = 6379
password = "123456"
db = 0

[authCodeConfig]
accessKeyID = "your accessKeyID in alibaba cloud"
accessKeySecret = "your accessKeySecret in alibaba cloud"
signName = "阿里云短信测试"
templateCode = "SMS_154950909"

[logConfig]
logPath = "your log path"

[kafkaConfig]
messageMode = "channel"# 消息模式 channel or kafka
hostPort = "127.0.0.1:9092" # "127.0.0.1:9092,127.0.0.1:9093,127.0.0.1:9094" 多个kafka服务器
chatTopic = "chat_message"
partition = 0 # kafka partition
timeout = 1 # 单位秒

[staticSrcConfig]
staticAvatarPath = "./static/avatars"
staticFilePath = "./static/files"
```

项目使用toml文件作为配置文件。

```go
type MainConfig struct {
    AppName string `toml:"appName"`
    Host    string `toml:"host"`
    Port    int    `toml:"port"`
}

type MysqlConfig struct {
    Host         string `toml:"host"`
    Port         int    `toml:"port"`
    User         string `toml:"user"`
    Password     string `toml:"password"`
    DatabaseName string `toml:"databaseName"`
}

type RedisConfig struct {
    Host     string `toml:"host"`
    Port     int    `toml:"port"`
    Password string `toml:"password"`
    Db       int    `toml:"db"`
}

type AuthCodeConfig struct {
    AccessKeyID     string `toml:"accessKeyID"`
    AccessKeySecret string `toml:"accessKeySecret"`
    SignName        string `toml:"signName"`
    TemplateCode    string `toml:"templateCode"`
}

type LogConfig struct {
    LogPath string `toml:"logPath"`
}

type KafkaConfig struct {
    MessageMode string        `toml:"messageMode"`
    HostPort    string        `toml:"hostPort"`
    LoginTopic  string        `toml:"loginTopic"`
    LogoutTopic string        `toml:"logoutTopic"`
    ChatTopic   string        `toml:"chatTopic"`
    Partition   int           `toml:"partition"`
    Timeout     time.Duration `toml:"timeout"`
}

type StaticSrcConfig struct {
    StaticAvatarPath string `toml:"staticAvatarPath"`
    StaticFilePath   string `toml:"staticFilePath"`
}

type Config struct {
    MainConfig      `toml:"mainConfig"`
    MysqlConfig     `toml:"mysqlConfig"`
    RedisConfig     `toml:"redisConfig"`
    AuthCodeConfig  `toml:"authCodeConfig"`
    LogConfig       `toml:"logConfig"`
    KafkaConfig     `toml:"kafkaConfig"`
    StaticSrcConfig `toml:"staticSrcConfig"`
}

var config *Config

func LoadConfig() error {
    if _, err := toml.DecodeFile("F:\\go\\kama-chat-server\\configs\\config_local.toml", config); err != nil {
        log.Fatal(err.Error())
        return err
    }
    return nil
}

func GetConfig() *Config {
    if config == nil {
        config = new(Config)
        _ = LoadConfig()
    }
    return config
}
```

使用github上面的开源库"github.com/BurntSushi/toml"写一套接口来读取toml文件中的内容，项目中如果要访问配置直接通过config.GetConfig()就可以获取到对应的配置信息结构体。

### 日志库

#### logger

一个好的日志库能够很好地被应用在各种项目需求中，这里我们要开发一个日志库，为了让它能够用在更多的情况下，它需要具备以下的能力：

> 1. 支持把日志写入到多个输出流中，比如测试开发环境到控制台和日志文件，生产环境到日志文件。
> 2. 多级别日志等级。常见有INFO，WARN，ERROR，FATAL，DEBUG，TRACE等。
> 3. 支持结构化输出。比如JSON格式输出，方便日志传输交互。
> 4. 支持日志切割。按照日期，文件大小等对日志进行切割。
> 5. 辅助信息。除了业务信息之外，还要有定位信息，包括函数，行号，时间等。

go语言有原生的logger，但是它有一些弊端：

> 1. 只有Print/Println，不支持INFO，DEBUG等多日志级别。
> 2. 错误日志只提供FATAL，PANIC，没有ERROR。
> 3. 缺乏结构化日志格式能力。
> 4. 无日志切割能力。

#### zap logger

相比于logrus，zap的并发处理能力更高。其中包括它每个要写入的字段都得携带类型，不需要通过反射来获得字段类型。

**自定义zap logger**

自定义的zap logger能够提供JSON结构化，多输出流，多日志级别等功能。下面是比较基础的自定义zap logger，录友们可以根据自己的需求实现自己的zap logger。

```go
// 自动调用
func init() {
	encoderConfig := zap.NewProductionEncoderConfig()
	// 设置日志记录中时间格式
	encoderConfig.EncodeTime = zapcore.ISO8601TimeEncoder
	// 日志encoder还是JSONEncoder，把日志行格式化成JSON格式
	encoder := zapcore.NewJSONEncoder(encoderConfig)
	conf := config.GetConfig()
	logPath = conf.LogPath
	file, _ := os.OpenFile(logPath, os.O_CREATE|os.O_APPEND|os.O_WRONLY, 644)
	fileWriteSyncer := zapcore.AddSync(file)
	core := zapcore.NewTee(
		zapcore.NewCore(encoder, zapcore.AddSync(os.Stdout), zapcore.DebugLevel),
		zapcore.NewCore(encoder, fileWriteSyncer, zapcore.DebugLevel),
	)
	logger = zap.New(core)
}

// getCallerInfoForLog 获得调用方的日志信息，包括函数名，文件名，行号
func getCallerInfoForLog() (callerFields []zap.Field) {
	pc, file, line, ok := runtime.Caller(2) // 回溯两层，拿到写日志的调用方的函数信息
	if !ok {
		return
	}
	funcName := runtime.FuncForPC(pc).Name()
	funcName = path.Base(funcName) // Base函数返回路径的最后一个元素，只保留函数名

	callerFields = append(callerFields, zap.String("func", funcName), zap.String("file", file), zap.Int("line", line))
	return
}

func Info(message string, fields ...zap.Field) {
	callerFields := getCallerInfoForLog()
	fields = append(fields, callerFields...)
	logger.Info(message, fields...)
}

func Warn(message string, fields ...zap.Field) {
	callerFields := getCallerInfoForLog()
	fields = append(fields, callerFields...)
	logger.Warn(message, fields...)
}

func Error(message string, fields ...zap.Field) {
	callerFields := getCallerInfoForLog()
	fields = append(fields, callerFields...)
	logger.Error(message, fields...)
}

func Fatal(message string, fields ...zap.Field) {
	callerFields := getCallerInfoForLog()
	fields = append(fields, callerFields...)
	logger.Fatal(message, fields...)
}

func Debug(message string, fields ...zap.Field) {
	callerFields := getCallerInfoForLog()
	fields = append(fields, callerFields...)
	logger.Debug(message, fields...)
}
```

init是go中包级别的初始化函数，不需要显示调用，每个包可以包含任意数量的init函数，这些函数在包被导入时自动执行，处于包级别变量声明和main函数执行之间执行。

### http response格式

```go
// controller.go
func JsonBack(c *gin.Context, message string, ret int, data interface{}) {
	if ret == 0 {
		if data != nil {
			c.JSON(http.StatusOK, gin.H{
				"code":    200,
				"message": message,
				"data":    data,
			})
		} else {
			c.JSON(http.StatusOK, gin.H{
				"code":    200,
				"message": message,
			})
		}
	} else if ret == -2 {
		c.JSON(http.StatusOK, gin.H{
			"code":    400,
			"message": message,
		})
	} else if ret == -1 {
		c.JSON(http.StatusOK, gin.H{
			"code":    500,
			"message": message,
		})
	}
}

```

为了方便前后端联合调试，这里所有的网络状态码返回结果都是http.StatusOK即200。通过gin框架中gin.H结构体返回具体的code和message。前端按照相应的code将message在f12控制台打印或者回显到页面中。

ret = 0时是业务走完正常流程返回的结果码，状态码统一为200；ret = -1是系统错误，比如序列化失败，redis缓存失败等，状态码统一为500；ret = -2是业务数据问题导致未正常走完业务流程返回的结果码，状态码统一为400。

### 三层架构

通常意义上的三层架构将整个业务应用划分成：界面层【表示层】Controller、业务逻辑层Service、数据访问层Dao。

![1738392664369-3bb0ce4f-d0fe-4b48-bb5b-42f4e508cce0.png](https://cdn.davidingplus.cn/images/2026/09/11/1738392664369-3bb0ce4f-d0fe-4b48-bb5b-42f4e508cce0-664117.png)

三层架构优点：

1. 开发人员只需关注其中一层
2. 可维护性高，可扩展性高，可降低层间依赖
3. 有利于标准化

三层架构缺点：

1. 降低系统性能，中间层延迟导致访问数据减慢
2. 导致级联修改，表示层增加一个功能，相应的业务逻辑层和数据访问层也需要增加相应的功能
3. 增加开发成本

**kama-chat-server后端使用了三层架构对项目进行开发，一个功能的调用过程如下：**

前端发送请求 -> controller层调用相关api -> service层执行内部业务逻辑 -> dao层通过gorm框架访问后台数据库 -> service层业务处理完成后返回结果 -> controller层调用JsonBack向前端返回结果 -> 前端拿到结果后页面变化或消息回显等。

（本项目在dao层没有与service层完全解耦，准确的三层架构应该是需要单独提供一个dao层，当service层有一个访问后台数据库的新功能时，之前的dao层函数无法提供该服务时，需要重新在dao层创建一个该service层函数对应的dao层后台数据库访问函数，以提供访问服务）

### controller api

```go
// https_server.go
func init() {
	GE = gin.Default()
    GE.Static("/static/avatars", config.GetConfig().StaticAvatarPath)
	GE.Static("/static/files", config.GetConfig().StaticFilePath)
	GE.POST("/login", v1.Login)
	GE.POST("/register", v1.Register)
	GE.POST("/user/updateUserInfo", v1.UpdateUserInfo)
	GE.POST("/user/getUserInfoList", v1.GetUserInfoList)
	GE.POST("/user/ableUsers", v1.AbleUsers)
	GE.POST("/user/getUserInfo", v1.GetUserInfo)
	GE.POST("/user/disableUsers", v1.DisableUsers)
	GE.POST("/user/deleteUsers", v1.DeleteUsers)
	GE.POST("/user/setAdmin", v1.SetAdmin)
	GE.POST("/user/sendSmsCode", v1.SendSmsCode)
	GE.POST("/user/smsLogin", v1.SmsLogin)
	GE.POST("/user/wsLogout", v1.WsLogout)
	GE.POST("/group/createGroup", v1.CreateGroup)
	GE.POST("/group/loadMyGroup", v1.LoadMyGroup)
	GE.POST("/group/checkGroupAddMode", v1.CheckGroupAddMode)
	GE.POST("/group/enterGroupDirectly", v1.EnterGroupDirectly)
	GE.POST("/group/leaveGroup", v1.LeaveGroup)
	GE.POST("/group/dismissGroup", v1.DismissGroup)
	GE.POST("/group/getGroupInfo", v1.GetGroupInfo)
	GE.POST("/group/getGroupInfoList", v1.GetGroupInfoList)
	GE.POST("/group/deleteGroups", v1.DeleteGroups)
	GE.POST("/group/setGroupsStatus", v1.SetGroupsStatus)
	GE.POST("/group/updateGroupInfo", v1.UpdateGroupInfo)
	GE.POST("/group/getGroupMemberList", v1.GetGroupMemberList)
	GE.POST("/group/removeGroupMembers", v1.RemoveGroupMembers)
	GE.POST("/session/openSession", v1.OpenSession)
	GE.POST("/session/getUserSessionList", v1.GetUserSessionList)
	GE.POST("/session/getGroupSessionList", v1.GetGroupSessionList)
	GE.POST("/session/deleteSession", v1.DeleteSession)
	GE.POST("/session/checkOpenSessionAllowed", v1.CheckOpenSessionAllowed)
	GE.POST("/contact/getUserList", v1.GetUserList)
	GE.POST("/contact/loadMyJoinedGroup", v1.LoadMyJoinedGroup)
	GE.POST("/contact/getContactInfo", v1.GetContactInfo)
	GE.POST("/contact/deleteContact", v1.DeleteContact)
	GE.POST("/contact/applyContact", v1.ApplyContact)
	GE.POST("/contact/getNewContactList", v1.GetNewContactList)
	GE.POST("/contact/passContactApply", v1.PassContactApply)
	GE.POST("/contact/blackContact", v1.BlackContact)
	GE.POST("/contact/cancelBlackContact", v1.CancelBlackContact)
	GE.POST("/contact/getAddGroupList", v1.GetAddGroupList)
	GE.POST("/contact/refuseContactApply", v1.RefuseContactApply)
	GE.POST("/contact/blackApply", v1.BlackApply)
	GE.POST("/message/getMessageList", v1.GetMessageList)
	GE.POST("/message/getGroupMessageList", v1.GetGroupMessageList)
	GE.POST("/message/uploadAvatar", v1.UploadAvatar)
	GE.POST("/message/uploadFile", v1.UploadFile)
	GE.GET("/wss", v1.WsLogin)
}
```

使用gin框架来完成controller层中前后端数据的交互业务。

其中两个Static调用专门用于前端访问后端静态文件的链接，比如前端需要上传文件或者是设置头像都需要后端静态文件的链接。

前端发起websocket连接时，必须使用GET请求，所以后端提供一个websocket登录的GET类型api。

### cors跨域处理

```go
// https_server.go
func init() {
	GE = gin.Default()
	corsConfig := cors.DefaultConfig()
	corsConfig.AllowOrigins = []string{"*"}
	corsConfig.AllowMethods = []string{"GET", "POST", "PUT", "DELETE", "OPTIONS"}
	corsConfig.AllowHeaders = []string{"Content-Type", "Authorization"}
	GE.Use(cors.New(corsConfig))
}
```

其中cors.DefaultConfig()，<font style="background-color:rgb(253, 253, 254);">获取CORS中间件的默认配置。</font>

<font style="background-color:rgb(253, 253, 254);">corsConfig.AllowOrigins = \[]string{"\*"}，设置了允许的源（Origin）列表。</font>`"*"`<font style="background-color:rgb(253, 253, 254);">表示接受任何源的请求，这是一种较为宽松的设置，但在生产环境中出于安全考虑，通常会指定具体的域名。</font>

<font style="background-color:rgb(253, 253, 254);">corsConfig.AllowMethods = \[]string{"GET", "POST", "PUT", "DELETE", "OPTIONS"}，</font><font style="color:#262626;background-color:rgb(253, 253, 254);">设置允许的HTTP请求方法。</font>

<font style="color:#262626;background-color:rgb(253, 253, 254);">corsConfig.AllowHeaders = \[]string{"Content-Type", "Authorization"}，设置允许在跨域请求中携带的HTTP头信息。</font>

<font style="color:#262626;background-color:rgb(253, 253, 254);"></font>

### <font style="color:#262626;background-color:rgb(253, 253, 254);">redis缓存机制</font>

```go
// redis_service.go
var redisClient *redis.Client
var ctx = context.Background()

func init() {
	redisClient = redis.NewClient(&redis.Options{
		Addr:     addr,
		Password: password,
		DB:       db,
	})
}

// 设置有超时时间的key
func SetKeyEx(key string, value string, timeout time.Duration) error 

// 获取key，如果不存在，err = nil
func GetKey(key string) (string, error)

// 获取key，如果不存在，err ≠ nil
func GetKeyNilIsErr(key string) (string, error) 

// 获取有prefix前缀的key，如果不存在，err ≠ nil
func GetKeyWithPrefixNilIsErr(prefix string) (string, error) 

// 获取有suffix后缀的key，如果不存在，err ≠ nil
func GetKeyWithSuffixNilIsErr(suffix string) (string, error)

// 如果key存在，删除该key
func DelKeyIfExists(key string) error

// 如果有该前缀的key，则删除
func DelKeysWithPrefix(prefix string) error

// 如果有该后缀的key，则删除
func DelKeysWithSuffix(suffix string) error

// 删除所有的key
func DeleteAllRedisKeys() error
```

r<font style="background-color:rgb(253, 253, 254);">edis是基于内存的数据库，这意味着它能够实现极高的读写速度。由于数据存储在内存中，redis能够支持每秒数十万次的读写操作，这对于需要高并发和低延迟的应用场景来说至关重要。因此，使用redis作为后台数据库信息的存储介质，可以显著提升系统的响应速度和性能。</font>

<font style="background-color:rgb(253, 253, 254);">在该项目中redis作为service层与dao层之间的缓冲层，开机后，第一次访问某api，会直接穿透到后台数据库访问具体的数据，返回给前端之前，会通过key，value类型存储到redis中。再下一次访问该api时，会先询问redis中是否有该key，如果有的话会直接将value反序列化后返给前端，不会穿透到后台数据库去调用数据库访问api。访问效率提升。</font>

<font style="background-color:rgb(253, 253, 254);">在该项目中，redis主要有2个用途：</font>

1. 缓存手机6位随机验证码
2. 缓存5种列表（联系人用户列表，我创建的群聊列表，我加入的群聊列表，用户会话列表，群聊会话列表）

### kafka通信服务

<font style="color:rgb(77, 77, 77);">Kafka是一种消息队列，主要用来处理大量数据状态下的消息队列。其中流量削峰是kafka特性之一，即当</font><font style="color:rgba(0, 0, 0, 0.75);">高流量的时候，使用消息队列作为中间件可以将流量的高峰保存在消息队列中，从而防止了系统的高请求，减轻服务器的请求处理压力。</font>

<font style="color:rgba(0, 0, 0, 0.75);">kafka在该项目中主要用于客户端给服务端发送消息时，当客户端同一时间发送消息太多，此情况满足高流量，所以可以使用kafka来进行流量削峰，让服务器能够顺利地处理大量的消息。</font>

#### <font style="color:rgba(0, 0, 0, 0.75);">下载kafka</font>

首先需要下载kafka，这里我是根据csdn上一篇比较出名的kafka安装教程来安装的。大家可以跟着该教程一步一步安装kafka。[【Kafka】Windows下安装Kafka（图文记录详细步骤）\_windows安装kafka-CSDN博客](https://blog.csdn.net/tttzzzqqq2018/article/details/132127105)

当将zookeeper，kafka都安装好了之后，需要经常使用到的两条指令如下：

![1739239488704-062679cb-e1c6-4ecf-abec-db43af43ea0e.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239488704-062679cb-e1c6-4ecf-abec-db43af43ea0e-308492.png)

当看到binding to port xxxx:2181，证明zookeeper的服务已经启动了。

![1739239504136-80044087-7e45-40ea-8517-2a0443d6bd96.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239504136-80044087-7e45-40ea-8517-2a0443d6bd96-963652.png)

另外一条指令是开启kafka服务的指令：

![1739239508239-ff866c8a-3b95-4bcd-bc1e-b04427cd8aa3.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239508239-ff866c8a-3b95-4bcd-bc1e-b04427cd8aa3-397082.png)

由于我已经创建好了消息topic，所以在演示的时候kafka服务启动成功如下：

![1739239511986-1896555c-1064-4309-9d87-5a75c171e794.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239511986-1896555c-1064-4309-9d87-5a75c171e794-672392.png)

总结一下，zkserver和.\bin\windows\kafka-server-start.bat .\config\server.properties是启动kafka的重要的两条指令。

当然，如果录友觉得命令行启动太麻烦，也可以去搜一下客户端版的kafka管理台。比如：

![1738674711134-dffda9e9-f504-41cf-87d4-8e557725614b.png](https://cdn.davidingplus.cn/images/2026/09/11/1738674711134-dffda9e9-f504-41cf-87d4-8e557725614b-628183.png)![1739239516824-c6e4859e-c03b-4a01-9dd9-711ce8e63849.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239516824-c6e4859e-c03b-4a01-9dd9-711ce8e63849-490752.png)

我用过offsetexplorer，不过有些时候会比较卡，是比较古早的客户端界面管理。还有其他的客户端软件大家可以自行去搜索。

#### kafka-go库

对于后端，还需要向后端服务器提供go版的kafka api。

Go社区中目前有三个比较常用的kafka客户端库 , 它们各有特点。

首先是IBM/sarama（这个库已经由Shopify转给了IBM），之前我写过一篇使用sarama操作Kafka的教程，相较于sarama， kafka-go 更简单、更易用。

segmentio/kafka-go 是纯Go实现，提供了与kafka交互的低级别和高级别两套API，同时也支持Context。

此外社区中另一个比较常用的confluentinc/confluent-kafka-go，它是一个基于cgo的librdkafka包装，在项目中使用它会引入对C库的依赖。

这里我使用的是github.com/segmentio/kafka-go。需要通过kafka-go，提供一个kafka-service。

首先是kafkaService结构体的设计。

```go
type kafkaService struct {
	ChatWriter *kafka.Writer
	ChatReader *kafka.Reader
	KafkaConn  *kafka.Conn
}
```

这里设置了1个主题，是chat聊天主题。也就是只要是跟聊天有关需要用到之前对应的Transmit转发通道进行转发的message，都属于这个主题的消息。该结构体提供了对该主题消息的读写流，以及一个kafka连接。

其次是kafka初始化。

```go
// KafkaInit 初始化kafka
func (k *kafkaService) KafkaInit() {
    //k.CreateTopic()
    kafkaConfig := myconfig.GetConfig().KafkaConfig
    k.ChatWriter = &kafka.Writer{
        Addr:                   kafka.TCP(kafkaConfig.HostPort),
        Topic:                  kafkaConfig.ChatTopic,
        Balancer:               &kafka.Hash{},
        WriteTimeout:           kafkaConfig.Timeout * time.Second,
        RequiredAcks:           kafka.RequireNone,
        AllowAutoTopicCreation: false,
    }
    k.ChatReader = kafka.NewReader(kafka.ReaderConfig{
        Brokers:        []string{kafkaConfig.HostPort},
        Topic:          kafkaConfig.ChatTopic,
        CommitInterval: kafkaConfig.Timeout * time.Second,
        GroupID:        "chat",
        StartOffset:    kafka.LastOffset,
    })
}
```

这里对ChatWriter进行了初始化，重点是Topic，并定义写超时时间为1s，并定义生产者发送消息后不等待任何来自服务器的确认。最后取消自动创建topic，创建topic可以手动创建。

对ChatReader进行初始化，brokers集群按照config中的配置，同时定义topic和读超时，GroupId设为chat，最后每次读都是从新的消息开始，防止如果之前发送了一条错误消息导致服务器出错，之后一直读该消息。这种LastOffset就相当于是读过的就不用再读，只读一遍。

```go
const (
    RequireNone RequiredAcks = 0
    RequireOne  RequiredAcks = 1
    RequireAll  RequiredAcks = -1
)
```

这是对kafka生产者的配置。

1. **<font style="background-color:rgb(253, 253, 254);">R</font>\*\*\*\*<font style="background-color:rgb(253, 253, 254);">equireNone (值为0)</font>**<font style="background-color:rgb(253, 253, 254);">:</font>

<font style="background-color:rgb(253, 253, 254);">生产者发送消息后不等待任何来自服务器的确认。</font>

<font style="background-color:rgb(253, 253, 254);">这种方式提供了最低的延迟，但不保证消息已被成功写入 Kafka。</font>

2. **<font style="background-color:rgb(253, 253, 254);">RequireOne (值为1)</font>**<font style="background-color:rgb(253, 253, 254);">:</font>

<font style="background-color:rgb(253, 253, 254);">生产者等待至少一个副本（通常是领导者副本）成功写入消息后的确认。</font>

<font style="background-color:rgb(253, 253, 254);">这种方式提供了较好的平衡，既保证了消息至少被一个副本持久化，又不会引入过多的延迟。</font>

3. **<font style="background-color:rgb(253, 253, 254);">RequireAll (值为-1)</font>**<font style="background-color:rgb(253, 253, 254);">:</font>

<font style="background-color:rgb(253, 253, 254);">生产者等待所有副本（ISR 列表中的副本）都成功写入消息后的确认。</font>

<font style="background-color:rgb(253, 253, 254);">这种方式提供了最高的持久性保证，但可能会增加延迟，因为需要等待更多的副本确认。</font>

```go
func (k *kafkaService) KafkaClose() {
    if err := k.ChatWriter.Close(); err != nil {
        zlog.Error(err.Error())
    }
    if err := k.ChatReader.Close(); err != nil {
        zlog.Error(err.Error())
    }
}
```

<font style="background-color:rgb(253, 253, 254);">当服务器关闭时，需要停止kafka的服务。此时需要关闭ChatWriter和ChatServer。</font>

```go
// CreateTopic 创建topic
func (k *kafkaService) CreateTopic() {
    // 如果已经有topic了，就不创建了
    kafkaConfig := myconfig.GetConfig().KafkaConfig

    chatTopic := kafkaConfig.ChatTopic

    // 连接至任意kafka节点
    var err error
    k.KafkaConn, err = kafka.Dial("tcp", kafkaConfig.HostPort)
    if err != nil {
        zlog.Error(err.Error())
    }

    topicConfigs := []kafka.TopicConfig{
        {
            Topic:             chatTopic,
            NumPartitions:     kafkaConfig.Partition,
            ReplicationFactor: kafkaConfig.ReplicationFactor,
        },
    }

    // 创建topic
    if err = k.KafkaConn.CreateTopics(topicConfigs...); err != nil {
        zlog.Error(err.Error())
    }

}
```

创建Topic，需要先通过Dial函数连接到kafka服务器上。之后向kafka进行topic配置，这里我每个主题定义了1个分区，定义了1个副本，大家可以根据自己的需求自定义配置。最后是创建topic。

这个函数只能执行1次，当已经创建该Topic之后，再次创建相同Topic会报错。

### ssl加密（可以跳过）

#### 自签名证书（可以跳过）

自签名证书的话方便尝试整个流程，想感受整个流程的录友可以尝试一下。

ssl加密 可以不看，感兴趣的录友 去尝试一下。

在生成证书时，我使用的一个工具是mkcert.exe。大家感兴趣可以详细地去看看这篇博客使用mkcert完成自签名证书的流程。

[使用mkcert工具生成受信任的SSL证书，解决局域网本地https访问问题\_mkcert局域网证书-CSDN博客](https://blog.csdn.net/qq_45392321/article/details/119676301)

> mkcert 是一个简单的工具，用于制作本地信任的开发证书。它不需要配置。
>
> 简化我们在本地搭建 https 环境的复杂性，无需操作繁杂的 openssl 实现自签证书了，这个小程序就可以帮助我们自签证书，在本机使用还会自动信任 CA，非常方便。
>
> 使用来自真实证书颁发机构 (CA) 的证书进行开发可能很危险或不可能（对于example.test、localhost或 之类的主机127.0.0.1），但自签名证书会导致信任错误。管理您自己的 CA 是最好的解决方案，但通常涉及神秘的命令、专业知识和手动步骤。
>
> mkcert 在系统根存储中自动创建并安装本地 CA，并生成本地信任的证书。mkcert 不会自动配置服务器以使用证书，但这取决于您。

![1739239525801-01b9c82b-836d-4906-8349-151d117701f1.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239525801-01b9c82b-836d-4906-8349-151d117701f1-683325.png)

通过ipconfig在powshell中找到自己局域网内的ip地址，为192.168.2.114。

![1739239529190-1cdb3ecf-e5e1-4473-8c38-efd4f80740f1.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239529190-1cdb3ecf-e5e1-4473-8c38-efd4f80740f1-588506.png)

我把下载好的mkcert.exe存在前端的/src/assets/cert目录中。通过执行如下命令：

![1739239532712-a3dccc58-a78b-47e2-b9db-17e4e3b2fa70.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239532712-a3dccc58-a78b-47e2-b9db-17e4e3b2fa70-944150.png)

就生成了由CA证书签署的自签名证书。然后将对应的./localhost+2.pem和./localhost+2-key.pem加入到后端服务器中。

```go
// https_server.go
func init() {
    GE = gin.Default()
    GE.Use(ssl.TlsHandler(config.GetConfig().MainConfig.Host, config.GetConfig().MainConfig.Port))
}


// tls_handler.go
func TlsHandler(host string, port int) gin.HandlerFunc {
	return func(c *gin.Context) {
		secureMiddleware := secure.New(secure.Options{
			SSLRedirect: true,
			SSLHost:     host + ":" + strconv.Itoa(port),
		})
		err := secureMiddleware.Process(c.Writer, c.Request)

		// If there was an error, do not continue.
		if err != nil {
			zlog.Fatal(err.Error())
			return
		}

		c.Next()
	}
}

// config_local.toml
[mainConfig]
appName = "your app name"
host = "127.0.0.1"
port = 8080
```

这样就将自签名证书和密钥添加到了服务器中。

在安全访问后端服务器之前，能够让本机信任该自签名证书的操作还需要一步，就是让本机信任签署该证书的CA证书。

![1739239537483-5c9520a5-9db6-44d9-9502-24da33840e9f.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239537483-5c9520a5-9db6-44d9-9502-24da33840e9f-292820.png)

![1739239540747-fdfb04ed-604b-4b38-afd1-28aae4bfff06.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239540747-fdfb04ed-604b-4b38-afd1-28aae4bfff06-444799.png)

查看CA证书存放位置。

> 可以看到 CA 路径下有两个文件rootCA-key.pem和rootCA.pem两个文件，用户需要信任rootCA.pem这个文件。将rootCA.pem拷贝一个副本，并命名为rootCA.crt(因为 windows 并不识别pem扩展名，并且 Ubuntu 也不会将pem扩展名作为 CA 证书文件对待)，将rootCA.crt文件分发给其他用户，手工导入。

![1739239544683-2981dea0-d91e-4374-a8c8-0f6c5e6f6ce5.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239544683-2981dea0-d91e-4374-a8c8-0f6c5e6f6ce5-866375.png)

修改之后Windows识别出了rootCA.crt是安全证书，双击该证书打开。

![1739239547479-7e773f12-46db-4a0d-a970-d8dae656ed47.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239547479-7e773f12-46db-4a0d-a970-d8dae656ed47-469979.png)

安装证书。

![1739239552305-5c7b1a6b-a644-4142-836f-0638eeada86b.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239552305-5c7b1a6b-a644-4142-836f-0638eeada86b-227068.png)

点击下一步。

![1739239557654-ee56b41a-c612-43fa-ac1f-5a25cd2543ed.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239557654-ee56b41a-c612-43fa-ac1f-5a25cd2543ed-228109.png)

选择“受信任的根证书颁发机构”，点击下一步。最后点击完成即可。

此时CA证书已经受到本机信任，它签名的证书也受到本机信任。如果还是不信任，可以再对localhost+2.pem采取同样的操作。

到此，便可以通过https顺利访问后端服务器了。如果想要做到另外一台机器访问本机的后端服务器，需要在局域网内（比如公用同一个wifi），给该机器的证书中也添加对应的CA证书，此时该机器也会信任这份由该CA证书自签名的证书，然后https://ip:port/login来访问本机后端服务器就会收到登录信息的返回。

![1739239564775-53b16478-3b89-4315-a69b-b465268a7bac.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239564775-53b16478-3b89-4315-a69b-b465268a7bac-399019.png)

此时访问后端服务器，成功返回200，说明https已成功。

如果需要跨域访问或公网访问，就需要去找专门的证书颁发机构，这里我试过腾讯云，阿里云，他们都对外提供域名服务和证书服务。有免费的证书可以使用。有条件的录友可以向腾讯云/阿里云申请对应的证书，通过公网进行测试。

#### 免费受信任证书（可以跳过）

我一开始是本地部署整个项目的，用的是win10。所以使用的是自签名证书，部署0成本。

后面想尝试给录友们做一期部署在Ubuntu（22.04）云服务器能够公网访问、部署上线的项目，所以需要用到受信任的颁发证书机构生成的证书。这里<font style="color:rgb(6, 6, 7);">使用 Let's Encrypt 的通配符证书。</font><font style="color:rgb(6, 6, 7);">如果你的 IP 地址是静态的，并且你控制着与该 IP 地址相关联的域名，你可以使用 Let's Encrypt 的通配符证书。通配符证书可以保护一个域名及其所有子域名。例如，如果你有一个域名 </font><code><font style="color:rgb(6, 6, 7);">example.com</font></code><font style="color:rgb(6, 6, 7);">，你可以获取一个 </font><code><font style="color:rgb(6, 6, 7);">*.example.com</font></code><font style="color:rgb(6, 6, 7);"> 的通配符证书，这个证书可以保护 </font><code><font style="color:rgb(6, 6, 7);">example.com</font></code><font style="color:rgb(6, 6, 7);">、</font><code><font style="color:rgb(6, 6, 7);">sub.example.com</font></code><font style="color:rgb(6, 6, 7);"> 以及任何其他子域名。</font>

<font style="color:rgb(6, 6, 7);">首先需要申请一个域名，这里我通过ai查到可以通过</font>[硅云](http://vpsor.cn)<font style="color:rgb(6, 6, 7);">来获取免费域名。</font>

<font style="color:rgb(6, 6, 7);">硅云（vpsor.cn）</font>

* **<font style="color:rgb(6, 6, 7);">特点</font>**<font style="color:rgb(6, 6, 7);">：</font>
  * <font style="color:rgb(6, 6, 7);">提供多达 8 种顶级域名免费注册，包括 </font>`.top`<font style="color:rgb(6, 6, 7);">、</font>`.xyz`<font style="color:rgb(6, 6, 7);">、</font>`.site`<font style="color:rgb(6, 6, 7);">、</font>`.website`<font style="color:rgb(6, 6, 7);">、</font>`.fun`<font style="color:rgb(6, 6, 7);">、</font>`.space`<font style="color:rgb(6, 6, 7);"> 等。</font>
  * <font style="color:rgb(6, 6, 7);">新用户首次注册域名免费，只能享受一次。</font>
  * <font style="color:rgb(6, 6, 7);">需要完成实名认证。</font>
* **<font style="color:rgb(6, 6, 7);">注册流程</font>**<font style="color:rgb(6, 6, 7);">：</font>
  1. <font style="color:rgb(6, 6, 7);">在硅云官网（</font>[http://vpsor.cn](http://vpsor.cn/)<font style="color:rgb(6, 6, 7);">）注册账号，并完成实名认证。</font>
  2. <font style="color:rgb(6, 6, 7);">新建一个域名模板，对模板完成实名认证。</font>
  3. <font style="color:rgb(6, 6, 7);">搜索要注册的域名，使用已实名的域名模板完成域名注册。</font>

![1740906590586-c2fca695-0a9f-4167-93c5-4003e0ef042b.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906590586-c2fca695-0a9f-4167-93c5-4003e0ef042b-050413.png)

新建域名模板。后面使用域名模板来注册域名。

![1740906455450-88c411e4-d02e-4159-867b-1884a7bb21dd.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906455450-88c411e4-d02e-4159-867b-1884a7bb21dd-496831.png)

<font style="color:rgb(6, 6, 7);">点击“新注域名”。</font>

![1740906474863-f21f088b-1a68-45d8-8d35-339cb522fdbb.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906474863-f21f088b-1a68-45d8-8d35-339cb522fdbb-085399.png)

在这个界面注册域名。

![1740906676230-6a8b755d-b88d-4290-80d9-9c7a725a301f.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906676230-6a8b755d-b88d-4290-80d9-9c7a725a301f-702662.png)

当注册完之后，并且实名都弄完了，需要等待注册局确认。确认完了的结果是这样的。

点击“管理”。

![1740906707357-04fb3a40-6faa-4658-9d3e-652d29f60a4d.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906707357-04fb3a40-6faa-4658-9d3e-652d29f60a4d-258525.png)

这是我注册的域名，现在需要追加域名解析，解析你云服务器的ip地址。

![1740906755585-f30ec4a1-8d9e-4372-866a-a16411217067.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906755585-f30ec4a1-8d9e-4372-866a-a16411217067-739640.png)

添加这样一条记录。

* **<font style="color:rgb(6, 6, 7);">主机记录</font>**<font style="color:rgb(6, 6, 7);">：填写 </font>`@`<font style="color:rgb(6, 6, 7);">（表示根域名，即 </font>`kamachat.top`<font style="color:rgb(6, 6, 7);">）。</font>
* **<font style="color:rgb(6, 6, 7);">记录类型</font>**<font style="color:rgb(6, 6, 7);">：选择 </font>`A`<font style="color:rgb(6, 6, 7);">。</font>
* **<font style="color:rgb(6, 6, 7);">记录值</font>**<font style="color:rgb(6, 6, 7);">：填写你的服务器 IP 地址 </font>`123.56.164.220`<font style="color:rgb(6, 6, 7);">。</font>
* **<font style="color:rgb(6, 6, 7);">TTL</font>**<font style="color:rgb(6, 6, 7);">：填写 </font>`3600`<font style="color:rgb(6, 6, 7);"> 或默认值（单位为秒，表示缓存时间）。</font>
* **<font style="color:rgb(6, 6, 7);">线路类型</font>**<font style="color:rgb(6, 6, 7);">：选择默认或留空。</font>

<font style="color:rgb(6, 6, 7);">在服务器上检查是否已经可以解析域名到云服务器的ip地址了。</font>

![1740906831256-edb32ada-f399-4a19-aaa9-3e8cab48353f.png](https://cdn.davidingplus.cn/images/2026/09/11/1740906831256-edb32ada-f399-4a19-aaa9-3e8cab48353f-739073.png)

## <font style="color:#262626;background-color:rgb(253, 253, 254);">业务开发</font>

### 登录注册

登录注册功能使用的账号都是在该项目中都是按照手机号来设定的，最多11位，因为支持了短信验证。

登录注册请求信息检验在前端需要先加一层拦截，比如手机号等请求信息是否符合规范。后端拿到的请求数据已经是通过检测的数据，只需要进行登录或者注册或者短信验证等功能即可。

#### /login

<font style="background-color:#FBDE28;">controller</font>

```go
// login_request.go
type LoginRequest struct {
	Telephone string `json:"telephone"`
	Password  string `json:"password"`
}

// user_info_controller.go
// Login 登录
func Login(c *gin.Context) {
	var req request.LoginRequest
	if err := c.BindJSON(&req); err != nil {
		zlog.Error(err.Error())
		c.JSON(http.StatusOK, gin.H{
			"code":    500,
			"message": constants.SYSTEM_ERROR,
		})
		return
	}
	message, userInfo, ret := gorm.UserInfoService.Login(req)
	JsonBack(c, message, ret, userInfo)
}
```

前端传过来的LoginRequest，在controller层反序列化之后，将req传入service层进行具体的业务逻辑处理。

业务逻辑处理之后有3个返回值，包括message返回的消息，data返回的数据，ret返回的结果码。

最终调用JsonBack函数将返回结果传回给前端。

<font style="background-color:#FBDE28;">service+dao</font>

```go
// user_info_service.go
// Login 登录
func (u *userInfoService) Login(loginReq request.LoginRequest) (string, *respond.LoginRespond, int) {
    password := loginReq.Password
    var user model.UserInfo
    res := dao.GormDB.First(&user, "telephone = ?", loginReq.Telephone)
    if res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            message := "用户不存在，请注册"
            zlog.Error(message)
            return message, nil, -2
        }
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    if user.Password != password {
        message := "密码不正确，请重试"
        zlog.Error(message)
        return message, nil, -2
    }

    loginRsp := &respond.LoginRespond{
        Uuid:      user.Uuid,
        Telephone: user.Telephone,
        Nickname:  user.Nickname,
        Email:     user.Email,
        Avatar:    user.Avatar,
        Gender:    user.Gender,
        Birthday:  user.Birthday,
        Signature: user.Signature,
        IsAdmin:   user.IsAdmin,
        Status:    user.Status,
    }
    year, month, day := user.CreatedAt.Date()
    loginRsp.CreatedAt = fmt.Sprintf("%d.%d.%d", year, month, day)

    return "登陆成功", loginRsp, 0
}
```

这里拿到了前端传来的Password后，与后台数据库中的Password进行比较，就可以判断密码是否正确。

最后需要将登录信息作为数据返回给前端，这相当于是前端的session了。

在这里做一个service层与dao层的解耦示例，录友们下来可以对这些尚未解耦的service+dao层尝试一下解耦。

```go
// user_info_service.go
// Login 登录
func (u *userInfoService) Login(loginReq request.LoginRequest) (string, *respond.LoginRespond, int) {
    password := loginReq.Password
    
    var user *model.UserInfo
    message, user, ret := UserInfoDao.GetUserInfoByTelephone(loginReq)
    if ret != 0 {
        return message, nil, ret
    }
    if user.Password != password {
        message := "密码不正确，请重试"
        zlog.Error(message)
        return message, nil, -2
    }

    loginRsp := &respond.LoginRespond{
        Uuid:      user.Uuid,
        Telephone: user.Telephone,
        Nickname:  user.Nickname,
        Email:     user.Email,
        Avatar:    user.Avatar,
        Gender:    user.Gender,
        Birthday:  user.Birthday,
        Signature: user.Signature,
        IsAdmin:   user.IsAdmin,
        Status:    user.Status,
    }
    year, month, day := user.CreatedAt.Date()
    loginRsp.CreatedAt = fmt.Sprintf("%d.%d.%d", year, month, day)

    return "登陆成功", loginRsp, 0
}

// user_info_dao.go
func (u *userInfoDao) GetUserInfoByTelephone(req request.LoginRequest) (string, *model.UserInfo, int) {
    var user model.UserInfo
    res := dao.GormDB.First(&user, "telephone = ?", loginReq.Telephone)
    if res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            message := "用户不存在，请注册"
            zlog.Error(message)
            return message, nil, -2
        }
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    return "", &user, 0
}
    
```

大家可以明显感受到当service+dao双层解耦之后，开发难度增加，特别能感觉到对于dao层函数返回值，service层也需要分类讨论，看是否返回还是继续往下执行；

好处也能感受到，如果合理设计dao函数，部分比较抽象的dao函数可以复用到其他的业务中去。

#### /user/sendSmsCode

<font style="background-color:#FBDE28;">controller</font>

```go
// user_info_controller.go
// SendSmsCode 发送短信验证码
func SendSmsCode(c *gin.Context) {
    var req request.SendSmsCodeRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserInfoService.SendSmsCode(req.Telephone)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// user_info_service.go
// SendSmsCode 发送短信验证码 - 验证码登录
func (u *userInfoService) SendSmsCode(telephone string) (string, int) {
    return sms.VerificationCode(telephone)
}

// auth_code_service.go
func VerificationCode(telephone string) (string, int) {
	client, err := createClient()
	if err != nil {
		zlog.Error(err.Error())
		return constants.SYSTEM_ERROR, -1
	}
	key := "auth_code_" + telephone
	code, err := redis.GetKey(key)
	if err != nil {
		zlog.Error(err.Error())
		return constants.SYSTEM_ERROR, -1
	}

	if code != "" {
		// 直接返回，验证码还没过期，用户应该去输验证码
		message := "目前还不能发送验证码，请输入已发送的验证码"
		zlog.Info(message)
		return message, -2
	}
	// 验证码过期，重新生成
	code = strconv.Itoa(random.GetRandomInt(6))
	fmt.Println(code)
	err = redis.SetKeyEx(key, code, time.Minute) // 1分钟有效
	if err != nil {
		zlog.Error(err.Error())
		return constants.SYSTEM_ERROR, -1
	}
	sendSmsRequest := &dysmsapi20170525.SendSmsRequest{
		SignName:      tea.String("阿里云短信测试"),
		TemplateCode:  tea.String("SMS_154950909"), // 短信模板
		PhoneNumbers:  tea.String(telephone),
		TemplateParam: tea.String("{\"code\":\"" + code + "\"}"),
	}

	runtime := &util.RuntimeOptions{}
	// 目前使用的是测试专用签名，签名必须是“阿里云短信测试”，模板code为“SMS_154950909”
	rsp, err := client.SendSmsWithOptions(sendSmsRequest, runtime)
	if err != nil {
		zlog.Error(err.Error())
		return constants.SYSTEM_ERROR, -1
	}
	zlog.Info(*util.ToJSONString(rsp))
	return "验证码发送成功，请及时在对应电话查收短信", 0
}
```

这里面的核心逻辑在VerificationCode中。

VerificationCode中包含2部分，第一部分是判断目前账号是否已经发送了验证码且验证码没过期？如果发送过了，在redis中是可以看到该账号的随机6位验证码的。通过redis.GetKey来获取到key为"auth\_code\_xxxxxx"的验证码。

第二部分如果没有找到，说明可能没有发送过或者已经过期了，则重新创建随机6位验证码。并向对应手机发送这6位验证码。

发送验证码的手机验证功能请看**手机验证**部分。

#### /register

<font style="background-color:#FBDE28;">controller</font>

```go
// Register 注册
func Register(c *gin.Context) {
    var registerReq request.RegisterRequest
    if err := c.BindJSON(&registerReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    fmt.Println(registerReq)
    message, userInfo, ret := gorm.UserInfoService.Register(registerReq)
    JsonBack(c, message, ret, userInfo)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// user_info_service
// Register 注册
func (u *userInfoService) Register(registerReq request.RegisterRequest) (string, *respond.RegisterRespond, int) {
	key := "auth_code_" + registerReq.Telephone
	code, err := myredis.GetKey(key)
	if err != nil {
		zlog.Error(err.Error())
		return constants.SYSTEM_ERROR, nil, -1
	}
	if code != registerReq.SmsCode {
		message := "验证码不正确，请重试"
		zlog.Info(message)
		return message, nil, -2
	} else {
		if err := myredis.DelKeyIfExists(key); err != nil {
			zlog.Error(err.Error())
			return constants.SYSTEM_ERROR, nil, -1
		}
	}
	// 不用校验手机号，前端校验
	// 判断电话是否已经被注册过了
	message, ret := u.checkTelephoneExist(registerReq.Telephone)
	if ret != 0 {
		return message, nil, ret
	}
	var newUser model.UserInfo
	newUser.Uuid = "U" + random.GetNowAndLenRandomString(11)
	newUser.Telephone = registerReq.Telephone
	newUser.Password = registerReq.Password
	newUser.Nickname = registerReq.Nickname
	newUser.Avatar = "https://cube.elemecdn.com/0/88/03b0d39583f48206768a7534e55bcpng.png"
	newUser.CreatedAt = time.Now()
	newUser.IsAdmin = u.checkUserIsAdminOrNot(newUser)
	newUser.Status = user_status_enum.NORMAL

	res := dao.GormDB.Create(&newUser)
	if res.Error != nil {
		zlog.Error(res.Error.Error())
		return constants.SYSTEM_ERROR, nil, -1
	}
	newUser.LastOnlineAt = time.Now()
	registerRsp := &respond.RegisterRespond{
		Uuid:      newUser.Uuid,
		Telephone: newUser.Telephone,
		Nickname:  newUser.Nickname,
		Email:     newUser.Email,
		Avatar:    newUser.Avatar,
		Gender:    newUser.Gender,
		Birthday:  newUser.Birthday,
		Signature: newUser.Signature,
		IsAdmin:   newUser.IsAdmin,
		Status:    newUser.Status,
	}
	year, month, day := newUser.CreatedAt.Date()
	registerRsp.CreatedAt = fmt.Sprintf("%d.%d.%d", year, month, day)

	return "注册成功", registerRsp, 0
}
```

注册功能也会添加手机验证的功能。前端在调用注册api的时候，是在手机验证发送验证码，并填写了验证码之后。前端传来的验证码，需要与后端key为"auth\_code\_xxxx"的value进行比对，如果相同，说明验证码正确，否则验证码错误。验证码正确需要删除redis中对应的key。

此时对电话号码进行检测，看是否已经提前注册过了。

```go
// checkTelephoneExist 检查手机号是否存在
func (u *userInfoService) checkTelephoneExist(telephone string) (string, int) {
    var user model.UserInfo
    // gorm默认排除软删除，所以翻译过来的select语句是SELECT * FROM `user_info` WHERE telephone = '18089596095' AND `user_info`.`deleted_at` IS NULL ORDER BY `user_info`.`id` LIMIT 1
    if res := dao.GormDB.Where("telephone = ?", telephone).First(&user); res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            zlog.Info("该电话不存在，可以注册")
            return "", 0
        }
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    zlog.Info("该电话已经存在，注册失败")
    return "该电话已经存在，注册失败", -2
}
```

这里补充一点，gorm框架大多数后台数据库访问方法都是默认不会查询到已经软删除的数据的，就是说如果该表项通过DeletedAt软删除了，在查询该表项的时候默认是查不到的，不需要再次在条件里面标明"deleted\_at is null"。如果想要数据软删除之后依旧可以访问到，则需要加上.Unscoped()，这样查询到的数据是不会从DeletedAt字段筛选的。

为什么要说这一点，因为后面管理员功能需要知道哪些用户是被删除过的，所以需要查询已经被软删除的数据。

#### /user/smsLogin

<font style="background-color:#FBDE28;">controller</font>

```go
// SmsLogin 验证码登录
func SmsLogin(c *gin.Context) {
    var req request.SmsLoginRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, userInfo, ret := gorm.UserInfoService.SmsLogin(req)
    JsonBack(c, message, ret, userInfo)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// SmsLogin 验证码登录
func (u *userInfoService) SmsLogin(req request.SmsLoginRequest) (string, *respond.LoginRespond, int) {
    var user model.UserInfo
    res := dao.GormDB.First(&user, "telephone = ?", req.Telephone)
    if res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            message := "用户不存在，请注册"
            zlog.Error(message)
            return message, nil, -2
        }
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }

    key := "auth_code_" + req.Telephone
    code, err := myredis.GetKey(key)
    if err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    if code != req.SmsCode {
        message := "验证码不正确，请重试"
        zlog.Info(message)
        return message, nil, -2
    } else {
        if err := myredis.DelKeyIfExists(key); err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }

    loginRsp := &respond.LoginRespond{
        Uuid:      user.Uuid,
        Telephone: user.Telephone,
        Nickname:  user.Nickname,
        Email:     user.Email,
        Avatar:    user.Avatar,
        Gender:    user.Gender,
        Birthday:  user.Birthday,
        Signature: user.Signature,
        IsAdmin:   user.IsAdmin,
        Status:    user.Status,
    }
    year, month, day := user.CreatedAt.Date()
    loginRsp.CreatedAt = fmt.Sprintf("%d.%d.%d", year, month, day)

    return "登陆成功", loginRsp, 0
}
```

前端应该是先调用了/user/sendSmsCode这个接口向手机发送了验证码，用户在前端输入要验证码，发起了验证码登录。

验证码登录，先检查在后端服务器上是否存在该用户的相关验证码，通过redis查找该key，发现value和传过来的随机6位验证码相同的话，就打包userInfo数据，向前端返回该结果。

该api接口用于用户记不住密码，需要使用验证码进行登录时。

### 手机验证

手机验证需要上网找一个短信服务提供商，这里我找的是[阿里云](https://www.aliyun.com/)。

在右上角直接搜索“短信服务”。

![1739239577179-bea948dc-3d36-4369-95f4-16b92e321ddd.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239577179-bea948dc-3d36-4369-95f4-16b92e321ddd-418878.png)

开通服务后，进入“短信控制台”。

![1739239581298-4d78a5a2-920a-418c-a7cb-64e65eb865d7.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239581298-4d78a5a2-920a-418c-a7cb-64e65eb865d7-481245.png)

进入“快速学习和测试”页面，然后申请资质、签名和模板。记得选择“调用api发送短信”。

![1739239584316-61abb0a3-e459-409c-a571-680f1087f04b.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239584316-61abb0a3-e459-409c-a571-680f1087f04b-392510.png)

往下划到2.调用测试签名模板API。可以选“自定义”和“专用”的测试签名/模板。我使用的是专用的测试签名/模板，它的签名要求是“<font style="color:#067d17;background-color:#ffffff;">阿里云短信测试</font>”，模板要求是“<font style="color:#067d17;background-color:#ffffff;">SMS\_154950909</font>”。大家也可以使用自定义的签名/模板，主要是测试验证码是否能够正确收发。

然后点击“调用API发送短信”。

![1739239587444-9869597d-7cab-474c-b32a-d69f1ea3eb4c.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239587444-9869597d-7cab-474c-b32a-d69f1ea3eb4c-446878.png)

点击“SDK示例”，点击Go，复制所有代码，将这些代码植入到项目中验证码功能里。

![1739239594779-e58d35bc-0e13-44fd-8cad-9c30be2a6369.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239594779-e58d35bc-0e13-44fd-8cad-9c30be2a6369-530944.png)

在能够成功调用这些SDK之前，需要点击“SDK文档”，把相关的包go get下来。

![1739239600174-a7375584-1931-44c3-8e3a-03020b928fe1.png](https://cdn.davidingplus.cn/images/2026/09/11/1739239600174-a7375584-1931-44c3-8e3a-03020b928fe1-767629.png)

除此以外，还需要创建accessKeyID, accessKeySecret，用于smsClient的创建。阿里云官方文档写了相关的创建方法，小伙伴可以去读一下。[创建阿里云AccessKey\_访问控制(RAM)-阿里云帮助中心](https://help.aliyun.com/zh/ram/user-guide/create-an-accesskey-pair)。

下面是从阿里云上复制下来修改的手机号验证码的sdk。

```go
func createClient() (result *dysmsapi20170525.Client, err error) {
	if smsClient == nil {
		config := &openapi.Config{
			AccessKeyId: tea.String(accessKeyID),
			AccessKeySecret: tea.String(accessKeySecret),
		}
		config.Endpoint = tea.String("dysmsapi.aliyuncs.com")
		smsClient, err = dysmsapi20170525.NewClient(config)
	}
	return smsClient, err
}

func VerificationCode(telephone string) error {
	client, err := createClient()
	if err != nil {
		return err
	}
	
    // redis管理code，定时过期……
    
	code = strconv.Itoa(random.GetRandomInt(6))
	sendSmsRequest := &dysmsapi20170525.SendSmsRequest{
		SignName:      tea.String(signName),	// 短信签名
		TemplateCode:  tea.String(templateCode), // 短信模板
		PhoneNumbers:  tea.String(telephone),
		TemplateParam: tea.String("{\"code\":\"" + code + "\"}"),
	}
	runtime := &util.RuntimeOptions{}
	// 目前使用的是测试专用签名，签名必须是“阿里云短信测试”，模板code为“SMS_154950909”
	_, err := client.SendSmsWithOptions(sendSmsRequest, runtime)
	if err != nil {
		return err
	}
	return nil
}
```

### 用户功能

#### /user/updateUserInfo

**更新用户信息**

<font style="background-color:#FBDE28;">controller</font>

```go
// UpdateUserInfo 修改用户信息
func UpdateUserInfo(c *gin.Context) {
    var req request.UpdateUserInfoRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserInfoService.UpdateUserInfo(req)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// UpdateUserInfo 修改用户信息
func (u *userInfoService) UpdateUserInfo(updateReq request.UpdateUserInfoRequest) (string, int) {
    var user model.UserInfo
    if res := dao.GormDB.First(&user, "uuid = ?", updateReq.Uuid); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if updateReq.Email != "" {
        user.Email = updateReq.Email
    }
    if updateReq.Nickname != "" {
        user.Nickname = updateReq.Nickname
    }
    if updateReq.Birthday != "" {
        user.Birthday = updateReq.Birthday
    }
    if updateReq.Signature != "" {
        user.Signature = updateReq.Signature
    }
    if updateReq.Avatar != "" {
        user.Avatar = updateReq.Avatar
    }
    if res := dao.GormDB.Save(&user); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "修改用户信息成功", 0
}
```

前端传来更新的数据，后端根据字段是否为空，来针对性更新，最后刷回后台数据库。

这个api接口用于在前端OwnInfo.vue页面修改个人主页信息。

#### /user/getUserInfo

**获取用户信息**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetUserInfo 获取用户信息
func GetUserInfo(c *gin.Context) {
    var req request.GetUserInfoRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, userInfo, ret := gorm.UserInfoService.GetUserInfo(req.Uuid)
    JsonBack(c, message, ret, userInfo)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// get_userinfo_respond.go
type GetUserInfoRespond struct {
	Uuid      string `json:"uuid"`
	Nickname  string `json:"nickname"`
	Telephone string `json:"telephone"`
	Avatar    string `json:"avatar"`
	Email     string `json:"email"`
	Gender    int8   `json:"gender"`
	Birthday  string `json:"birthday"`
	Signature string `json:"signature"`
	CreatedAt string `json:"created_at"`
	IsAdmin   int8   `json:"is_admin"`
	Status    int8   `json:"status"`
}

// user_info_service.go
// GetUserInfo 获取用户信息
func (u *userInfoService) GetUserInfo(uuid string) (string, *respond.GetUserInfoRespond, int) {
    var user model.UserInfo
    if res := dao.GormDB.Where("uuid = ?", uuid).Find(&user); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    rsp := respond.GetUserInfoRespond{
        Uuid:      user.Uuid,
        Telephone: user.Telephone,
        Nickname:  user.Nickname,
        Avatar:    user.Avatar,
        Birthday:  user.Birthday,
        Email:     user.Email,
        Gender:    user.Gender,
        Signature: user.Signature,
        CreatedAt: user.CreatedAt.Format("2006-01-02 15:04:05"),
        IsAdmin:   user.IsAdmin,
        Status:    user.Status,
    }
    return "获取用户信息成功", &rsp, 0
}
```

通过uuid访问后台数据库，获取对应的UserInfo，新建DTO（DTO是<font style="color:rgb(77, 77, 77);">数据传输对象，是在应用网络层需要传输的对象，是一个为了减少方法调用次数而在进程间传输数据的对象。），返回给前端。</font>

<font style="color:rgb(77, 77, 77);">这个api接口用于前端的App.vue主页面在OnMouted刷新或者页面跳转时，都会获取当前的UserInfo，保证该客户端的Session时刻存储该用户信息。</font>

<font style="background-color:#FBDE28;"></font>

### 群组功能

#### /group/createGroup

**创建群聊**

<font style="background-color:#FBDE28;">controller</font>

```go
// group_info_controller.go
// CreateGroup 创建群聊
func CreateGroup(c *gin.Context) {
    var createGroupReq request.CreateGroupRequest
    if err := c.BindJSON(&createGroupReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.CreateGroup(createGroupReq)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// group_info_service.go
// CreateGroup 创建群聊
func (g *groupInfoService) CreateGroup(groupReq request.CreateGroupRequest) (string, int) {
    group := model.GroupInfo{
        Uuid:      fmt.Sprintf("G%s", random.GetNowAndLenRandomString(11)),
        Name:      groupReq.Name,
        Notice:    groupReq.Notice,
        OwnerId:   groupReq.OwnerId,
        MemberCnt: 1,
        AddMode:   groupReq.AddMode,
        Avatar:    groupReq.Avatar,
        Status:    group_status_enum.NORMAL,
        CreatedAt: time.Now(),
        UpdatedAt: time.Now(),
    }
    var members []string
    members = append(members, groupReq.OwnerId)
    var err error
    group.Members, err = json.Marshal(members)
    if err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if res := dao.GormDB.Create(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    // 添加联系人
    contact := model.UserContact{
        UserId:      groupReq.OwnerId,
        ContactId:   group.Uuid,
        ContactType: contact_type_enum.GROUP,
        Status:      contact_status_enum.NORMAL,
        CreatedAt:   time.Now(),
        UpdateAt:    time.Now(),
    }
    if res := dao.GormDB.Create(&contact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    return "创建成功", 0
}
```

创建群聊是通过前端返回的req数据，新建GroupInfo DAO对象，记住一开始群聊只有群主一个人，所以group.Members 应该是只有一个uuid的数组，记住序列化和反序列化操作；创建DAO对象之后，还需要添加一个UserContact DAO对象。该对象的UserId是群主，ContactId是群组id。

该api接口用于需要新建一个群聊的情况。

#### /group/loadMyGroup

**获取我创建的群聊**

<font style="background-color:#FBDE28;">controller</font>

```go
// LoadMyGroup 获取我创建的群聊
func LoadMyGroup(c *gin.Context) {
    var loadMyGroupReq request.OwnlistRequest
    if err := c.BindJSON(&loadMyGroupReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, groupList, ret := gorm.GroupInfoService.LoadMyGroup(loadMyGroupReq.OwnerId)
    JsonBack(c, message, ret, groupList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// LoadMyGroup 获取我创建的群聊
func (g *groupInfoService) LoadMyGroup(ownerId string) (string, []respond.LoadMyGroupRespond, int) {
    var groupList []model.GroupInfo
    if res := dao.GormDB.Order("created_at DESC").Where("owner_id = ?", ownerId).Find(&groupList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var groupListRsp []respond.LoadMyGroupRespond
    for _, group := range groupList {
        groupListRsp = append(groupListRsp, respond.LoadMyGroupRespond{
            GroupId:   group.Uuid,
            GroupName: group.Name,
            Avatar:    group.Avatar,
        })
    }
    return "获取成功", groupListRsp, 0
}
```

通过群主id从GroupInfo数据表中获取是该用户id创建的群组，并新建DTO对象，向前端返回对应的数据。

该api接口用于在联系人列表中获取我创建的群聊并展示出来。

<font style="background-color:#FBDE28;"></font>

#### /group/checkGroupAddMode

**检查群聊加群方式**

<font style="background-color:#FBDE28;">controller</font>

```go
// CheckGroupAddMode 检查群聊加群方式
func CheckGroupAddMode(c *gin.Context) {
    var req request.CheckGroupAddModeRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, addMode, ret := gorm.GroupInfoService.CheckGroupAddMode(req.GroupId)
    JsonBack(c, message, ret, addMode)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// CheckGroupAddMode 检查群聊加群方式
func (g *groupInfoService) CheckGroupAddMode(groupId string) (string, int8, int) {
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", groupId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1, -1
    }
    return "加群方式获取成功", group.AddMode, 0
}
```

检查群聊加群方式直接调用GroupInfo表的关于uuid的条件查询。

该api接口用于在申请入群时，判断是否可以直接加入，还是等待群主审核。如果是直接加入的话，就不会涉及后续新建ContactApply联系人申请记录；如果是群主审核，就会涉及新建ContactApply联系人申请记录，在群主的管理组件会进一步的审核。

#### /group/enterGroupDirectly

**直接进群**

<font style="background-color:#FBDE28;">controller</font>

```go
// EnterGroupDirectly 直接进群
func EnterGroupDirectly(c *gin.Context) {
    var req request.EnterGroupDirectlyRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.EnterGroupDirectly(req.OwnerId, req.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// EnterGroupDirectly 直接进群
// ownerId 是群聊id
func (g *groupInfoService) EnterGroupDirectly(ownerId, contactId string) (string, int) {
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", ownerId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    var members []string
    if err := json.Unmarshal(group.Members, &members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }
    members = append(members, contactId)
    if data, err := json.Marshal(members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    } else {
        group.Members = data
    }
    group.MemberCnt += 1
    if res := dao.GormDB.Save(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    newContact := model.UserContact{
        UserId:      contactId,
        ContactId:   ownerId,
        ContactType: contact_type_enum.GROUP,    // 用户
        Status:      contact_status_enum.NORMAL, // 正常
        CreatedAt:   time.Now(),
        UpdateAt:    time.Now(),
    }
    if res := dao.GormDB.Create(&newContact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "进群成功", 0
}
```

当用户直接进群时，需要将group.Members中的uuid添加一个，群人数也需要加1。之后需要向UserContact表添加一个UserId为新用户，ContactId为群组的联系人记录。

该api接口用于当检查群聊加群方式为直接进入时，将该用户添加到群组中去。

<font style="background-color:#FBDE28;"></font>

#### /group/leaveGroup

**离开群聊**

<font style="background-color:#FBDE28;">controller</font>

```go
// LeaveGroup 退群
func LeaveGroup(c *gin.Context) {
    var req request.LeaveGroupRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.LeaveGroup(req.UserId, req.GroupId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// LeaveGroup 退群
func (g *groupInfoService) LeaveGroup(userId string, groupId string) (string, int) {
    // 从群聊中清除该用户
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", groupId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    var members []string
    if err := json.Unmarshal(group.Members, &members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for i, member := range members {
        if member == userId {
            members = append(members[:i], members[i+1:]...)
            break
        }
    }
    if data, err := json.Marshal(members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    } else {
        group.Members = data
    }
    group.MemberCnt -= 1
    if res := dao.GormDB.Save(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    // 删除会话
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    if res := dao.GormDB.Model(&model.Session{}).Where("send_id = ? AND receive_id = ?", userId, groupId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    // 删除联系人
    if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", userId, groupId).Updates(map[string]interface{}{
        "deleted_at": deletedAt,
        "status":     contact_status_enum.QUIT_GROUP, // 退群
    }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    // 删除申请记录，后面还可以加
    if res := dao.GormDB.Model(&model.ContactApply{}).Where("contact_id = ? AND user_id = ?", groupId, userId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "退群成功", 0
}
```

用户退群时，需要让group.Members添加用户uuid，群人数+1。并且删除该用户与群组的会话记录，删除该用户与群组的联系人记录，删除该用户向群组发起的申请记录。

该api接口用于用户在前端点击退群时。

<font style="background-color:#FBDE28;"></font>

#### /group/dismissGroup

**解散群聊**

<font style="background-color:#FBDE28;">controller</font>

```go
// DismissGroup 解散群聊
func DismissGroup(c *gin.Context) {
    var req request.DismissGroupRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.DismissGroup(req.GroupId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// DismissGroup 解散群聊
func (g *groupInfoService) DismissGroup(groupId string) (string, int) {
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    if res := dao.GormDB.Model(&model.GroupInfo{}).Where("uuid = ?", groupId).Updates(
        map[string]interface{}{
            "deleted_at": deletedAt
        }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    var sessionList []model.Session
    if res := dao.GormDB.Model(&model.Session{}).Where("receive_id = ?", groupId).Find(&sessionList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, session := range sessionList {
        if res := dao.GormDB.Model(&session).Updates(
            map[string]interface{}{
                "deleted_at": deletedAt,
            }); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }

    var userContactList []model.UserContact
    if res := dao.GormDB.Model(&model.UserContact{}).Where("contact_id = ?", groupId).Find(&userContactList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    for _, userContact := range userContactList {
        if res := dao.GormDB.Model(&userContact).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }

    var contactApplys []model.ContactApply
    if res := dao.GormDB.Model(&contactApplys).Where("contact_id = ?", groupId).Find(&contactApplys); res.Error != nil {
        if res.Error != gorm.ErrRecordNotFound {
            zlog.Info(res.Error.Error())
            return "无响应的申请记录需要删除", 0
        }
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, contactApply := range contactApplys {
        if res := dao.GormDB.Model(&contactApply).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }
    return "解散群聊成功", 0
}
```

删除群聊时，需要在GroupInfo表中找到该群聊并软删除；在Session表中找到ReceiveId是群组的所有会话记录并全部软删除；在UserContact表中找到ContactId是群组的所有联系人记录并全部软删除；在ContactApply表中找到ContactId是群组的所有联系人申请记录并全部软删除。

该api接口用于群主群管理中的删除群聊。

#### /group/getGroupInfo

**获取群聊详情**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetGroupInfo 获取群聊详情
func GetGroupInfo(c *gin.Context) {
    var req request.GetGroupInfoRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, groupInfo, ret := gorm.GroupInfoService.GetGroupInfo(req.GroupId)
    JsonBack(c, message, ret, groupInfo)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetGroupInfo 获取群聊详情
func (g *groupInfoService) GetGroupInfo(groupId string) (string, *respond.GetGroupInfoRespond, int) {
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", groupId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    rsp := &respond.GetGroupInfoRespond{
        Uuid:      group.Uuid,
        Name:      group.Name,
        Notice:    group.Notice,
        Avatar:    group.Avatar,
        MemberCnt: group.MemberCnt,
        OwnerId:   group.OwnerId,
        AddMode:   group.AddMode,
        Status:    group.Status,
    }
    if group.DeletedAt.Valid {
        rsp.IsDeleted = true
    } else {
        rsp.IsDeleted = false
    }
    return "获取成功", rsp, 0
}
```

获取群聊详情，从GroupInfo表中uuid条件查询到该DAO群聊对象，并返回DTO对象。

该api接口用于查询该群聊的群聊信息时。

<font style="background-color:#FBDE28;"></font>

#### /group/updateGroupInfo

**更新群组详情**

<font style="background-color:#FBDE28;">controller</font>

```go
// UpdateGroupInfo 更新群聊消息
func UpdateGroupInfo(c *gin.Context) {
    var req request.UpdateGroupInfoRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.UpdateGroupInfo(req)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// UpdateGroupInfo 更新群聊消息
func (g *groupInfoService) UpdateGroupInfo(req request.UpdateGroupInfoRequest) (string, int) {
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", req.Uuid); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if req.Name != "" {
        group.Name = req.Name
    }
    if req.AddMode != -1 {
        group.AddMode = req.AddMode
    }
    if req.Notice != "" {
        group.Notice = req.Notice
    }
    if req.Avatar != "" {
        group.Avatar = req.Avatar
    }
    if res := dao.GormDB.Save(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    // 修改会话
    var sessionList []model.Session
    if res := dao.GormDB.Where("receive_id = ?", req.Uuid).Find(&sessionList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, session := range sessionList {
        session.ReceiveName = group.Name
        session.Avatar = group.Avatar
        log.Println(session)
        if res := dao.GormDB.Save(&session); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }
    return "更新成功", 0
}
```

前端传来更新的数据，后端根据字段是否为空，来针对性更新，最后刷回后台数据库。

这个api接口用于群主在群管理中修改群组信息时。

#### /group/getGroupMemberList

**获取群聊成员列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetGroupMemberList 获取群聊成员列表
func GetGroupMemberList(c *gin.Context) {
    var req request.GetGroupMemberListRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, groupMemberList, ret := gorm.GroupInfoService.GetGroupMemberList(req.GroupId)
    JsonBack(c, message, ret, groupMemberList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
type GetGroupMemberListRespond struct {
	UserId   string `json:"user_id"`
	Nickname string `json:"nickname"`
	Avatar   string `json:"avatar"`
}

// GetGroupMemberList 获取群聊成员列表
func (g *groupInfoService) GetGroupMemberList(groupId string) (string, []respond.GetGroupMemberListRespond, int) {
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", groupId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var members []string
    if err := json.Unmarshal(group.Members, &members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var rspList []respond.GetGroupMemberListRespond
    for _, member := range members {
        var user model.UserInfo
        if res := dao.GormDB.First(&user, "uuid = ?", member); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
        rspList = append(rspList, respond.GetGroupMemberListRespond{
            UserId:   user.Uuid,
            Nickname: user.Nickname,
            Avatar:   user.Avatar,
        })
    }
    return "获取群聊成员列表成功", rspList, 0
}
```

获取群组成员列表，需要先从GroupInfo表中查询到该群组，并遍历group.Members，每个uuid都查询对应的user，并获取到用户的昵称和头像，最后打包返给前端。

该api接口用于管理群成员时需要先获取群成员所有人的昵称，头像。

<font style="background-color:#FBDE28;"></font>

#### /group/removeGroupMembers

**移除群聊成员**

<font style="background-color:#FBDE28;">controller</font>

```go
// RemoveGroupMembers 移除群聊成员
func RemoveGroupMembers(c *gin.Context) {
    var req request.RemoveGroupMembersRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.RemoveGroupMembers(req)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// RemoveGroupMembers 移除群聊成员
func (g *groupInfoService) RemoveGroupMembers(req request.RemoveGroupMembersRequest) (string, int) {
    var group model.GroupInfo
    if res := dao.GormDB.First(&group, "uuid = ?", req.GroupId); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    var members []string
    if err := json.Unmarshal(group.Members, &members); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    log.Println(req.UuidList, req.OwnerId)
    for _, uuid := range req.UuidList {
        if req.OwnerId == uuid {
            return "不能移除群主", -2
        }
        // 从members中找到uuid，移除
        for i, member := range members {
            if member == uuid {
                members = append(members[:i], members[i+1:]...)
            }
        }
        group.MemberCnt -= 1
        // 删除会话
        if res := dao.GormDB.Model(&model.Session{}).Where("send_id = ? AND receive_id = ?", uuid, req.GroupId).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        // 删除联系人
        if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", uuid, req.GroupId).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        // 删除申请记录
        if res := dao.GormDB.Model(&model.ContactApply{}).Where("user_id = ? AND contact_id = ?", uuid, req.GroupId).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }
    group.Members, _ = json.Marshal(members)
    if res := dao.GormDB.Save(&group); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if err := myredis.DelKeysWithPattern("group_info_" + req.GroupId); err != nil {
        zlog.Error(err.Error())
    }
    if err := myredis.DelKeysWithPattern("groupmember_list_" + req.GroupId); err != nil {
        zlog.Error(err.Error())
    }
    return "移除群聊成员成功", 0
}
```

移除n个群聊成员，就相当于执行了n次成员退群。先从GroupInfo表中获取该群组，然后按照uuidList中每个uuid删除对应的会话记录、联系人记录以及联系人申请记录。要记住的是，如果是群主的话，是不能删除的。

该api接口用于群主群管理时移除群成员时。

<font style="background-color:#FBDE28;"></font>

### 联系人功能

#### /contact/getUserList

**获取联系人列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetUserList 获取联系人列表
func GetUserList(c *gin.Context) {
    var myUserListReq request.OwnlistRequest
    if err := c.BindJSON(&myUserListReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
    }
    message, userList, ret := gorm.UserContactService.GetUserList(myUserListReq.OwnerId)
    JsonBack(c, message, ret, userList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
type MyUserListRespond struct {
	UserId   string `json:"user_id"`
	UserName string `json:"user_name"`
	Avatar   string `json:"avatar"`
}

// GetUserList 获取用户列表
// 关于用户被禁用的问题，这里查到的是所有联系人，如果被禁用或被拉黑会以弹窗的形式提醒，无法打开会话框；如果被删除，是搜索不到该联系人的。
func (u *userContactService) GetUserList(ownerId string) (string, []respond.MyUserListRespond, int) {
    var contactList []model.UserContact
    // 没有被删除
    if res := dao.GormDB.Order("created_at DESC").Where("user_id = ? AND status != 4", ownerId).Find(&contactList); res.Error != nil {
        // 不存在不是业务问题，用Info，return 0
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            message := "目前不存在联系人"
            zlog.Info(message)
            return message, nil, 0
        } else {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    // dto
    var userListRsp []respond.MyUserListRespond
    for _, contact := range contactList {
        // 联系人中是用户的
        if contact.ContactType == contact_type_enum.USER {
            // 获取用户信息
            var user model.UserInfo
            if res := dao.GormDB.First(&user, "uuid = ?", contact.ContactId); res.Error != nil {
                // 肯定是存在的，不可能无缘无故删掉，目前不用加notfound的判断
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, nil, -1
            }
            userListRsp = append(userListRsp, respond.MyUserListRespond{
                UserId:   user.Uuid,
                UserName: user.Nickname,
                Avatar:   user.Avatar,
            })
        }
    }
    return "获取用户列表成功", userListRsp, 0
}
```

获取用户列表时，如果是已经删除的用户，UserContact表中已经删除了联系人记录，所以是无法放入用户列表中的。但是如果是被禁用了，用户列表是可以放入的，但是默认是无法向被禁用的用户发起会话的。

该api接口用于联系人列表中获取用户列表时。

#### /contact/loadMyJoinedGroup

**获取我加入的群聊**

<font style="background-color:#FBDE28;">controller</font>

```go
// LoadMyJoinedGroup 获取我加入的群聊
func LoadMyJoinedGroup(c *gin.Context) {
    var loadMyJoinedGroupReq request.OwnlistRequest
    if err := c.BindJSON(&loadMyJoinedGroupReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, groupList, ret := gorm.UserContactService.LoadMyJoinedGroup(loadMyJoinedGroupReq.OwnerId)
    JsonBack(c, message, ret, groupList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// LoadMyJoinedGroup 获取我加入的群聊
func (u *userContactService) LoadMyJoinedGroup(ownerId string) (string, []respond.LoadMyJoinedGroupRespond, int) {
    var contactList []model.UserContact
    // 没有退群，也没有被踢出群聊
    if res := dao.GormDB.Order("created_at DESC").Where("user_id = ? AND status != 6 AND status != 7", ownerId).Find(&contactList); res.Error != nil {
        // 不存在不是业务问题，用Info，return 0
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            message := "目前不存在加入的群聊"
            zlog.Info(message)
            return message, nil, 0
        } else {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var groupList []model.GroupInfo
    for _, contact := range contactList {
        if contact.ContactId[0] == 'G' {
            // 获取群聊信息
            var group model.GroupInfo
            if res := dao.GormDB.First(&group, "uuid = ?", contact.ContactId); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, nil, -1
            }
            // 群没被删除，同时群主不是自己
            // 群主删除或admin删除群聊，status为7，即被踢出群聊，所以不用判断群是否被删除，删除了到不了这步
            if group.OwnerId != ownerId {
                groupList = append(groupList, group)
            }
        }
    }
    var groupListRsp []respond.LoadMyJoinedGroupRespond
    for _, group := range groupList {
        groupListRsp = append(groupListRsp, respond.LoadMyJoinedGroupRespond{
            GroupId:   group.Uuid,
            GroupName: group.Name,
            Avatar:    group.Avatar,
        })
    }
    return "获取加入群成功", groupListRsp, 0
}
```

获取加入群列表，需要从UserContact表中查询没有退出该群或者被该群踢出的且UserId是自己的contactList。然后查看该list中联系人类型是群聊，判断该群聊的群主是否是自己，如果是自己，则不应该加入获取加入群列表，而是应该放入自己创建群组的列表；如果不是自己，就放入获取加入群列表，最后返回给前端。

该api接口用于打开联系人列表中获取加入群列表时。

<font style="background-color:#FBDE28;"></font>

#### /contact/getContactInfo

**获取联系人信息**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetContactInfo 获取联系人信息
func GetContactInfo(c *gin.Context) {
    var getContactInfoReq request.GetContactInfoRequest
    if err := c.BindJSON(&getContactInfoReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    log.Println(getContactInfoReq)
    message, contactInfo, ret := gorm.UserContactService.GetContactInfo(getContactInfoReq.ContactId)
    JsonBack(c, message, ret, contactInfo)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetContactInfo 获取联系人信息
// 调用这个接口的前提是该联系人没有处在删除或被删除，或者该用户还在群聊中
func (u *userContactService) GetContactInfo(contactId string) (string, respond.GetContactInfoRespond, int) {
    if contactId[0] == 'G' {
        var group model.GroupInfo
        if res := dao.GormDB.First(&group, "uuid = ?", contactId); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, respond.GetContactInfoRespond{}, -1
        }
        // 没被禁用
        if group.Status != group_status_enum.DISABLE {
            return "获取联系人信息成功", respond.GetContactInfoRespond{
                ContactId:        group.Uuid,
                ContactName:      group.Name,
                ContactAvatar:    group.Avatar,
                ContactNotice:    group.Notice,
                ContactAddMode:   group.AddMode,
                ContactMembers:   group.Members,
                ContactMemberCnt: group.MemberCnt,
                ContactOwnerId:   group.OwnerId,
            }, 0
        } else {
            zlog.Error("该群聊处于禁用状态")
            return "该群聊处于禁用状态", respond.GetContactInfoRespond{}, -2
        }
    } else {
        var user model.UserInfo
        if res := dao.GormDB.First(&user, "uuid = ?", contactId); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, respond.GetContactInfoRespond{}, -1
        }
        log.Println(user)
        if user.Status != user_status_enum.DISABLE {
            return "获取联系人信息成功", respond.GetContactInfoRespond{
                ContactId:        user.Uuid,
                ContactName:      user.Nickname,
                ContactAvatar:    user.Avatar,
                ContactBirthday:  user.Birthday,
                ContactEmail:     user.Email,
                ContactPhone:     user.Telephone,
                ContactGender:    user.Gender,
                ContactSignature: user.Signature,
            }, 0
        } else {
            zlog.Info("该用户处于禁用状态")
            return "该用户处于禁用状态", respond.GetContactInfoRespond{}, -2
        }
    }
}
```

获取联系人信息，想判断该联系人类型是用户还是群聊。如果是群聊的话，就从GroupInfo表中查询该群聊，并判断群聊状态当前是否已被禁用，如果已经被禁用了，需要返回给前端“群聊被禁用”的消息；反之就正常返回群聊信息。如果是用户的话，从UserInfo表中查询该群聊，流程和群聊相同。

该api接口用于当进入聊天界面时，有对应的联系人，所以需要拿到该联系人的信息。

<font style="background-color:#FBDE28;"></font>

#### /contact/deleteContact

**删除联系人**

<font style="background-color:#FBDE28;">controller</font>

```go
// DeleteContact 删除联系人
func DeleteContact(c *gin.Context) {
    var deleteContactReq request.DeleteContactRequest
    if err := c.BindJSON(&deleteContactReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.DeleteContact(deleteContactReq.OwnerId, deleteContactReq.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// DeleteContact 删除联系人（只包含用户）
func (u *userContactService) DeleteContact(ownerId, contactId string) (string, int) {
    // status改变为删除
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", ownerId, contactId).Updates(map[string]interface{}{
        "deleted_at": deletedAt,
        "status":     contact_status_enum.DELETE,
    }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", contactId, ownerId).Updates(map[string]interface{}{
        "deleted_at": deletedAt,
        "status":     contact_status_enum.BE_DELETE,
    }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    if res := dao.GormDB.Model(&model.Session{}).Where("send_id = ? AND receive_id = ?", ownerId, contactId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }

    if res := dao.GormDB.Model(&model.Session{}).Where("send_id = ? AND receive_id = ?", contactId, ownerId).Update("deleted_at", deletedAt); res.Error != nil {
		zlog.Error(res.Error.Error())
		return constants.SYSTEM_ERROR, -1
	}
    // 联系人添加的记录得删，这样之后再添加就看新的申请记录，如果申请记录结果是拉黑就没法再添加，如果是拒绝可以再添加
    if res := dao.GormDB.Model(&model.ContactApply{}).Where("contact_id = ? AND user_id = ?", ownerId, contactId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if res := dao.GormDB.Model(&model.ContactApply{}).Where("contact_id = ? AND user_id = ?", contactId, ownerId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "删除联系人成功", 0
}
```

删除联系人，这个接口只包含删除用户类型的联系人，需要从UserContact表中查询到2条与自己和该用户有关的联系人记录，即userId是自己，contactId是该用户和userId是该用户，contactId是自己（这里用户与用户类型之间是两条联系人记录，用户和群聊之间只有一条联系人记录，即userId是用户，contactId是群聊）。然后需要删除彼此的会话记录。再删除彼此的联系人申请记录。

该api接口用于用户在删除自己的用户联系人时。

###

#### /contact/applyContact

**申请添加联系人**

<font style="background-color:#FBDE28;">controller</font>

```go
// ApplyContact 申请添加联系人
func ApplyContact(c *gin.Context) {
    var applyContactReq request.ApplyContactRequest
    if err := c.BindJSON(&applyContactReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.ApplyContact(applyContactReq)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// ApplyContact 申请添加联系人
func (u *userContactService) ApplyContact(req request.ApplyContactRequest) (string, int) {
	if req.ContactId[0] == 'U' {
		var user model.UserInfo
		if res := dao.GormDB.First(&user, "uuid = ?", req.ContactId); res.Error != nil {
			if errors.Is(res.Error, gorm.ErrRecordNotFound) {
				zlog.Error("用户不存在")
				return "用户不存在", -2
			} else {
				zlog.Error(res.Error.Error())
				return constants.SYSTEM_ERROR, -1
			}
		}

		if user.Status == user_status_enum.DISABLE {
			zlog.Info("群聊已被禁用")
			return "群聊已被禁用", -2
		}
		var contactApply model.ContactApply
		if res := dao.GormDB.Where("user_id = ? AND contact_id = ?", req.OwnerId, req.ContactId).First(&contactApply); res.Error != nil {
			if errors.Is(res.Error, gorm.ErrRecordNotFound) {
				contactApply = model.ContactApply{
					Uuid:        fmt.Sprintf("A%s", random.GetNowAndLenRandomString(11)),
					UserId:      req.OwnerId,
					ContactId:   req.ContactId,
					ContactType: contact_type_enum.USER,
					Status:      contact_apply_status_enum.PENDING,
					Message:     req.Message,
					LastApplyAt: time.Now(),
				}
				if res := dao.GormDB.Create(&contactApply); res.Error != nil {
					zlog.Error(res.Error.Error())
					return constants.SYSTEM_ERROR, -1
				}
			} else {
				zlog.Error(res.Error.Error())
				return constants.SYSTEM_ERROR, -1
			}
		}
		// 如果存在申请记录，先看看有没有被拉黑
		if contactApply.Status == contact_apply_status_enum.BLACK {
			return "对方已将你拉黑", -2
		}
		contactApply.LastApplyAt = time.Now()

		if res := dao.GormDB.Save(&contactApply); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		return "申请成功", 0
	} else if req.ContactId[0] == 'G' {
		var group model.GroupInfo
		if res := dao.GormDB.First(&group, "uuid = ?", req.ContactId); res.Error != nil {
			if errors.Is(res.Error, gorm.ErrRecordNotFound) {
				zlog.Error("群聊不存在")
				return "群聊不存在", -2
			} else {
				zlog.Error(res.Error.Error())
				return constants.SYSTEM_ERROR, -1
			}
		}
		if group.Status == group_status_enum.DISABLE {
			zlog.Info("群聊已被禁用")
			return "群聊已被禁用", -2
		}
		var contactApply model.ContactApply
		if res := dao.GormDB.Where("user_id = ? AND contact_id = ?", req.OwnerId, req.ContactId).First(&contactApply); res.Error != nil {
			if errors.Is(res.Error, gorm.ErrRecordNotFound) {
				contactApply = model.ContactApply{
					Uuid:        fmt.Sprintf("A%s", random.GetNowAndLenRandomString(11)),
					UserId:      req.OwnerId,
					ContactId:   req.ContactId,
					ContactType: contact_type_enum.GROUP,
					Status:      contact_apply_status_enum.PENDING,
					Message:     req.Message,
					LastApplyAt: time.Now(),
				}
				if res := dao.GormDB.Create(&contactApply); res.Error != nil {
					zlog.Error(res.Error.Error())
					return constants.SYSTEM_ERROR, -1
				}
			} else {
				zlog.Error(res.Error.Error())
				return constants.SYSTEM_ERROR, -1
			}
		}
		contactApply.LastApplyAt = time.Now()

		if res := dao.GormDB.Save(&contactApply); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		return "申请成功", 0
	} else {
		return "用户/群聊不存在", -2
	}

}
```

申请添加联系人，如果联系人是用户类型，先从UserInfo表中查询看是否该用户已经被软删除了，如果删除了，提示“用户不存在”。之后看用户状态是不是正常，如果被禁用了，会提示“用户已被禁用”。之后需要查询是否之前已经有过申请记录，如果没有申请记录，就需要先创建申请记录，然后将状态设为申请中。如果有申请记录，需要先判断对方是不是已经处理该申请记录，将你拉黑，如果拉黑了，会提示“对方已拉黑”；如果没拉黑，会更新申请记录的时间，但不会重复申请。

如果联系人是群聊类型，和上面是相似的。

该api接口用于申请添加用户或者群聊时。

#### /contact/getNewContactList

**获取新的联系人申请列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetNewContactList 获取新的联系人申请列表
func GetNewContactList(c *gin.Context) {
    var req request.OwnlistRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, data, ret := gorm.UserContactService.GetNewContactList(req.OwnerId)
    JsonBack(c, message, ret, data)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetNewContactList 获取新的联系人申请列表
func (u *userContactService) GetNewContactList(ownerId string) (string, []respond.NewContactListRespond, int) {
    var contactApplyList []model.ContactApply
    if res := dao.GormDB.Where("contact_id = ? AND status = ?", ownerId, contact_apply_status_enum.PENDING).Find(&contactApplyList); res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            zlog.Info("没有在申请的联系人")
            return "没有在申请的联系人", nil, 0
        } else {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var rsp []respond.NewContactListRespond
	// 所有contact都没被删除
    for _, contactApply := range contactApplyList {
        var message string
        if contactApply.Message == "" {
            message = "申请理由：无"
        } else {
            message = "申请理由：" + contactApply.Message
        }
        newContact := respond.NewContactListRespond{
            ContactId: contactApply.Uuid,
            Message:   message,
        }
        var user model.UserInfo
        if res := dao.GormDB.First(&user, "uuid = ?", contactApply.UserId); res.Error != nil {
            return constants.SYSTEM_ERROR, nil, -1
        }
        newContact.ContactId = user.Uuid
        newContact.ContactName = user.Nickname
        newContact.ContactAvatar = user.Avatar
        rsp = append(rsp, newContact)
    }
    return "获取成功", rsp, 0
}
```

获取新联系人申请列表，从ContactApply表中查询出contactId是自己且状态为在申请中的列表，这时候获取的列表中所有联系人都没被删除，最后打包数据，返回给前端。

该api接口用于自己在处理联系人的申请时。

<font style="background-color:#FBDE28;"></font>

<font style="background-color:#FBDE28;"></font>

#### /contact/passContactApply

**通过联系人申请**

<font style="background-color:#FBDE28;">controller</font>

```go
// PassContactApply 通过联系人申请
func PassContactApply(c *gin.Context) {
    var passContactApplyReq request.PassContactApplyRequest
    if err := c.BindJSON(&passContactApplyReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.PassContactApply(passContactApplyReq.OwnerId, passContactApplyReq.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// PassContactApply 通过联系人申请
func (u *userContactService) PassContactApply(ownerId string, contactId string) (string, int) {
	// ownerId 如果是用户的话就是登录用户，如果是群聊的话就是群聊id
	var contactApply model.ContactApply
	if res := dao.GormDB.Where("contact_id = ? AND user_id = ?", ownerId, contactId).First(&contactApply); res.Error != nil {
		zlog.Error(res.Error.Error())
		return constants.SYSTEM_ERROR, -1
	}
	if ownerId[0] == 'U' {
		var user model.UserInfo
		if res := dao.GormDB.Where("uuid = ?", contactId).Find(&user); res.Error != nil {
			zlog.Error(res.Error.Error())
		}
		if user.Status == user_status_enum.DISABLE {
			zlog.Error("用户已被禁用")
			return "用户已被禁用", -2
		}
		contactApply.Status = contact_apply_status_enum.AGREE
		if res := dao.GormDB.Save(&contactApply); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		newContact := model.UserContact{
			UserId:      ownerId,
			ContactId:   contactId,
			ContactType: contact_type_enum.USER,     // 用户
			Status:      contact_status_enum.NORMAL, // 正常
			CreatedAt:   time.Now(),
			UpdateAt:    time.Now(),
		}
		if res := dao.GormDB.Create(&newContact); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		anotherContact := model.UserContact{
			UserId:      contactId,
			ContactId:   ownerId,
			ContactType: contact_type_enum.USER,     // 用户
			Status:      contact_status_enum.NORMAL, // 正常
			CreatedAt:   newContact.CreatedAt,
			UpdateAt:    newContact.UpdateAt,
		}
		if res := dao.GormDB.Create(&anotherContact); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		return "已添加该联系人", 0
	} else {
		var group model.GroupInfo
		if res := dao.GormDB.Where("uuid = ?", ownerId).Find(&group); res.Error != nil {
			zlog.Error(res.Error.Error())
		}
		if group.Status == group_status_enum.DISABLE {
			zlog.Error("群聊已被禁用")
			return "群聊已被禁用", -2
		}
		contactApply.Status = contact_apply_status_enum.AGREE
		if res := dao.GormDB.Save(&contactApply); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		// 群聊就只用创建一个UserContact，因为一个UserContact足以表达双方的状态
		newContact := model.UserContact{
			UserId:      contactId,
			ContactId:   ownerId,
			ContactType: contact_type_enum.GROUP,    // 用户
			Status:      contact_status_enum.NORMAL, // 正常
			CreatedAt:   time.Now(),
			UpdateAt:    time.Now(),
		}
		if res := dao.GormDB.Create(&newContact); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		var members []string
		if err := json.Unmarshal(group.Members, &members); err != nil {
			zlog.Error(err.Error())
			return constants.SYSTEM_ERROR, -1
		}
		members = append(members, contactId)
		group.MemberCnt = len(members)
		group.Members, _ = json.Marshal(members)
		if res := dao.GormDB.Save(&group); res.Error != nil {
			zlog.Error(res.Error.Error())
			return constants.SYSTEM_ERROR, -1
		}
		return "已通过加群申请", 0
	}

}
```

通过联系人申请，参数中ownerId如果是用户就是用户id，如果是群聊就是群聊id，不是群主id。意思是该用户或者该群聊通过contactId的申请。如果是用户，先通过UserInfo表查询出该用户，看是否已经被禁用了。如果未被禁用，就将申请状态改为同意。然后添加对应的UserContact记录。

如果是群聊的话，先通过GroupInfo表查询出该群聊，看是否已经被禁用了。如果未被禁用，就将申请状态改为同意，并添加对应的UserContact记录，还需要向群组中添加该成员，群人数+1。

该api接口用于用户在申请添加联系人时，如果该联系人是用户，则等待用户处理，用户点击同意即申请通过；如果该联系人是群聊，如果是直接入群，则会在申请加群之后，直接调用该api接口，如果是审核的话，也会等群主点击同意后申请通过。

#### /contact/blackContact

**拉黑联系人**

<font style="background-color:#FBDE28;">controller</font>

```go
// BlackContact 拉黑联系人
func BlackContact(c *gin.Context) {
    var req request.BlackContactRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.BlackContact(req.OwnerId, req.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// BlackContact 拉黑联系人
func (u *userContactService) BlackContact(ownerId string, contactId string) (string, int) {
    // 拉黑
    if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", ownerId, contactId).Updates(map[string]interface{}{
        "status":    contact_status_enum.BLACK,
        "update_at": time.Now(),
    }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    // 被拉黑
    if res := dao.GormDB.Model(&model.UserContact{}).Where("user_id = ? AND contact_id = ?", contactId, ownerId).Updates(map[string]interface{}{
        "status":    contact_status_enum.BE_BLACK,
        "update_at": time.Now(),
    }); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    // 删除会话
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    if res := dao.GormDB.Model(&model.Session{}).Where("send_id = ? AND receive_id = ?", ownerId, contactId).Update("deleted_at", deletedAt); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "已拉黑该联系人", 0
}
```

拉黑联系人，需要从UserContact找出彼此的联系人记录。将拉黑方的状态置为拉黑，将被拉黑方的状态置为被拉黑。并删除彼此的会话记录。

该api接口用于用户拉黑联系人（用户）时。

#### /contact/cancelBlackContact

**解除拉黑联系人**

<font style="background-color:#FBDE28;">controller</font>

```go
// CancelBlackContact 解除拉黑联系人
func CancelBlackContact(c *gin.Context) {
    var req request.BlackContactRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.CancelBlackContact(req.OwnerId, req.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// CancelBlackContact 取消拉黑联系人
func (u *userContactService) CancelBlackContact(ownerId string, contactId string) (string, int) {
    // 因为前端的设定，这里需要判断一下ownerId和contactId是不是有拉黑和被拉黑的状态
    var blackContact model.UserContact
    if res := dao.GormDB.Where("user_id = ? AND contact_id = ?", ownerId, contactId).First(&blackContact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if blackContact.Status != contact_status_enum.BLACK {
        return "未拉黑该联系人，无需解除拉黑", -2
    }
    var beBlackContact model.UserContact
    if res := dao.GormDB.Where("user_id = ? AND contact_id = ?", contactId, ownerId).First(&beBlackContact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if beBlackContact.Status != contact_status_enum.BE_BLACK {
        return "该联系人未被拉黑，无需解除拉黑", -2
    }

    // 取消拉黑
    blackContact.Status = contact_status_enum.NORMAL
    beBlackContact.Status = contact_status_enum.NORMAL
    if res := dao.GormDB.Save(&blackContact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if res := dao.GormDB.Save(&beBlackContact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "已解除拉黑该联系人", 0
}
```

解除拉黑联系人，需要先判断目前双方是否处于拉黑或者被拉黑的状态，如果依旧处于该状态，将双方的拉黑状态改为正常状态即可。

该api接口用于前端右键点击联系人列表中某联系人后，点击拉黑时。

#### /contact/getAddGroupList

**获取新的群聊申请列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetAddGroupList 获取新的群聊申请列表
func GetAddGroupList(c *gin.Context) {
    var req request.AddGroupListRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, data, ret := gorm.UserContactService.GetAddGroupList(req.GroupId)
    JsonBack(c, message, ret, data)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetAddGroupList 获取新的加群列表
// 前端已经判断调用接口的用户是群主，也只有群主才能调用这个接口
func (u *userContactService) GetAddGroupList(groupId string) (string, []respond.AddGroupListRespond, int) {
    var contactApplyList []model.ContactApply
    if res := dao.GormDB.Where("contact_id = ? AND status = ?", groupId, contact_apply_status_enum.PENDING).Find(&contactApplyList); res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            zlog.Info("没有在申请的联系人")
            return "没有在申请的联系人", nil, 0
        } else {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var rsp []respond.AddGroupListRespond
    for _, contactApply := range contactApplyList {
        var message string
        if contactApply.Message == "" {
            message = "申请理由：无"
        } else {
            message = "申请理由：" + contactApply.Message
        }
        newContact := respond.AddGroupListRespond{
            ContactId: contactApply.Uuid,
            Message:   message,
        }
        var user model.UserInfo
        if res := dao.GormDB.First(&user, "uuid = ?", contactApply.UserId); res.Error != nil {
            return constants.SYSTEM_ERROR, nil, -1
        }
        newContact.ContactId = user.Uuid
        newContact.ContactName = user.Nickname
        newContact.ContactAvatar = user.Avatar
        rsp = append(rsp, newContact)
    }
    return "获取成功", rsp, 0
}
```

获取新的群聊申请列表，从ContactApply表中查询出contactId是群聊且状态为在申请中的列表，这时候获取的列表中所有联系人都没被删除，最后打包数据，返回给前端。

该api接口用于群聊在处理联系人的申请时。

#### /contact/refuseContactApply

**拒绝联系人申请**

<font style="background-color:#FBDE28;">controller</font>

```go
// RefuseContactApply 拒绝联系人申请
func RefuseContactApply(c *gin.Context) {
    var passContactApplyReq request.PassContactApplyRequest
    if err := c.BindJSON(&passContactApplyReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.RefuseContactApply(passContactApplyReq.OwnerId, passContactApplyReq.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// RefuseContactApply 拒绝联系人申请
func (u *userContactService) RefuseContactApply(ownerId string, contactId string) (string, int) {
    // ownerId 如果是用户的话就是登录用户，如果是群聊的话就是群聊id
    var contactApply model.ContactApply
    if res := dao.GormDB.Where("contact_id = ? AND user_id = ?", ownerId, contactId).First(&contactApply); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    contactApply.Status = contact_apply_status_enum.REFUSE
    if res := dao.GormDB.Save(&contactApply); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    if ownerId[0] == 'U' {
        return "已拒绝该联系人申请", 0
    } else {
        return "已拒绝该加群申请", 0
    }

}
```

拒绝联系人申请，先通过ContactApply表查询对应的申请记录，并把该记录状态修改为拒绝，保存后，返回结果给前端。

该api接口用于在处理联系人申请，点击拒绝时。

<font style="background-color:#FBDE28;"></font>

#### /contact/blackApply

**拉黑申请**

<font style="background-color:#FBDE28;">controller</font>

```go
// BlackApply 拉黑申请
func BlackApply(c *gin.Context) {
    var req request.BlackApplyRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserContactService.BlackApply(req.OwnerId, req.ContactId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// BlackApply 拉黑申请
func (u *userContactService) BlackApply(ownerId string, contactId string) (string, int) {
    var contactApply model.ContactApply
    if res := dao.GormDB.Where("contact_id = ? AND user_id = ?", ownerId, contactId).First(&contactApply); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    contactApply.Status = contact_apply_status_enum.BLACK
    if res := dao.GormDB.Save(&contactApply); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "已拉黑该申请", 0
}
```

拉黑联系人申请，先通过ContactApply表查询对应的申请记录，并把该记录状态修改为拉黑，保存后，返回结果给前端。

该api接口用于在处理联系人申请，点击拉黑时。

<font style="background-color:#FBDE28;"></font>

### 会话功能

#### /session/openSession

**打开会话**

<font style="background-color:#FBDE28;">controller</font>

```go
// OpenSession 打开会话
func OpenSession(c *gin.Context) {
    var openSessionReq request.OpenSessionRequest
    if err := c.BindJSON(&openSessionReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, sessionId, ret := gorm.SessionService.OpenSession(openSessionReq)
    JsonBack(c, message, ret, sessionId)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// OpenSession 打开会话
func (s *sessionService) OpenSession(req request.OpenSessionRequest) (string, string, int) {
    var session model.Session
    if res := dao.GormDB.Where("send_id = ? and receive_id = ?", req.SendId, req.ReceiveId).First(&session); res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            zlog.Info("会话没有找到，将新建会话")
            createReq := request.CreateSessionRequest{
                SendId:    req.SendId,
                ReceiveId: req.ReceiveId,
            }
            return s.CreateSession(createReq)
        }
    }
    return "会话创建成功", session.Uuid, 0
}
```

打开会话，先从Session表中查询看是否存在该会话，如果不存在新建会话。

该api接口用于前端点击某联系人时，会调用该接口打开与该联系人的聊天会话。

#### /session/getUserSessionList

**获取用户会话列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetUserSessionList 获取用户会话列表
func GetUserSessionList(c *gin.Context) {
    var getUserSessionListReq request.OwnlistRequest
    if err := c.BindJSON(&getUserSessionListReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, sessionList, ret := gorm.SessionService.GetUserSessionList(getUserSessionListReq.OwnerId)
    JsonBack(c, message, ret, sessionList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetUserSessionList 获取用户会话列表
func (s *sessionService) GetUserSessionList(ownerId string) (string, []respond.UserSessionListRespond, int) {
    var sessionList []model.Session
    if res := dao.GormDB.Order("created_at DESC").Where("send_id = ?", ownerId).Find(&sessionList); res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            zlog.Info("未创建用户会话")
            return "未创建用户会话", nil, 0
        } else {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var sessionListRsp []respond.UserSessionListRespond
    for i := 0; i < len(sessionList); i++ {
        if sessionList[i].ReceiveId[0] == 'U' {
            sessionListRsp = append(sessionListRsp, respond.UserSessionListRespond{
                SessionId: sessionList[i].Uuid,
                Avatar:    sessionList[i].Avatar,
                UserId:    sessionList[i].ReceiveId,
                Username:  sessionList[i].ReceiveName,
            })
        }
    }
    return "获取成功", sessionListRsp, 0
}
```

获取用户会话列表，从Session表中收集所有的sendId是自己的会话，如果没有，则返回“未创建用户会话”。如果有，打包将数据返回给前端。

该api接口用于点击会话列表中用户列表时。

<font style="background-color:#FBDE28;"></font>

#### /session/getGroupSessionList

**获取群聊会话列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetGroupSessionList 获取用户群聊列表
func GetGroupSessionList(c *gin.Context) {
    var getGroupListReq request.OwnlistRequest
    if err := c.BindJSON(&getGroupListReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, groupList, ret := gorm.SessionService.GetGroupSessionList(getGroupListReq.OwnerId)
    JsonBack(c, message, ret, groupList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetGroupSessionList 获取群聊会话列表
func (s *sessionService) GetGroupSessionList(ownerId string) (string, []respond.GroupSessionListRespond, int) {
    var sessionList []model.Session
    if res := dao.GormDB.Order("created_at DESC").Where("send_id = ?", ownerId).Find(&sessionList); res.Error != nil {
        if errors.Is(res.Error, gorm.ErrRecordNotFound) {
            zlog.Info("未创建群聊会话")
            return "未创建群聊会话", nil, 0
        } else {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, nil, -1
        }
    }
    var sessionListRsp []respond.GroupSessionListRespond
    for i := 0; i < len(sessionList); i++ {
        if sessionList[i].ReceiveId[0] == 'G' {
            sessionListRsp = append(sessionListRsp, respond.GroupSessionListRespond{
                SessionId: sessionList[i].Uuid,
                Avatar:    sessionList[i].Avatar,
                GroupId:   sessionList[i].ReceiveId,
                GroupName: sessionList[i].ReceiveName,
            })
        }
    }
    rspString, err := json.Marshal(sessionListRsp)
    if err != nil {
        zlog.Error(err.Error())
    }
    if err := myredis.SetKeyEx("group_session_list_"+ownerId, string(rspString), time.Minute*constants.REDIS_TIMEOUT); err != nil {
        zlog.Error(err.Error())
    }
    return "获取成功", sessionListRsp, 0
}
```

获取群聊会话列表，从Session表中收集所有的sendId是自己的会话，如果没有，则返回“未创建群聊会话”。如果有，打包将数据返回给前端。

该api接口用于点击会话列表中群聊列表时。

<font style="background-color:#FBDE28;"></font>

#### /session/deleteSession

<font style="background-color:#FBDE28;">controller</font>

```go
// DeleteSession 删除会话
func DeleteSession(c *gin.Context) {
    var deleteSessionReq request.DeleteSessionRequest
    if err := c.BindJSON(&deleteSessionReq); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.SessionService.DeleteSession(deleteSessionReq.SessionId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// DeleteSession 删除会话
func (s *sessionService) DeleteSession(sessionId string) (string, int) {

    var session model.Session
    if res := dao.GormDB.Where("uuid = ?", sessionId).Find(&session); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    session.DeletedAt.Valid = true
    session.DeletedAt.Time = time.Now()
    if res := dao.GormDB.Save(&session); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    return "删除成功", 0
}
```

删除会话，则直接从Session表中查询该会话，并软删除。

该api接口用于删除会话。

<font style="background-color:#FBDE28;"></font>

#### /session/checkOpenSessionAllowed

**检查是否可以打开会话**

<font style="background-color:#FBDE28;">controller</font>

```go
// CheckOpenSessionAllowed 检查是否可以打开会话
func CheckOpenSessionAllowed(c *gin.Context) {
    var req request.CreateSessionRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, res, ret := gorm.SessionService.CheckOpenSessionAllowed(req.SendId, req.ReceiveId)
    JsonBack(c, message, ret, res)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// CheckOpenSessionAllowed 检查是否允许发起会话
func (s *sessionService) CheckOpenSessionAllowed(sendId, receiveId string) (string, bool, int) {
    var contact model.UserContact
    if res := dao.GormDB.Where("user_id = ? and contact_id = ?", sendId, receiveId).First(&contact); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, false, -1
    }
    if contact.Status == contact_status_enum.BE_BLACK {
        return "已被对方拉黑，无法发起会话", false, -2
    } else if contact.Status == contact_status_enum.BLACK {
        return "已拉黑对方，先解除拉黑状态才能发起会话", false, -2
    }
    if receiveId[0] == 'U' {
        var user model.UserInfo
        if res := dao.GormDB.Where("uuid = ?", receiveId).First(&user); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, false, -1
        }
        if user.Status == user_status_enum.DISABLE {
            zlog.Info("对方已被禁用，无法发起会话")
            return "对方已被禁用，无法发起会话", false, -2
        }
    } else {
        var group model.GroupInfo
        if res := dao.GormDB.Where("uuid = ?", receiveId).First(&group); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, false, -1
        }
        if group.Status == group_status_enum.DISABLE {
            zlog.Info("对方已被禁用，无法发起会话")
            return "对方已被禁用，无法发起会话", false, -2
        }
    }
    return "可以发起会话", true, 0
}
```

检查是否允许发起会话，先看双方是否处于拉黑或被拉黑的状态。如果是正常状态，需要看对方是否处于被禁用的状态，如果不是被禁用的状态，则可以发起会话。

该api接口用于打开会话之前，需要先判断一下是否可以开启会话。

<font style="background-color:#FBDE28;"></font>

### 消息功能

#### /message/getMessageList

**获取聊天记录**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetMessageList 获取聊天记录
func GetMessageList(c *gin.Context) {
    var req request.GetMessageListRequest
    if err := c.BindJSON(&req); err != nil {
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, rsp, ret := gorm.MessageService.GetMessageList(req.UserOneId, req.UserTwoId)
    JsonBack(c, message, ret, rsp)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetMessageList 获取聊天记录
func (m *messageService) GetMessageList(userOneId, userTwoId string) (string, []respond.GetMessageListRespond, int) {
    var messageList []model.Message
    if res := dao.GormDB.Where("(send_id = ? AND receive_id = ?) OR (send_id = ? AND receive_id = ?)", userOneId, userTwoId, userTwoId, userOneId).Order("created_at ASC").Find(&messageList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var rspList []respond.GetMessageListRespond
    for _, message := range messageList {
        rspList = append(rspList, respond.GetMessageListRespond{
            SendId:     message.SendId,
            SendName:   message.SendName,
            SendAvatar: message.SendAvatar,
            ReceiveId:  message.ReceiveId,
            Content:    message.Content,
            Url:        message.Url,
            Type:       message.Type,
            FileType:   message.FileType,
            FileName:   message.FileName,
            FileSize:   message.FileSize,
            CreatedAt:  message.CreatedAt.Format("2006-01-02 15:04:05"),
        })
    }
    return "获取聊天记录成功", rspList, 0
}
```

获取聊天记录，从Message表中查询双方的聊天记录，包括自己是sendId或者是receiveId。并把聊天记录打包发给前端。

该api接口用于当打开会话之后，会获取对应聊天记录。

<font style="background-color:#FBDE28;"></font>

#### /message/getGroupMessageList

**获取群聊消息记录**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetGroupMessageList 获取群聊消息记录
func GetGroupMessageList(c *gin.Context) {
    var req request.GetGroupMessageListRequest
    if err := c.BindJSON(&req); err != nil {
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, rsp, ret := gorm.MessageService.GetGroupMessageList(req.GroupId)
    JsonBack(c, message, ret, rsp)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetGroupMessageList 获取群聊消息记录
func (m *messageService) GetGroupMessageList(groupId string) (string, []respond.GetGroupMessageListRespond, int) {
    var messageList []model.Message
    if res := dao.GormDB.Where("receive_id = ?", groupId).Order("created_at ASC").Find(&messageList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var rspList []respond.GetGroupMessageListRespond
    for _, message := range messageList {
        rsp := respond.GetGroupMessageListRespond{
            SendId:     message.SendId,
            SendName:   message.SendName,
            SendAvatar: message.SendAvatar,
            ReceiveId:  message.ReceiveId,
            Content:    message.Content,
            Url:        message.Url,
            Type:       message.Type,
            FileType:   message.FileType,
            FileName:   message.FileName,
            FileSize:   message.FileSize,
            CreatedAt:  message.CreatedAt.Format("2006-01-02 15:04:05"),
        }
        rspList = append(rspList, rsp)
    }
    return "获取聊天记录成功", rspList, 0
}
```

获取群聊消息记录，从Message表中该群聊的聊天记录。并把聊天记录打包发给前端。

该api接口用于当打开会话之后，会获取对应聊天记录。

### 管理员功能

#### /user/getUserInfoList

**获取用户列表 - 管理员**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetUserInfoList 获取用户列表
func GetUserInfoList(c *gin.Context) {
    var req request.GetUserInfoListRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, userList, ret := gorm.UserInfoService.GetUserInfoList(req.OwnerId)
    JsonBack(c, message, ret, userList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetUserInfoList 获取用户列表除了ownerId之外 - 管理员
// 管理员少，而且如果用户更改了，那么管理员会一直频繁删除redis，更新redis，比较麻烦，所以管理员暂时不使用redis缓存
func (u *userInfoService) GetUserInfoList(ownerId string) (string, []respond.GetUserListRespond, int) {
    // redis中没有数据，从数据库中获取
    var users []model.UserInfo
    // 获取所有的用户
    if res := dao.GormDB.Unscoped().Where("uuid != ?", ownerId).Find(&users); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var rsp []respond.GetUserListRespond
    for _, user := range users {
        rp := respond.GetUserListRespond{
            Uuid:      user.Uuid,
            Telephone: user.Telephone,
            Nickname:  user.Nickname,
            Status:    user.Status,
            IsAdmin:   user.IsAdmin,
        }
        if user.DeletedAt.Valid {
            rp.IsDeleted = true
        } else {
            rp.IsDeleted = false
        }
        rsp = append(rsp, rp)
    }
    return "获取用户列表成功", rsp, 0
}
```

获取用户列表，先从UserInfo表中获取除了管理员自己的用户，这里使用.Unscoped屏蔽软删除。最后打包好返回给前端。

该api接口用于管理员在获取除了自己以外的用户列表时。

<font style="background-color:#FBDE28;"></font>

#### /user/ableUsers

**启用用户**

<font style="background-color:#FBDE28;">controller</font>

```go
// AbleUsers 启用用户
func AbleUsers(c *gin.Context) {
    var req request.AbleUsersRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserInfoService.AbleUsers(req.UuidList)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// AbleUsers 启用用户
// 用户是否启用禁用需要实时更新contact_user_list状态，所以redis的contact_user_list需要删除
func (u *userInfoService) AbleUsers(uuidList []string) (string, int) {
    var users []model.UserInfo
    if res := dao.GormDB.Model(model.UserInfo{}).Where("uuid in (?)", uuidList).Find(&users); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, user := range users {
        user.Status = user_status_enum.NORMAL
        if res := dao.GormDB.Save(&user); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }
    // 删除所有"contact_user_list"开头的key
    if err := myredis.DelKeysWithPrefix("contact_user_list"); err != nil {
        zlog.Error(err.Error())
    }
    return "启用用户成功", 0
}
```

启用用户，先从UserInfo表中查询出所有非自己的用户，并注意修改状态至正常。

该api接口用于管理员禁用用户后启用用户。

<font style="background-color:#FBDE28;"></font>

#### /user/disableUsers

**禁用用户**

<font style="background-color:#FBDE28;">controller</font>

```go
// DisableUsers 禁用用户
func DisableUsers(c *gin.Context) {
    var req request.AbleUsersRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserInfoService.DisableUsers(req.UuidList)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// DisableUsers 禁用用户
// 用户是否启用禁用需要实时更新contact_user_list状态，所以redis的contact_user_list需要删除
func (u *userInfoService) DisableUsers(uuidList []string) (string, int) {
    var users []model.UserInfo
    if res := dao.GormDB.Model(model.UserInfo{}).Where("uuid in (?)", uuidList).Find(&users); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, user := range users {
        user.Status = user_status_enum.DISABLE
        if res := dao.GormDB.Save(&user); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        var sessionList []model.Session
        if res := dao.GormDB.Where("send_id = ? or receive_id = ?", user.Uuid, user.Uuid).Find(&sessionList); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        for _, session := range sessionList {
            var deletedAt gorm.DeletedAt
            deletedAt.Time = time.Now()
            deletedAt.Valid = true
            session.DeletedAt = deletedAt
            if res := dao.GormDB.Save(&session); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
    }
    // 删除所有"contact_user_list"开头的key
    if err := myredis.DelKeysWithPrefix("contact_user_list"); err != nil {
        zlog.Error(err.Error())
    }
    return "禁用用户成功", 0
}
```

禁用用户，先从UserInfo表中查询出所有非自己的用户，并注意修改状态至禁用，并删除每个用户跟自己有关的会话记录。

该api接口用于管理员禁用用户。

<font style="background-color:#FBDE28;"></font>

#### /user/deleteUsers

**删除用户**

<font style="background-color:#FBDE28;">controller</font>

```go
// DeleteUsers 删除用户
func DeleteUsers(c *gin.Context) {
    var req request.AbleUsersRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserInfoService.DeleteUsers(req.UuidList)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// DeleteUsers 删除用户
// 用户是否启用禁用需要实时更新contact_user_list状态，所以redis的contact_user_list需要删除
func (u *userInfoService) DeleteUsers(uuidList []string) (string, int) {
    var users []model.UserInfo
    if res := dao.GormDB.Model(model.UserInfo{}).Where("uuid in (?)", uuidList).Find(&users); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, user := range users {
        user.DeletedAt.Valid = true
        user.DeletedAt.Time = time.Now()
        if res := dao.GormDB.Save(&user); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }

        // 删除会话
        var sessionList []model.Session
        if res := dao.GormDB.Where("send_id = ? or receive_id = ?", user.Uuid, user.Uuid).Find(&sessionList); res.Error != nil {
            if errors.Is(res.Error, gorm.ErrRecordNotFound) {
                zlog.Info(res.Error.Error())
            } else {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
        for _, session := range sessionList {
            var deletedAt gorm.DeletedAt
            deletedAt.Time = time.Now()
            deletedAt.Valid = true
            session.DeletedAt = deletedAt
            if res := dao.GormDB.Save(&session); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }

        // 删除联系人
        var contactList []model.UserContact
        if res := dao.GormDB.Where("user_id = ? or contact_id = ?", user.Uuid, user.Uuid).Find(&contactList); res.Error != nil {
            if errors.Is(res.Error, gorm.ErrRecordNotFound) {
                zlog.Info(res.Error.Error())
            } else {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
        for _, contact := range contactList {
            var deletedAt gorm.DeletedAt
            deletedAt.Time = time.Now()
            deletedAt.Valid = true
            contact.DeletedAt = deletedAt
            if res := dao.GormDB.Save(&contact); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }

        // 删除申请记录
        var applyList []model.ContactApply
        if res := dao.GormDB.Where("user_id = ? or contact_id = ?", user.Uuid, user.Uuid).Find(&applyList); res.Error != nil {
            if errors.Is(res.Error, gorm.ErrRecordNotFound) {
                zlog.Info(res.Error.Error())
            } else {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
        for _, apply := range applyList {
            var deletedAt gorm.DeletedAt
            deletedAt.Time = time.Now()
            deletedAt.Valid = true
            apply.DeletedAt = deletedAt
            if res := dao.GormDB.Save(&apply); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }

    }
    return "删除用户成功", 0
}
```

删除用户，需要将每个用户在UserInfo表、UserContact表、Session表、ContactApply表中的记录删掉。

该api接口用于管理员删除所选用户时。

#### /user/setAdmin

**设置管理员**

<font style="background-color:#FBDE28;">controller</font>

```go
// SetAdmin 设置管理员
func SetAdmin(c *gin.Context) {
    var req request.AbleUsersRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.UserInfoService.SetAdmin(req.UuidList, req.IsAdmin)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// SetAdmin 设置管理员
func (u *userInfoService) SetAdmin(uuidList []string, isAdmin int8) (string, int) {
    var users []model.UserInfo
    if res := dao.GormDB.Where("uuid = (?)", uuidList).Find(&users); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, -1
    }
    for _, user := range users {
        user.IsAdmin = isAdmin
        if res := dao.GormDB.Save(&user); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
    }
    return "设置管理员成功", 0
}
```

设置管理员，从UserInfo表中获取对应的用户列表，并根据是否设置为管理员来修改表项。

该api接口用于管理员设置其他用户是否为管理员。

#### /group/getGroupInfoList

**获取群聊列表**

<font style="background-color:#FBDE28;">controller</font>

```go
// GetGroupInfoList 获取群聊列表 - 管理员
func GetGroupInfoList(c *gin.Context) {
    message, groupList, ret := gorm.GroupInfoService.GetGroupInfoList()
    JsonBack(c, message, ret, groupList)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// GetGroupInfoList 获取群聊列表 - 管理员
// 管理员少，而且如果用户更改了，那么管理员会一直频繁删除redis，更新redis，比较麻烦，所以管理员暂时不使用redis缓存
func (g *groupInfoService) GetGroupInfoList() (string, []respond.GetGroupListRespond, int) {
    var groupList []model.GroupInfo
    if res := dao.GormDB.Unscoped().Find(&groupList); res.Error != nil {
        zlog.Error(res.Error.Error())
        return constants.SYSTEM_ERROR, nil, -1
    }
    var rsp []respond.GetGroupListRespond
    for _, group := range groupList {
        rp := respond.GetGroupListRespond{
            Uuid:    group.Uuid,
            Name:    group.Name,
            OwnerId: group.OwnerId,
            Status:  group.Status,
        }
        if group.DeletedAt.Valid {
            rp.IsDeleted = true
        } else {
            rp.IsDeleted = false
        }
        rsp = append(rsp, rp)
    }
    return "获取成功", rsp, 0
}
```

获取群组列表，先从GroupInfo表中获取所有的群组，这里使用.Unscoped屏蔽软删除。最后打包好返回给前端。

该api接口用于管理员在获取所有的群组列表时。

#### /group/deleteGroups

**删除列表中的群聊**

<font style="background-color:#FBDE28;">controller</font>

```go
// DeleteGroups 删除列表中群聊 - 管理员
func DeleteGroups(c *gin.Context) {
    var req request.DeleteGroupsRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.DeleteGroups(req.UuidList)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// DeleteGroups 删除列表中群聊 - 管理员
func (g *groupInfoService) DeleteGroups(uuidList []string) (string, int) {
    for _, uuid := range uuidList {
        var deletedAt gorm.DeletedAt
        deletedAt.Time = time.Now()
        deletedAt.Valid = true
        if res := dao.GormDB.Model(&model.GroupInfo{}).Where("uuid = ?", uuid).Update("deleted_at", deletedAt); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        // 删除会话
        var sessionList []model.Session
        if res := dao.GormDB.Model(&model.Session{}).Where("receive_id = ?", uuid).Find(&sessionList); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        for _, session := range sessionList {
            if res := dao.GormDB.Model(&session).Update("deleted_at", deletedAt); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
        // 删除联系人
        var userContactList []model.UserContact
        if res := dao.GormDB.Model(&model.UserContact{}).Where("contact_id = ?", uuid).Find(&userContactList); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }

        for _, userContact := range userContactList {
            if res := dao.GormDB.Model(&userContact).Update("deleted_at", deletedAt); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }

        var contactApplys []model.ContactApply
        if res := dao.GormDB.Model(&contactApplys).Where("contact_id = ?", uuid).Find(&contactApplys); res.Error != nil {
            if res.Error != gorm.ErrRecordNotFound {
                zlog.Info(res.Error.Error())
                return "无响应的申请记录需要删除", 0
            }
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        for _, contactApply := range contactApplys {
            if res := dao.GormDB.Model(&contactApply).Update("deleted_at", deletedAt); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
        }
    }
    return "解散/删除群聊成功", 0
}
```

删除解散群聊，按照uuidList中的每个群组，将GroupInfo表中对应的群信息记录软删除掉，将Session表中对应的会话记录、与该群组相关的Contact记录，以及申请加群的记录全部删掉。

该api接口用于管理员在管理群组，解散、删除群组时。

#### /group/setGroupsStatus

**设置群聊是否启用**

<font style="background-color:#FBDE28;">controller</font>

```go
// SetGroupsStatus 设置群聊是否启用
func SetGroupsStatus(c *gin.Context) {
    var req request.SetGroupsStatusRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := gorm.GroupInfoService.SetGroupsStatus(req.UuidList, req.Status)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// SetGroupsStatus 设置群聊是否启用
func (g *groupInfoService) SetGroupsStatus(uuidList []string, status int8) (string, int) {
    var deletedAt gorm.DeletedAt
    deletedAt.Time = time.Now()
    deletedAt.Valid = true
    for _, uuid := range uuidList {
        if res := dao.GormDB.Model(&model.GroupInfo{}).Where("uuid = ?", uuid).Update("status", status); res.Error != nil {
            zlog.Error(res.Error.Error())
            return constants.SYSTEM_ERROR, -1
        }
        if status == group_status_enum.DISABLE {
            var sessionList []model.Session
            if res := dao.GormDB.Model(&sessionList).Where("receive_id = ?", uuid).Find(&sessionList); res.Error != nil {
                zlog.Error(res.Error.Error())
                return constants.SYSTEM_ERROR, -1
            }
            for _, session := range sessionList {
                if res := dao.GormDB.Model(&session).Update("deleted_at", deletedAt); res.Error != nil {
                    zlog.Error(res.Error.Error())
                    return constants.SYSTEM_ERROR, -1
                }
            }
        }
    }
    return "设置成功", 0
}
```

设置群聊是否启用，根据uuidList中的uuid，从GroupInfo表中查询相关的群组，如果是禁用，除了更新状态之后，还需要删除对应的会话记录。

该api接口用于管理员在管理群是否启用时。

###

### 文件上传下载

#### /message/uploadAvatar

**上传头像**

<font style="background-color:#FBDE28;">controller</font>

```go
// UploadAvatar 上传头像
func UploadAvatar(c *gin.Context) {
    message, ret := gorm.MessageService.UploadAvatar(c)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// UploadAvatar 上传头像
func (m *messageService) UploadAvatar(c *gin.Context) (string, int) {
    if err := c.Request.ParseMultipartForm(constants.FILE_MAX_SIZE); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }
    mForm := c.Request.MultipartForm
    for key, _ := range mForm.File {
        file, fileHeader, err := c.Request.FormFile(key)
        if err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        defer file.Close()
        zlog.Info(fmt.Sprintf("文件名：%s，文件大小：%d", fileHeader.Filename, fileHeader.Size))
        // 原来Filename应该是213451545.xxx，将Filename修改为avatar_ownerId.xxx
        ext := filepath.Ext(fileHeader.Filename)
        zlog.Info(ext)
        localFileName := config.GetConfig().StaticAvatarPath + "/" + fileHeader.Filename
        out, err := os.Create(localFileName)
        if err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        defer out.Close()
        if _, err := io.Copy(out, file); err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        zlog.Info("完成文件上传")
    }
    return "上传成功", 0
}

// ParseMultipartForm parses a request body as multipart/form-data.
// The whole request body is parsed and up to a total of maxMemory bytes of
// its file parts are stored in memory, with the remainder stored on
// disk in temporary files.
// ParseMultipartForm calls ParseForm if necessary.
// If ParseForm returns an error, ParseMultipartForm returns it but also
// continues parsing the request body.
// After one call to ParseMultipartForm, subsequent calls have no effect.
func (r *Request) ParseMultipartForm(maxMemory int64) error
```

在POST请求中，有一个enctype属性，规定了form表单在发送到服务器时的编码方式，它有3种值：

1. **application/x-www-form-urlencoded**：默认的编码方式。但是在用文本的传输和MP3等大型文件的时候，使用这种编码就显得 效率低下。
2. **multipart/form-data**：指定传输数据为二进制类型，比如图片、mp3、文件。
3. **text/plain**：纯文体的传输。空格转换为 “+” 加号，但不对特殊字符编码。

那么在传输文件时，post请求会按照Content-Type: multipart/form-data的请求内容格式来请求数据。

而gin框架中提供了ParseMultipartForm函数来解析请求体中的multipart/form-data数据。它规定最多可以存储maxMemory大小的数据到内存中，剩下的会存储到磁盘中。

在ParseMultipartForm函数中，如果成功解析，会将内容存储到对应的MultipartForm结构体中，大家感兴趣的可以去阅读一下ParseMultipartForm的源码。

```go
// MultipartForm is the parsed multipart form, including file uploads.
// This field is only available after ParseMultipartForm is called.
// The HTTP client ignores MultipartForm and uses Body instead.
MultipartForm *multipart.Form

// multipart.Form
// Form is a parsed multipart form.
// Its File parts are stored either in memory or on disk,
// and are accessible via the *FileHeader's Open method.
// Its Value parts are stored as strings.
// Both are keyed by field name.
type Form struct {
	Value map[string][]string
	File  map[string][]*FileHeader
}

// A FileHeader describes a file part of a multipart request.
type FileHeader struct {
	Filename string
	Header   textproto.MIMEHeader
	Size     int64

	content   []byte
	tmpfile   string
	tmpoff    int64
	tmpshared bool
}

// FormFile returns the first file for the provided form key.
// FormFile calls ParseMultipartForm and ParseForm if necessary.
func (r *Request) FormFile(key string) (multipart.File, *multipart.FileHeader, error)

// File is an interface to access the file part of a multipart message.
// Its contents may be either stored in memory or on disk.
// If stored on disk, the File's underlying concrete type will be an *os.File.
type File interface {
	io.Reader
	io.ReaderAt
	io.Seeker
	io.Closer
}
```

在MultipartForm结构体中，有一个multipart.Form的指针，Form中有两个字段，一个Value，一个是File字段。其中File字段是一个map。每个key对应了一个请求文件头。这个请求文件头里比较重要的几个值是Filename文件名，Size文件大小，content文件头内容。我们拿到这些内容就可以清楚文件的基本信息了。

之后将需要文件的key传入FormFile，能够成功读取到对应的File和FileHeader。File提供了几个文件流用于文件的读取。

当前端传来了multipart/form-data数据时，我们先把post的中该数据解析到MultipartForm结构体中，通过文件头的key读取对应的文件流，文件大小，文件名等基本信息。还需要规范文件名。项目中保留了文件后缀，把原文件名规定成avatar\_加上ownerId（用户id）来区分头像。之后在指定静态文件路径创造空文件，并将File中的内容拷贝到空文件中。至此，文件上传的后端部分就完成了。最后需要提醒前端“文件上传完成”。

#### /message/uploadFile

**上传文件**

<font style="background-color:#FBDE28;">controller</font>

```go
// UploadFile 上传头像
func UploadFile(c *gin.Context) {
    message, ret := gorm.MessageService.UploadFile(c)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// UploadFile 上传文件
func (m *messageService) UploadFile(c *gin.Context) (string, int) {
    if err := c.Request.ParseMultipartForm(constants.FILE_MAX_SIZE); err != nil {
        zlog.Error(err.Error())
        return constants.SYSTEM_ERROR, -1
    }
    mForm := c.Request.MultipartForm
    for key, _ := range mForm.File {
        file, fileHeader, err := c.Request.FormFile(key)
        if err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        defer file.Close()
        zlog.Info(fmt.Sprintf("文件名：%s，文件大小：%d", fileHeader.Filename, fileHeader.Size))
        // 原来Filename应该是213451545.xxx，将Filename修改为avatar_ownerId.xxx
        ext := filepath.Ext(fileHeader.Filename)
        zlog.Info(ext)
        localFileName := config.GetConfig().StaticFilePath + "/" + fileHeader.Filename
        out, err := os.Create(localFileName)
        if err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        defer out.Close()
        if _, err := io.Copy(out, file); err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        zlog.Info("完成文件上传")
    }
    return "上传成功", 0
}
```

上传文件和上传头像的方式是一样的，只是上传头像只包括图片类型的文件比如jpg，png，jpeg等。

在后端存储文件时是分类存的。文件上传是存在/static/files/中，而头像上传是存在/static/avatars/中。

```go
func init() {
    GE = gin.Default()
    GE.Static("/static/avatars", config.GetConfig().StaticAvatarPath)
    GE.Static("/static/files", config.GetConfig().StaticFilePath)
}
```

/static是静态文件目录，方便前端直接通过网址链接访问服务器上的静态文件。但是需要提前给gin engine设置到静态文件路径。

### 聊天功能

#### websocket

##### /wss

**ws登录**

<font style="background-color:#FBDE28;">controller</font>

```go
// WsLogin wss登录 Get
func WsLogin(c *gin.Context) {
    clientId := c.Query("client_id")
    if clientId == "" {
        zlog.Error("clientId获取失败")
        c.JSON(http.StatusOK, gin.H{
            "code":    400,
            "message": "clientId获取失败",
        })
        return
    }
    chat.NewClientInit(c, clientId)
}
```

这是后端服务器api中唯一一个get请求的api。由于没有post请求的表单，无法从req结构体中请求数据。所以通过http参数进行数据传递。这里完整的前端调用链接应该是http://ip:port/wss?client\_id=xxxx。gin框架通过Query能够获取到http参数的value。这里之所以不是500是因为如果获取失败的话，应该是业务问题，即用户可能输入的client\_id有误或者没有输入client\_id，所以返回400。

<font style="background-color:#FBDE28;">service+dao</font>

```go
// client.go
var upgrader = websocket.Upgrader{
	ReadBufferSize:  2048,
	WriteBufferSize: 2048,
	// 检查连接的Origin头
	CheckOrigin: func(r *http.Request) bool {
		return true
	},
}


// NewClientInit 当接受到前端有登录消息时，会调用该函数
func NewClientInit(c *gin.Context, clientId string) {
    conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
    if err != nil {
        zlog.Error(err.Error())
    }
    // kafka, client初始化等操作...
    zlog.Info("ws连接成功")
}
```

这里通过websocket.Upgrader将http连接升级为websocket连接。

1. **<font style="background-color:rgb(253, 253, 254);">ReadBufferSize</font>**<font style="background-color:rgb(253, 253, 254);"> 和 </font>**<font style="background-color:rgb(253, 253, 254);">WriteBufferSize</font>**<font style="background-color:rgb(253, 253, 254);">：这两个字段分别设置了读取和写入缓冲区的大小，这里都被设置为2048字节。缓冲区的大小可以根据实际需求调整，较大的缓冲区可以减少内存分配的次数，但也可能增加延迟和内存占用。</font>
2. **<font style="background-color:rgb(253, 253, 254);">CheckOrigin</font>**<font style="background-color:rgb(253, 253, 254);">：用于检查WebSocket连接的来源（Origin）。在Web安全模型中，Origin是一个标识请求来源的机制，主要用于跨源资源共享（CORS）和WebSocket连接的安全性检查。这里提供的</font><code><font style="background-color:rgb(253, 253, 254);">CheckOrigin</font></code><font style="background-color:rgb(253, 253, 254);">函数简单地返回</font><code><font style="background-color:rgb(253, 253, 254);">true</font></code><font style="background-color:rgb(253, 253, 254);">，它接受所有来源的连接请求。在实际应用中，为了提高安全性，应该根据应用的需求实现更严格的检查逻辑，只允许来自特定域的请求。</font>

<font style="background-color:rgb(253, 253, 254);">通过调用这个api，则成功将前端和服务器的ws连接建立成功了。</font>

该api接口用于当用户登录时成功输入密码跳转到chat界面时，会建立起ws连接。

##### /user/wsLogout

**ws登出**

<font style="background-color:#FBDE28;">controller</font>

```go
// WsLogout wss登出
func WsLogout(c *gin.Context) {
    var req request.WsLogoutRequest
    if err := c.BindJSON(&req); err != nil {
        zlog.Error(err.Error())
        c.JSON(http.StatusOK, gin.H{
            "code":    500,
            "message": constants.SYSTEM_ERROR,
        })
        return
    }
    message, ret := chat.ClientLogout(req.OwnerId)
    JsonBack(c, message, ret, nil)
}
```

<font style="background-color:#FBDE28;">service+dao</font>

```go
// ClientLogout 当接受到前端有登出消息时，会调用该函数
func ClientLogout(clientId string) (string, int) {
    client := ChatServer.Clients[clientId]
    if client != nil {
        if err := client.Conn.Close(); err != nil {
            zlog.Error(err.Error())
            return constants.SYSTEM_ERROR, -1
        }
        // channel关闭等操作
    }
    return "退出成功", 0
}
```

当前端登出时，会将登出的用户id传回后端，后端需要同时关闭建立好的ws连接。

该api接口用于当用户点击了退出登录时。

#### client

如果对client和server不太清楚的话可以先看这两张图，前端是用户端，也就是用户在浏览器上看到的界面。client是后端中创建出来与前端一一对应，进行websocket连接通信的通信单元。而server主要作为所有message的中转服务器，通话的信令服务器。

![1738582404859-714a001b-10f4-4d8f-9bd0-5407122fba42.png](https://cdn.davidingplus.cn/images/2026/09/11/1738582404859-714a001b-10f4-4d8f-9bd0-5407122fba42-054298.png)

接下来先聊聊client。

```go
type Client struct {
    Conn     *websocket.Conn
    Uuid     string
    SendTo   chan []byte       // 给server端
    SendBack chan *MessageBack // 给前端
}
```

先看看client的结构体，首先是websocket连接指针，当前端跟client一一对应，通过websocket传输message。其次是Uuid，即当前前端用户的用户id。然后是SendTo通道，该通道作为消息的缓冲通道，在Server的转发通道满的时候，会先存放到SendTo缓冲通道中，等到检查到Server的转发通道不满时，将SendTo通道中的message存入到转发通道中。最后是SendBack通道，这是Server将message通过SendBack发送给client。

另外需要给client开2个go协程，1个Read协程，1个Write协程。

```go
// 读取websocket消息并发送给send通道
func (c *Client) Read() {
    zlog.Info("ws read goroutine start")
    for {
        _, jsonMessage, err := c.Conn.ReadMessage() // 阻塞状态
        if err != nil {
            zlog.Error(err.Error())
            return // 直接断开websocket
        } else {
            var message = request.ChatMessageRequest{}
            if err := json.Unmarshal(jsonMessage, &message); err != nil {
                zlog.Error(err.Error())
            }
            log.Println("接受到消息为: ", jsonMessage)
            if messageMode == "channel" {
                // 如果server的转发channel没满，先把sendto中的给transmit
                for len(ChatServer.Transmit) < constants.CHANNEL_SIZE && len(c.SendTo) > 0 {
                    sendToMessage := <-c.SendTo
                    ChatServer.SendMessageToTransmit(sendToMessage)
                }
                // 如果server没满，sendto空了，直接给server的transmit
                if len(ChatServer.Transmit) < constants.CHANNEL_SIZE {
                    ChatServer.SendMessageToTransmit(jsonMessage)
                } else if len(c.SendTo) < constants.CHANNEL_SIZE {
                    // 如果server满了，直接塞sendto
                    c.SendTo <- jsonMessage
                } else {
                    // 否则考虑加宽channel size，或者使用kafka
                    if err := c.Conn.WriteMessage(websocket.TextMessage, []byte("由于目前同一时间过多用户发送消息，消息发送失败，请稍后重试")); err != nil {
                        zlog.Error(err.Error())
                    }
                }
            }
        }
    }
}
```

前端通过websocket把用户所要发送的message读取出来，通过SendTo通道转发给Server。这里有个先后判定逻辑。首先是判断Transmit转发通道是否已经满了，如果没有满，先把SendTo通道的消息，塞给Transmit通道，直到Transmit通道满了。如果SendTo通道中本来没有消息，则在一开始直接塞给Transmit通道；如果Transmit通道是满的，则先塞给SendTo通道，等到后面Transmit通道不满了再从SendTo通道取出来；如果SendTo通道也满了，则说明这一时刻消息发送太多了，服务器传输不过来了，此时会通过client向前端发送一条“由于目前同一时间过多用户发送消息，消息发送失败，请稍后重试”的提示消息。

```go
// 从send通道读取消息发送给websocket
func (c *Client) Write() {
    zlog.Info("ws write goroutine start")
    for messageBack := range c.SendBack { // 阻塞状态
        // 通过 WebSocket 发送消息
        err := c.Conn.WriteMessage(websocket.TextMessage, messageBack.Message)
        if err != nil {
            zlog.Error(err.Error())
            return // 直接断开websocket
        }
        // log.Println("已发送消息：", messageBack.Message)
        // 说明顺利发送，修改状态为已发送
        if res := dao.GormDB.Model(&model.Message{}).Where("uuid = ?", messageBack.Uuid).Update("status", message_status_enum.Sent); res.Error != nil {
            zlog.Error(res.Error.Error())
        }
    }
}
```

当从SendBack通道接收到message后，直接通过websocket发送回前端，并且在发送之后，将Message表中对应message状态修改为已发送。

```go
// NewClientInit 当接受到前端有登录消息时，会调用该函数
func NewClientInit(c *gin.Context, clientId string) {
    conn, err := upgrader.Upgrade(c.Writer, c.Request, nil)
    if err != nil {
        zlog.Error(err.Error())
    }
    client := &Client{
        Conn:     conn,
        Uuid:     clientId,
        SendTo:   make(chan []byte, constants.CHANNEL_SIZE),
        SendBack: make(chan *MessageBack, constants.CHANNEL_SIZE),
    }

    // 把client发送给server存储起来
    
    go client.Read()
    go client.Write()
    zlog.Info("ws连接成功")
}
```

在websocket创建时，同时创建client，并启动client的read和write写成。

#### server

接下来看看server。

```go
type Server struct {
    Clients  map[string]*Client
    mutex    *sync.Mutex
    Transmit chan []byte  // 转发通道
    Login    chan *Client // 登录通道
    Logout   chan *Client // 退出登录通道
}
```

Server结构体中，有一个装Client的map。key是Client对应的用户id，value是指向Client的指针。通过互斥锁来操作Server。此外，Server还有3个通道，转发通道，登录通道，退出登录通道。

```go
select {
    case client := <-s.Login:
    {
        s.mutex.Lock()
        s.Clients[client.Uuid] = client
        s.mutex.Unlock()
        zlog.Debug(fmt.Sprintf("欢迎来到kama聊天服务器，亲爱的用户%s\n", client.Uuid))
        err := client.Conn.WriteMessage(websocket.TextMessage, []byte("欢迎来到kama聊天服务器"))
        if err != nil {
            zlog.Error(err.Error())
        }
    }

    case client := <-s.Logout:
    {
        s.mutex.Lock()
        delete(s.Clients, client.Uuid)
        s.mutex.Unlock()
        zlog.Info(fmt.Sprintf("用户%s退出登录\n", client.Uuid))
        if err := client.Conn.WriteMessage(websocket.TextMessage, []byte("已退出登录")); err != nil {
            zlog.Error(err.Error())
        }
    }
}
```

其中登录通道和退出登录通道，传输该Client，当登录通道接收到该client时，会向map中插入该client，至此，Server中就有该client了。之后会让client通过websocket向前端发送一条“欢迎来到kama聊天服务器”的消息；如果退出登录通道接收到该client时，会从map中删除该client，并让client通过websocket向前端发送一条“已退出登录”的消息。

```go
case data := <-s.Transmit:
{
    // 执行message转发流程
}
```

当转发通道，接收到消息时，就会执行相应的message转发流程。下面会详细介绍message的转发流程。

#### message转发流程

#####

![1738582404859-714a001b-10f4-4d8f-9bd0-5407122fba42.png](https://cdn.davidingplus.cn/images/2026/09/11/1738582404859-714a001b-10f4-4d8f-9bd0-5407122fba42-054298.png)

这是普通的使用channel转发message的转发流程。

前端通过建立起的websocket连接，向后端对应的client传输要发送的message。后端的client接收到之后，会向server的转发通道转发message。server通过select不断监听通道，当转发通道有message发送过来，server会开始处理message，分析message的类型，以及接收联系人。

```go
// server.go
var chatMessageReq request.ChatMessageRequest
if err := json.Unmarshal(data, &chatMessageReq); err != nil {
    zlog.Error(err.Error())
}
// log.Println("原消息为：", data, "反序列化后为：", chatMessageReq)
if chatMessageReq.Type == message_type_enum.Text {
    
    // 保存message到Message表中
    
    if message.ReceiveId[0] == 'U' { // 发送给User

        // 打包序列化消息
        
        var messageBack = &MessageBack{
            Message: jsonMessage,
            Uuid:    message.Uuid,
        }
        s.mutex.Lock()
        if receiveClient, ok := s.Clients[message.ReceiveId]; ok {
            receiveClient.SendBack <- messageBack // 向client.Send发送
        }
        sendClient := s.Clients[message.SendId]
        sendClient.SendBack <- messageBack
        s.mutex.Unlock()
        
    } else if message.ReceiveId[0] == 'G' { // 发送给Group

        // 打包序列化消息
        
        var messageBack = &MessageBack{
          Message: jsonMessage,
          Uuid:    message.Uuid,
        }
        var group model.GroupInfo
        if res := dao.GormDB.Where("uuid = ?", message.ReceiveId).First(&group); res.Error != nil {
          zlog.Error(res.Error.Error())
        }
        var members []string
        if err := json.Unmarshal(group.Members, &members); err != nil {
          zlog.Error(err.Error())
        }
        s.mutex.Lock()
        for _, member := range members {
          if member != message.SendId {
             if receiveClient, ok := s.Clients[member]; ok {
                receiveClient.SendBack <- messageBack
             }
          } else {
             sendClient := s.Clients[message.SendId]
             sendClient.SendBack <- messageBack
          }
        }
        s.mutex.Unlock()
    }
} else if chatMessageReq.Type == message_type_enum.File {

    // 保存message到Message表中
    
    if message.ReceiveId[0] == 'U' { // 发送给User

        // 打包序列化消息
        
        
        var messageBack = &MessageBack{
          Message: jsonMessage,
          Uuid:    message.Uuid,
        }
        s.mutex.Lock()
        if receiveClient, ok := s.Clients[message.ReceiveId]; ok {
          //messageBack.Message = jsonMessage
          //messageBack.Uuid = message.Uuid
          receiveClient.SendBack <- messageBack // 向client.Send发送
        }
        sendClient := s.Clients[message.SendId]
        sendClient.SendBack <- messageBack
        s.mutex.Unlock()
    } else {
        
        // 打包序列化消息
        
        var messageBack = &MessageBack{
          Message: jsonMessage,
          Uuid:    message.Uuid,
        }
        var group model.GroupInfo
        if res := dao.GormDB.Where("uuid = ?", message.ReceiveId).First(&group); res.Error != nil {
          zlog.Error(res.Error.Error())
        }
        var members []string
        if err := json.Unmarshal(group.Members, &members); err != nil {
          zlog.Error(err.Error())
        }
        s.mutex.Lock()
        for _, member := range members {
          if member != message.SendId {
             if receiveClient, ok := s.Clients[member]; ok {
                receiveClient.SendBack <- messageBack
             }
          } else {
             sendClient := s.Clients[message.SendId]
             sendClient.SendBack <- messageBack
          }
        }
        s.mutex.Unlock()
    }
} else if chatMessageReq.Type == message_type_enum.AudioOrVideo {

    // 保存message到Message表中 

    if chatMessageReq.ReceiveId[0] == 'U' { // 发送给User

        // 打包序列化消息
        
        var messageBack = &MessageBack{
          Message: jsonMessage,
          Uuid:    message.Uuid,
        }
        s.mutex.Lock()
        if receiveClient, ok := s.Clients[message.ReceiveId]; ok {
          receiveClient.SendBack <- messageBack // 向client.Send发送
        }
        s.mutex.Unlock()
    }
}
```

首先所有的message转发到server转发通道时，会将对应的message存储到后台Message表中。

如果message是文本类型，若发送给用户，则需要向对应的client的SendBack通道发送message，并且还有向自己的client也发回这条message。这是因为我们发送消息的同时，对方要接收到该消息并在前端更新聊天界面，自己的聊天界面同样也需要更新聊天界面，所以也要向自己的client发送这条消息，做一个消息回显；

若发送给群聊，就需要通过receiveId到GroupInfo表中查询对应的群组，获取他的群成员列表。遍历群成员列表，对每一个群成员列表都上面对单个用户相同的操作，即向对应的client发送消息，最后也需要向自己的client发送消息。

如果message是文件类型，除了存储message时需要使用到不同的字段，以及在打包序列化消息时消息字段也不同之外，其他的流程都和文本类型是一样的。

如果message是通话类型，由于message消息涉及类似于http握手，即自己这端发送start\_call消息，对端接收到之后发送receive\_call消息。所以只会向对端client发送start\_call消息，就不再需要向己端发送start\_call消息（不再是为了回显），否则就会报错（后面讲到音视频通话功能的时候会再详细说）

#### 离线消息如何处理？

```go
// 将https://127.0.0.1:8000/static/xxx 转为 /static/xxx
func normalizePath(path string) string {
	// 查找 "/static/" 的位置
	if path == "https://cube.elemecdn.com/0/88/03b0d39583f48206768a7534e55bcpng.png" {
		return path
	}
	staticIndex := strings.Index(path, "/static/")
	if staticIndex < 0 {
		log.Println(path)
		zlog.Error("路径不合法")
	}
	// 返回从 "/static/" 开始的部分
	return path[staticIndex:]
}

// 存message
message := model.Message{
    Uuid:       fmt.Sprintf("M%s", random.GetNowAndLenRandomString(11)),
    SessionId:  chatMessageReq.SessionId,
    Type:       chatMessageReq.Type,
    Content:    chatMessageReq.Content,
    Url:        "",
    SendId:     chatMessageReq.SendId,
    SendName:   chatMessageReq.SendName,
    SendAvatar: chatMessageReq.SendAvatar,
    ReceiveId:  chatMessageReq.ReceiveId,
    FileSize:   "0B",
    FileType:   "",
    FileName:   "",
    Status:     message_status_enum.Unsent,
    CreatedAt:  time.Now(),
    AVdata:     "",
}
// 对SendAvatar去除前面/static之前的所有内容，防止ip前缀引入
message.SendAvatar = normalizePath(message.SendAvatar)
if res := dao.GormDB.Create(&message); res.Error != nil {
    zlog.Error(res.Error.Error())
}
```

对于离线消息，在Transmit转发通道接收到message时，会事先存储到Message表中。在之后用户登陆的时候，前端会调用/message/getMessageList的api，这时候会把整个聊天记录都返回给前端。而对于在线用户，转发时会有如下判断：

```go
if receiveClient, ok := s.Clients[message.ReceiveId]; ok {
    receiveClient.SendBack <- messageBack // 向client.Send发送
}
```

如果此时Server中的Clients map中有该用户，说明该用户此时是在线的，这是不需要通过前端调用/message/getMessageList来获取全部的聊天记录。只需要通过SendBack通道把message转发给Client，Client通过websocket传输给前端。前端拿到之后会将message显示出来，这样节省了一次调用后台数据库的操作。如果Server中的Clients map没有该用户，说明该用户不在线，就不会向该用户发送message，而是保存到后台。

#### kafka

如果后端服务器同一时间发送的消息太多太多了，多个client用channel发送给server的消息太多太多了，导致server的转发通道满了，client的SendTo通道也满了，而且此时还一直在发送消息。

这是一种情况就是会出现“由于目前同一时间过多用户发送消息，消息发送失败，请稍后重试”这样的消息返回给前端。但是这样的结果就是想发送的消息因为服务器太繁忙没法发送。

另一种情况就是在client和server之间加入消息队列，比如kafka这样的流量削峰的消息队列，确保client能够在高峰期顺利地将message发送给server。

那么用kafka替换channel的后端服务器结构就出现了。

![1738593589556-3afa08d1-5b6a-451c-a5b0-92809c9f1859.png](https://cdn.davidingplus.cn/images/2026/09/11/1738593589556-3afa08d1-5b6a-451c-a5b0-92809c9f1859-371450.png)

##### client

相对使用SendTo通道的client，加入kafka的client只需要如下三个字段：

```go
type Client struct {
    Conn     *websocket.Conn
    Uuid     string
    SendBack chan *MessageBack // 给前端
}
```

相对于channel类型的client，还有如下的区别：

```go
// 读取websocket消息并发送给send通道
func (c *Client) Read() {
    zlog.Info("ws read goroutine start")
    for {
        // 阻塞有一定隐患，因为下面要处理缓冲的逻辑，但是可以先不做优化，问题不大
        _, jsonMessage, err := c.Conn.ReadMessage() // 阻塞状态
        if err != nil {
            zlog.Error(err.Error())
            return // 直接断开websocket
        } else {
            var message = request.ChatMessageRequest{}
            if err := json.Unmarshal(jsonMessage, &message); err != nil {
                zlog.Error(err.Error())
            }
            log.Println("接受到消息为: ", jsonMessage)
            if messageMode == "channel" {
                // 如果server的转发channel没满，先把sendto中的给transmit
                for len(ChatServer.Transmit) < constants.CHANNEL_SIZE && len(c.SendTo) > 0 {
                    sendToMessage := <-c.SendTo
                    ChatServer.SendMessageToTransmit(sendToMessage)
                }
                // 如果server没满，sendto空了，直接给server的transmit
                if len(ChatServer.Transmit) < constants.CHANNEL_SIZE {
                    ChatServer.SendMessageToTransmit(jsonMessage)
                } else if len(c.SendTo) < constants.CHANNEL_SIZE {
                    // 如果server满了，直接塞sendto
                    c.SendTo <- jsonMessage
                } else {
                    // 否则考虑加宽channel size，或者使用kafka
                    if err := c.Conn.WriteMessage(websocket.TextMessage, []byte("由于目前同一时间过多用户发送消息，消息发送失败，请稍后重试")); err != nil {
                        zlog.Error(err.Error())
                    }
                }
            } else {
                if err := myKafka.KafkaService.ChatWriter.WriteMessages(ctx, kafka.Message{
                    Key:   []byte(strconv.Itoa(config.GetConfig().KafkaConfig.Partition)),
                    Value: jsonMessage,
                }); err != nil {
                    zlog.Error(err.Error())
                }
                zlog.Info("已发送消息：" + string(jsonMessage))
            }
        }
    }
}
```

相较于channel，这里kafka拿到了message之后是直接通过chat主题的ChatWriter向kafka写message，按照分区设定key，value是序列化的message，即某区收到的message。相较于channel的逻辑更简单一点。

##### server

```go
func (k *KafkaServer) Start() {
    defer func() {
        if r := recover(); r != nil {
            zlog.Error(fmt.Sprintf("kafka server panic: %v", r))
        }
        close(k.Login)
        close(k.Logout)
    }()

    // read chat message
    go func() {
        defer func() {
            if r := recover(); r != nil {
                zlog.Error(fmt.Sprintf("kafka server panic: %v", r))
            }
        }()
        for {
            kafkaMessage, err := kafka.KafkaService.ChatReader.ReadMessage(ctx)
            if err != nil {
                zlog.Error(err.Error())
            }
            // message 存储和转发流程
        }
    }()

    // login, logout message
    for {
       select {
       case client := <-k.Login:
          {
             k.mutex.Lock()
             k.Clients[client.Uuid] = client
             k.mutex.Unlock()
             zlog.Debug(fmt.Sprintf("欢迎来到kama聊天服务器，亲爱的用户%s\n", client.Uuid))
             err := client.Conn.WriteMessage(websocket.TextMessage, []byte("欢迎来到kama聊天服务器"))
             if err != nil {
                zlog.Error(err.Error())
             }
          }

       case client := <-k.Logout:
          {
             k.mutex.Lock()
             delete(k.Clients, client.Uuid)
             k.mutex.Unlock()
             zlog.Info(fmt.Sprintf("用户%s退出登录\n", client.Uuid))
             if err := client.Conn.WriteMessage(websocket.TextMessage, []byte("已退出登录")); err != nil {
                zlog.Error(err.Error())
             }
          }
       }
    }
}
```

在server中，登录和登出由于触发的次数没这么多，所以没有使用kafka，依旧是channel进行message传输的。而聊天chat使用一个协程去监听kafka，作为消费者。当监听到chat主题有消息了，执行相应的message存储以及转发message流程。

下面是加上kafka的message转发流程。

当前端传给client相关的message后，client需要按照对应主题把message写到kafka。然后server从kafka上读取对应主题的message，并且判断message的类型以及接收联系人，将message再通过channel传到对应client上，client通过websocket传回前端。

server从kafka读取到消息后，对消息的操作其实是和channel版相同的，唯一不同就是消息传入是通过channel传入还是通过kafka传入。

### 音视频通话功能

音视频通话功能是通过<font style="color:rgb(64, 64, 64);">WebRTC</font>实现的。

<font style="color:rgb(64, 64, 64);">WebRTC（Web Real-Time Communication）是一种支持浏览器之间进行实时音视频通信的技术。建立P2P（Peer-to-Peer）连接的过程涉及多个步骤，包括信令、媒体协商和网络穿透等。</font>

![1738720815594-8a9e7557-e617-4bcb-910d-b9530ff422f6.png](https://cdn.davidingplus.cn/images/2026/09/11/1738720815594-8a9e7557-e617-4bcb-910d-b9530ff422f6-329210.png)

它的建立p2p通信的流程如上图所示。

1. **<font style="color:rgb(64, 64, 64);">初始化连接</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientA</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">和</font><font style="color:rgb(64, 64, 64);"> </font>**<font style="color:rgb(64, 64, 64);">ClientB</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">是两个需要建立连接的客户端。</font>
   * **<font style="color:rgb(64, 64, 64);">Signal</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">是信令服务器，用于在客户端之间传递消息。</font>
2. **<font style="color:rgb(64, 64, 64);">创建RTCPeerConnection</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * <font style="color:rgb(64, 64, 64);">每个客户端创建一个</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">RTCPeerConnection</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">对象，用于管理P2P连接。</font>
3. **<font style="color:rgb(64, 64, 64);">获取媒体流</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * <font style="color:rgb(64, 64, 64);">使用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">getUserMedia</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">获取本地媒体流（如摄像头和麦克风）。</font>
   * <font style="color:rgb(64, 64, 64);">使用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">addTrack</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">将媒体流添加到</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">RTCPeerConnection</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">中。</font>
4. **<font style="color:rgb(64, 64, 64);">创建Offer</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientA</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">调用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">createOffer</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">生成一个SDP（Session Description Protocol）offer，描述其媒体能力。</font>
   * **<font style="color:rgb(64, 64, 64);">ClientA</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">调用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">setLocalDescription</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">将生成的offer设置为本地描述。</font>
5. **<font style="color:rgb(64, 64, 64);">发送Offer</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientA</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">通过信令服务器将offer发送给</font><font style="color:rgb(64, 64, 64);"> </font>**<font style="color:rgb(64, 64, 64);">ClientB</font>**<font style="color:rgb(64, 64, 64);">。</font>
6. **<font style="color:rgb(64, 64, 64);">接收Offer</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientB</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">接收到offer后，调用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">setRemoteDescription</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">将其设置为远程描述。</font>
7. **<font style="color:rgb(64, 64, 64);">创建Answer</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientB</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">调用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">createAnswer</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">生成一个SDP answer，描述其媒体能力。</font>
   * **<font style="color:rgb(64, 64, 64);">ClientB</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">调用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">setLocalDescription</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">将生成的answer设置为本地描述。</font>
8. **<font style="color:rgb(64, 64, 64);">发送Answer</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientB</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">通过信令服务器将answer发送给</font><font style="color:rgb(64, 64, 64);"> </font>**<font style="color:rgb(64, 64, 64);">ClientA</font>**<font style="color:rgb(64, 64, 64);">。</font>
9. **<font style="color:rgb(64, 64, 64);">接收Answer</font>**<font style="color:rgb(64, 64, 64);">：</font>
   * **<font style="color:rgb(64, 64, 64);">ClientA</font>**<font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">接收到answer后，调用</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">setRemoteDescription</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">将其设置为远程描述。</font>
10. **<font style="color:rgb(64, 64, 64);">ICE候选交换</font>**<font style="color:rgb(64, 64, 64);">：</font>
    * <font style="color:rgb(64, 64, 64);">双方客户端在发现ICE（Interactive Connectivity Establishment）候选时，会触发</font><font style="color:rgb(64, 64, 64);"> </font><code><font style="color:rgb(64, 64, 64);">onIceCandidate</font></code><font style="color:rgb(64, 64, 64);"> </font><font style="color:rgb(64, 64, 64);">事件。</font>
    * <font style="color:rgb(64, 64, 64);">每个客户端通过信令服务器将ICE候选发送给对方。</font>
    * <font style="color:rgb(64, 64, 64);">接收到ICE候选后，调用 </font><code><font style="color:rgb(64, 64, 64);">addIceCandidate</font></code><font style="color:rgb(64, 64, 64);"> 将其添加到 </font><code><font style="color:rgb(64, 64, 64);">RTCPeerConnection</font></code><font style="color:rgb(64, 64, 64);"> 中。</font>
11. **<font style="color:rgb(64, 64, 64);">建立P2P连接</font>**<font style="color:rgb(64, 64, 64);">：</font>
    * <font style="color:rgb(64, 64, 64);">当双方客户端交换完ICE候选并成功建立连接后，P2P通道就建立起来了。</font>
    * <font style="color:rgb(64, 64, 64);">此时，双方可以通过这个通道进行实时音视频通信。</font>

对于后端服务器，在整个WebRTC通信过程中充当了信令服务器的角色。即将前端发送过来的包括offer sdp、answer sdp、以及ice候选等互换信息通过转发通道转发给对端用户。

```go
if chatMessageReq.Type == message_type_enum.AudioOrVideo {
    var avData request.AVData
    if err := json.Unmarshal([]byte(chatMessageReq.AVdata), &avData); err != nil {
        zlog.Error(err.Error())
    }
    //log.Println(avData)
    message := model.Message{
        Uuid:       fmt.Sprintf("M%s", random.GetNowAndLenRandomString(11)),
        SessionId:  chatMessageReq.SessionId,
        Type:       chatMessageReq.Type,
        Content:    "",
        Url:        "",
        SendId:     chatMessageReq.SendId,
        SendName:   chatMessageReq.SendName,
        SendAvatar: chatMessageReq.SendAvatar,
        ReceiveId:  chatMessageReq.ReceiveId,
        FileSize:   "",
        FileType:   "",
        FileName:   "",
        Status:     message_status_enum.Unsent,
        CreatedAt:  time.Now(),
        AVdata:     chatMessageReq.AVdata,
    }
    if avData.MessageId == "PROXY" && (avData.Type == "start_call" || avData.Type == "receive_call" || avData.Type == "reject_call") {
        // 存message
        // 对SendAvatar去除前面/static之前的所有内容，防止ip前缀引入
        message.SendAvatar = normalizePath(message.SendAvatar)
        if res := dao.GormDB.Create(&message); res.Error != nil {
            zlog.Error(res.Error.Error())
        }
    }

    if chatMessageReq.ReceiveId[0] == 'U' { // 发送给User
        // 如果能找到ReceiveId，说明在线，可以发送，否则存表后跳过
        // 因为在线的时候是通过websocket更新消息记录的，离线后通过存表，登录时只调用一次数据库操作
        // 切换chat对象后，前端的messageList也会改变，获取messageList从第二次就是从redis中获取
        messageRsp := respond.AVMessageRespond{
            SendId:     message.SendId,
            SendName:   message.SendName,
            SendAvatar: message.SendAvatar,
            ReceiveId:  message.ReceiveId,
            Type:       message.Type,
            Content:    message.Content,
            Url:        message.Url,
            FileSize:   message.FileSize,
            FileName:   message.FileName,
            FileType:   message.FileType,
            CreatedAt:  message.CreatedAt.Format("2006-01-02 15:04:05"),
            AVdata:     message.AVdata,
        }
        jsonMessage, err := json.Marshal(messageRsp)
        if err != nil {
            zlog.Error(err.Error())
        }
        // log.Println("返回的消息为：", messageRsp, "序列化后为：", jsonMessage)
        log.Println("返回的消息为：", messageRsp)
        var messageBack = &MessageBack{
            Message: jsonMessage,
            Uuid:    message.Uuid,
        }
        s.mutex.Lock()
        if receiveClient, ok := s.Clients[message.ReceiveId]; ok {
            //messageBack.Message = jsonMessage
            //messageBack.Uuid = message.Uuid
            receiveClient.SendBack <- messageBack // 向client.Send发送
        }
        // 通话这不能回显，发回去的话就会出现两个start_call。
        //sendClient := s.Clients[message.SendId]
        //sendClient.SendBack <- messageBack
        s.mutex.Unlock()
    }
}
```

当message是音视频通话类型的时候，也是先将message存储到Message表中。在存入之前，需要判断是否为PROXY类型（这里的类型是前端设定的，意思是该后端服务器作为中转message的代理服务器），如果是PROXY中的start\_call或者receive\_call。则需要存储到后台数据库中，如果是其他类型比如sdp，ice之类的，就不用，否则可能数据会存的比较多，每次通话就有几十条数据要存储。

当存储完了之后，就是转发，这里就跟之前说的，会转发给对端用户，但是自己就不需要再回显了，否则就有两个receive\_call。前端就会报错，而无法正常建立p2p连接开始通信。

以上就是后端部分的代码讲解，接下来可以看前端部分的代码逻辑讲解了。


> 更新: 2025-04-07 10:53:10  
> 原文: <https://www.yuque.com/chengxuyuancarl/zbfw4l/cgv4uddazccvfu20>