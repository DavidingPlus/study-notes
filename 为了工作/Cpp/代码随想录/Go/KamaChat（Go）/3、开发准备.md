# 3、开发准备

# KamaChat

【代码随想录知识星球】项目分享-基于go+vue实现的聊天室+仿微信项目

# 项目概述

1. 简介：KamaChat 是一个前后端分离的即时通讯项目，具备后台管理、单聊群聊、联系人管理、多种消息（文本 / 文件 / 视频）处理、离线消息处理以及音视频通话等功能，旨在打造类似微信的聊天体验。
2. 技术栈：
   * 前端：Vue3、Vue Router、Vuex、WebSocket、Element - UI 等。
   * 后端：Go、Gin、GORM、GoRedis、WebSocket、Kafka、WebRTC、Zap 日志库等。

# 功能特性

1. 即时通讯功能
   * 单聊与群聊：支持一对一私密聊天和群组聊天，消息实时推送。
   * 联系人管理：可添加、删除、拉黑联系人，处理好友申请等。
   * 消息类型：支持文本、文件、音视频等多种类型消息的发送与接收。
   * 离线消息处理：确保用户离线时消息不丢失，上线后可正常接收。
2. 音视频通话：基于 WebRTC 实现 1 对 1 音视频通话，包括发起、拒绝、接收、挂断通话等功能。
3. 后台管理：具备后台管理界面，靓号用户可进行人员管控等维护操作。
4. 安全与验证：登录注册采用 SMS 短信验证方式，并支持 SSL 加密，保障用户信息安全。（<font style="color:#1DC0C9;">这块当前可能不能个人认证了，可以利用chatgpt生成数据到user\_info表中,直接进行登录操作，todo : 更改代码为QQ邮箱验证码登录</font>)
5. 后台mysql数据库：使用 GORM 进行数据库操作，确保数据持久化存储。
6. 日志记录：使用 Zap 日志库记录系统运行日志，便于问题排查与性能监控。
7. 消息队列：使用 Kafka 处理消息队列，确保消息的高效传输与处理。
8. redis缓存：使用 GoRedis 进行缓存操作，提高系统性能。
9. WebSocket：使用 WebSocket 实现实时消息推送，保证消息的实时性。

# 服务器准备

建议购买一台云服务器，腾讯云或者阿里云，用来部署自己的项目 非常方便，无论是做本项目还是做其他项目，无论是自己练习，还是项目上线，都需要一台有公网ip的独立服务器。自己电脑安装虚拟机，不仅麻烦，经常除莫名其妙的问题。

* [阿里云活动期间服务器购买](https://www.aliyun.com/minisite/goods?taskCode=shareNew2205\&recordId=3641992\&userCode=roof0wob)

我购买的是阿里云服务器ECS。大家可以按照这个配置来购买，方便一键部署。

![1742834562804-4b1de98f-0118-469e-b44c-3fa5a677881e.png](./img/l6VphSInjxz39AXO/1742834562804-4b1de98f-0118-469e-b44c-3fa5a677881e-212406.png)

# 项目结构

## 项目架构图

![1744807788779-23c52d46-a3ce-48b8-8825-b4805cb859d4.png](./img/l6VphSInjxz39AXO/1744807788779-23c52d46-a3ce-48b8-8825-b4805cb859d4-359990.png)

## 项目目录结构

### 后端

```plain
kama-chat-server/
├── api/
│   └── v1/
│       └── chatroom_controller.go
│       └── controller.go
│       └── group_info_controller.go
│       └── message_controller.go
│       └── session_controller.go
│       └── user_contact_controller.go
│       └── user_info_controller.go
│       └── ws_controller.go
├── cmd/
│   └── kama-chat-server/
│       └── main.go
├── internal/
│   ├── config/
│   │   └── config.go
│   ├── dao/
│   │   └── gorm.go
│   ├── dto/
│   │   ├── request/
│   │   │   └── ......
│   │   └── respond/
│   │   │   └── ......
│   ├── https_server/
│   │   └── https_server.go
│   ├── model/
│   │   ├── contact_apply.go
│   │   ├── group_info.go
│   │   ├── message.go
│   │   ├── session.go
│   │   ├── user_contact.go
│   │   └── user_info.go
│   └── service/
│       ├── chat/
│       │   ├── client.go
│       │   ├── kafka_server.go
│       │   └── server.go
│       ├── gorm/
│       │   ├── chatroom_service.go
│       │   ├── group_info_service.go
│       │   ├── message_service.go
│       │   ├── session_service.go
│       │   ├── user_contact_service.go
│       │   └── user_info_service.go
│       ├── kafka/
│       │   └── kafka_service.go
│       ├── redis/
│       │   └── redis_service.go
│       └── sms/
│           ├── local/
│           │   └── user_info_service_local.go
│           └── auth_code_service.go
├── logs/
│   └── test.log
├── pkg/
│   ├── constants/
│   │   └── constants.go
│   ├── enum/
│   │   ├── contact/
│   │   ├── contact_apply/
│   │   ├── group_info/
│   │   ├── message/
│   │   ├── session/
│   │   └── user_info/
│   ├── ssl/
│   │   ├── xxx.pem
│   │   ├── xxx-key.pem
│   │   └── tls_handler.go
│   ├── util/
│   │   └── random/
│   │       └── random_int.go
│   └── zlog/
│       └── logger.go
├── configs/
│   ├── config.toml
│   └── config_local.toml
├── static/
│   ├── avatars/
│   │   └── ......
│   └── files/
│   │   └── ......
├── web/
│   └── (前端项目结构)
├── .gitignore
├── go.mod
├── go.sum
└── README.md

```

### 前端

```plain
web/chat-server/
├── src/
│   ├── assets/
│   │   ├── cert/
│   │   │   ├── xxx.pem
│   │   │   ├── xxx-key.pem
│   │   │   └── mkcert.exe
│   │   ├── css/
│   │   │   └── chat.css
│   │   ├── img/
│   │   │   └── chat_server_background.jpg
│   │   ├── js/
│   │   │   ├── random.js
│   │   │   └── valid.js
│   ├── components/
│   │   ├── ContactListModal.vue
│   │   ├── DeleteGroupModal.vue
│   │   ├── DeleteUserModal.vue
│   │   ├── DisableGroupModal.vue
│   │   ├── DisableUserModal.vue
│   │   ├── Modal.vue
│   │   ├── NavigationModal.vue
│   │   ├── SetAdminModal.vue
│   │   ├── SmallModal.vue
│   │   └── VideoModal.vue
│   ├── router/
│   │   └── index.js
│   ├── store/
│   │   └── index.js
│   ├── views/
│   │   ├── access/
│   │   │   ├── Login.vue
│   │   │   ├── Register.vue
│   │   │   └── SmsLogin.vue
│   │   ├── chat/
│   │   │   ├── contact/
│   │   │   │   ├── ContactChat.vue
│   │   │   │   └── ContactList.vue
│   │   │   ├── session/
│   │   │   │   └── SessionList.vue
│   │   │   └── user/
│   │   │       └── OwnInfo.vue
│   │   ├── manager/
│   │       └── Manager.vue
│   ├── App.vue
│   └── main.js
├── .gitignore
├── package.json
├── README.md
└── vue.config.js
```

# （更为推荐）安装与运行（一键配置Docker版）

由于原作者写的脚本可能会有些问题，[@焕、心](undefined/huanxin-katrm)将所需要安装的东西全部提前安装好，并配置好镜像到DockerHub上了，大家只需要有docker的环境，就可以一键拉取一个现成的镜像，创建容器进入后只需要更改ip地址等少量操作，就可以让服务运行起来了

## 0：注意点：

* 在我部署的docker环境中的服务，数据库本身含有两个账号，其中账号分别为13686666610与13600000000,密码都是123456。因为短信服务我看过，似乎是需要钱的，而且似乎现在没有了个人认证，所以就手动在数据库的user\_info表中插入了两行数据，当然大家可以尝试一下是否可行,我这边就没有尝试了，后续可能将其换成邮箱登录
* 我将原先的https版本换成了http版本，因为考虑到有录友说，https一直配置失败，但是换成http版本之后，webrtc部分由于需要https，所以我们这边要做特殊处理，具体可以参考下面的 标题\*\*（强烈建议看，可能遇到的问题中的第二点，跟着它配置即可）\*\*

## 1:下载Docker

可以i查看这个文章来下载对应的docker

<https://blog.csdn.net/jackeydengjun/article/details/147185455>

## 2：从远程拉取我配置好的镜像并创建容器

```cpp
# 将我提供的远程镜像拉取到本地
docker pull huanheart/gochat:v1
```

如果在这个过程中出现

Error response from daemon: Get "<https://registry-1.docker.io/v2/":> context deadline exceeded

可以在群里找焕、心

```cpp
# 生成该json
vim /etc/docker/daemon.json
```

将下面内容复制上去

```cpp
{
  "registry-mirrors": [
    "https://docker.registry.cyou",
    "https://docker-cf.registry.cyou",
    "https://dockercf.jsdelivr.fyi",
    "https://docker.jsdelivr.fyi",
    "https://dockertest.jsdelivr.fyi",
    "https://mirror2.aliyuncs.com",
    "https://dockerproxy.com",
    "https://mirror.baidubce.com",
    "https://docker.m.daocloud.io",
    "https://docker.nju.edu.cn",
    "https://docker.mirrors.sjtug.sjtu.edu.cn",
    "https://docker.mirrors.ustc.edu.cn",
    "https://mirror.iscas.ac.cn",
    "https://docker.rainbond.cc"
  ]
}

```

保存之后重新启动docker，并重新拉取该镜像，应该就可以了，

如果代理不可用了，大家可以自行网上搜索，或者在群里找[@焕、心](undefined/huanxin-katrm)

```cpp
sudo systemctl daemon-reexec
sudo systemctl restart docker
docker pull huanheart/gochat:v1

```

如果还是拉取失败:<font style="color:rgb(38, 38, 38);">请新增加这几个镜像源</font>

```go
"https://docker.1ms.run",
"https://dockerproxy.com",
"https://hub.rat.dev"
```

<font style="color:rgb(38, 38, 38);"></font>

此时镜像就已经拉取好了，现在需要创建容器了

![1750443126674-1c79c3cb-34cd-4ada-a21f-5fb646649d76.png](./img/l6VphSInjxz39AXO/1750443126674-1c79c3cb-34cd-4ada-a21f-5fb646649d76-740394.png)

创建容器,执行该命令

```cpp
docker run -d \
  --name gochat-container \
  -p 81:80 \
  -p 8001:8000 \
  -p 3307:3306 \
  -p 6380:6379 \
  -p 3479:3478/udp \
  -p 3479:3478/tcp \
  -p 444:443 \
  -p 3390:3389 \
  huanheart/gochat:v1 \
  tail -f /dev/null

```

## 3：进入Docker更改ip地址以及toml文件

输入docker ps查看正在运行中的容器

![1750443322443-59d57d2c-d374-47b5-b264-1d7bf1a63917.png](./img/l6VphSInjxz39AXO/1750443322443-59d57d2c-d374-47b5-b264-1d7bf1a63917-852958.png)

通过docker exec -it 882a3a70f2c0 bash进入容器（882a3a70f2c0需要对应你自己的名字）

接下来更改配置

```cpp
# 进入指定目录，找到index.js
cd /root/gochat/KamaChat/web/chat-server/src/store
# 编辑index.js 
vim index.js 

```

将192.168.15.129更换成你当前的ip地址（公网ip或者用本地虚拟机的话换成局域网地址（切记要用桥接模式若是虚拟机的话））

**注意：如果是wsl，那么ip地址改成127.0.0.1(若是windows环境下的docker，看第六点）**

**还有一点需要说明的是，有不少录友端口号也改了，还改成81了，强调一下，这里只需要改ip不用改端口！**

**我们一定要知道81端口和8001端口的区别**

**前者81是浏览器访问前端的地址，后者是前端转发给后端的地址，这里的url填写的是前端转发给后端的地址**

![1750426979695-b16814b0-6131-43b6-a513-f15dc0f0f930.png](./img/l6VphSInjxz39AXO/1750426979695-b16814b0-6131-43b6-a513-f15dc0f0f930-159115.png)

执行以下命令

```cpp
cd /root/gochat/KamaChat/web/chat-server
rm -f -r dist
rm -f -r /var/www/html/*
yarn build 
cp -r dist/*  /var/www/html/
    
```

## 4：启动项目

```cpp
cd /root/gochat/KamaChat/cmd/kama_chat_server
./main
```

./main的时候可能会遇到下面错误，此时说明mysql没有开启，可以执行

```cpp
service mysql start
service apache2 start 
service redis-server start
```

最后重新执行一次./main就可以了

```cpp
2025/06/20 18:18:55 /root/gochat/KamaChat/internal/dao/gorm.go:24
[error] failed to initialize database, got error dial unix /var/run/mysqld/mysqld.sock: connect: no such file or directory
{"level":"fatal","ts":"2025-06-20T18:18:55.016Z","msg":"dial unix /var/run/mysqld/mysqld.sock: connect: no such file or directory","func":"dao.init.0","file":"/root/gochat/KamaChat/internal/dao/gorm.go","line":26}
2025-06-20 18:18:55.016157054 +0000 UTC m=+0.037753414 write error: invalid argument
```

此时后端服务开起来了，我们只需在浏览器中访问前端即可

例如我的（注意，此时我的docker中是80端口，但是跟主机的81端口有一个映射关系）

防止服务器本身已经含有80端口服务运行中

**（重点）你还需要留意的是，如果你用的是云服务器ECS，那么你需要将81端口也给暴露出来**

```cpp
192.168.15.129:81
```

此时就成功进入了，用原本在数据库中存在的两个账号登录就可以了

![1750427467474-e0dc1d77-c2e0-4c85-ae35-ef7b1be02ad4.png](./img/l6VphSInjxz39AXO/1750427467474-e0dc1d77-c2e0-4c85-ae35-ef7b1be02ad4-576191.png)

## 5：修改一个小问题（刷新页面的时候，出现404问题）

针对有录友说：当我刷新页面的时候，出现404问题

原因：阿帕奇配置文件我忘记对它做操作了，固然会请求一个静态资源目录，但是本身阿帕奇路径下没有login，固然会出现404的问题，那么如何解决呢？请看代码块部分，此时按照如下流程走就可以了

由于这个很容易改，就不更改docker部分了

注意（这个是http，针对docker版本的，如果是https的话，看（强烈建议看部分）

![1751462717557-ca51849f-a747-4417-9f45-c8b2776d6117.png](./img/l6VphSInjxz39AXO/1751462717557-ca51849f-a747-4417-9f45-c8b2776d6117-422642.png)

```cpp
# 删除原先的阿帕奇配置文件
rm -f /etc/apache2/sites-enabled/000-default.conf
# 重新编辑并复制粘贴第二个代码块的内容
vim /etc/apache2/sites-enabled/000-default.conf
    
```

```cpp
<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    RewriteEngine On

    # 排除静态资源（js、css、img）不进行重写
    RewriteCond %{REQUEST_URI} !^/static/
    RewriteCond %{REQUEST_URI} !\.(js|css|png|jpg|jpeg|gif|ico|svg)$
    RewriteRule ^ /index.html [L]

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
```

## 6：windows下可以使用docker吗？&\&windows使用docker注意事项

windows下可以使用docker的，因为docker本身内部容器和主机环境是隔离的，所以这也是docker的方便之处

但是有几个点需要注意

![1750426979695-b16814b0-6131-43b6-a513-f15dc0f0f930.png](./img/l6VphSInjxz39AXO/1750426979695-b16814b0-6131-43b6-a513-f15dc0f0f930-353584.webp)

windows更改index.js的时候，需要将其更改成windows上以太网适配器 VMware Network Adapter VMnet8:对应的这个ipv4的地址

在后续全部启动，访问页面的时候，需要使用127.0.0.1去进行访问

![1758644293819-25e742e0-48c8-426f-b58f-5fe306e50593.png](./img/l6VphSInjxz39AXO/1758644293819-25e742e0-48c8-426f-b58f-5fe306e50593-801954.png)

即下面这种形式

![1758644384300-3ab01baa-5b12-481a-af61-3692941a93e5.png](./img/l6VphSInjxz39AXO/1758644384300-3ab01baa-5b12-481a-af61-3692941a93e5-226460.png)

## 7：如何访问docker中的MYSQL？

这个docker中的mysql是没有密码的，直接这么访问就可以了

```cpp
mysql -u root
```

![1759326186585-dcb1d801-6857-4ddc-9468-a9c2e70263b0.png](./img/l6VphSInjxz39AXO/1759326186585-dcb1d801-6857-4ddc-9468-a9c2e70263b0-994847.png)

# 安装与运行（一键配置版）（建议使用docker）

> 一键部署的前提，对代码进行合适的配置，下面的配置还是需要一步一步按照图示去操作！！！已经把占大头的部署部分写成了脚本，还有一些需要手动配置。

此次安装运行为一键部署，即可在Ubuntu22.04的云服务器上部署上线，公网都可以访问。

在执行脚本代码之前，需要做一些前置准备。

（注：若一键配置出现问题，可以先查看脚本源码，一个一个输入命令，焕心看了下前作者写的脚本，可能会出现一些问题，后续待更改（**已更新Docker一键配置版本，无需配环境**），如果有问题，可以看强烈建议看部分或者在群里 @卡哥助手

## 克隆代码到本地并处理权限

![1742980501590-8e12b6db-c995-4753-a189-6451a27f8685.png](./img/l6VphSInjxz39AXO/1742980501590-8e12b6db-c995-4753-a189-6451a27f8685-272521.png)

在克隆的时候出现这个问题就是你的ssh连接到github的权限还没打开。执行下面的代码。

```shell
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub

# 把打印在终端的相关的密钥信息复制到剪切板
# 登录 GitHub 账户，进入 Settings -> SSH and GPG keys -> New SSH key。
# 将复制的公钥内容粘贴到文本框中，点击 Add SSH key。

ssh -T git@github.com # 运行以下命令测试是否能够成功连接到 GitHub
```

![1742993704692-c0a94511-dd0a-4a31-8ccb-d80d21bf2a1e.png](./img/l6VphSInjxz39AXO/1742993704692-c0a94511-dd0a-4a31-8ccb-d80d21bf2a1e-310642.png)

如果22端口超时，可以修改为访问github备用的443端口。

首先先打开或创建SSH配置文件，并添加下面的内容，并保存

```shell
nano ~/.ssh/config

# 添加下面的内容
Host github.com
  Hostname ssh.github.com
  Port 443
  User git
```

重新git clone。

```shell
# 重新尝试克隆远程仓库
cd ~
mkdir project
cd project
git clone git@github.com:youngyangyang04/KamaChat.git
```

\*\*如果失败，尝试直接执行： \*\*

```shell
git clone https://github.com/youngyangyang04/KamaChat.git
```

## 开放端口

这是我的阿里云服务器，我现在需要开放一些端口，以确保项目启动时能够正常访问到这些端口。

![1741176011055-c5118e73-7fec-4bb2-8b93-be66752edccf.png](./img/l6VphSInjxz39AXO/1741176011055-c5118e73-7fec-4bb2-8b93-be66752edccf-499438.png)

![1741176002397-01161242-31fd-4899-8801-9aa89af36d44.png](./img/l6VphSInjxz39AXO/1741176002397-01161242-31fd-4899-8801-9aa89af36d44-917084.png)

把端口3306（mysql）, 6379（redis）, 443（前端访问）, 80（云服务器http访问）, 22（ssh）, 3478（turn服务器，用于音视频公网转发）, 8000（后端访问）等端口开放。

## 更新配置文件

```toml
[mainConfig]
appName = "kama_chat_server"
host = "0.0.0.0"
port = 8000

[mysqlConfig]
host = "127.0.0.1"
port = 3306
user = "root"
password = "" # 云服务器始终保证root状态，所以默认没有密码
databaseName = "kama_chat_server"

[redisConfig]
host = "127.0.0.1"
port = 6379
password = "" # redis也是默认没有密码
db = 0

[authCodeConfig]
accessKeyID = "your accessKeyID in alibaba cloud"
accessKeySecret = "your accessKeySecret in alibaba cloud"
signName = "阿里云短信测试"
templateCode = "SMS_154950909"

[logConfig]
logPath = "./logs"

[kafkaConfig]
messageMode = "channel"# 消息模式 channel or kafka
hostPort = "127.0.0.1:9092" # "127.0.0.1:9092,127.0.0.1:9093,127.0.0.1:9094" 多个kafka服务器
loginTopic = "login"
chatTopic = "chat_message"
logoutTopic = "logout"
partition = 0 # kafka partition
timeout = 1 # 单位秒

[staticSrcConfig]
staticAvatarPath = "./static/avatars"
staticFilePath = "./static/files"
```

这个是在后端文件夹里面的configs文件夹中添加一个叫做config\_local.toml的配置文件，填入上面的配置信息。

![1742828439710-c23fd6b5-963d-4803-b72b-5b5f27383c33.png](./img/l6VphSInjxz39AXO/1742828439710-c23fd6b5-963d-4803-b72b-5b5f27383c33-121426.png)

整个配置文件中，你只需要修改阿里云短信验证功能的accessKeyID和accessKeySecret，这篇需要提前看“后端开发”里的“手机验证”功能。（如果不看也没关系，不妨碍你部署项目，只是后面你需要通过手机验证的方式注册账号，或者也可以直接在后台mysql数据库里添加用户即可）

![1742992383924-86d59e1b-9c00-4b1f-964f-49860259cb0d.png](./img/l6VphSInjxz39AXO/1742992383924-86d59e1b-9c00-4b1f-964f-49860259cb0d-070430.png)

## 开整！！！

在这些都完成之后，就可以开始执行脚本代码了。这里我提供脚本全部内容，如果有人有部分依赖已经在之前完成其他工作时部署好了，可以从脚本代码中注释掉那一部分，然后整体运行脚本即可。

整个脚本执行时间预估在10多分钟左右，请耐心等待，如果有卡死的情况，ctrl+c，并重新执行脚本。

如果没有按照wget，先安转一下

```shell
sudo apt install wget
```

下载如下脚本：

[附件: deploy\_kamachat.sh](./attachments/l6VphSInjxz39AXO/deploy_kamachat.sh)

开启脚本执行权限。

```shell
chmod +x deploy_kamachat.sh
sh ./deploy_kamachat.sh
```

![1742830242096-8cf7f706-2b59-41d4-b9ad-bc7a2caa21c5.png](./img/l6VphSInjxz39AXO/1742830242096-8cf7f706-2b59-41d4-b9ad-bc7a2caa21c5-841883.png)

Enter即可。

![1742830408054-906bab4f-ccb3-4cc0-b592-360e3878159c.png](./img/l6VphSInjxz39AXO/1742830408054-906bab4f-ccb3-4cc0-b592-360e3878159c-163887.png)

不用管，ESC即可。

![1742830432627-2ae9601b-f2d8-4032-a96c-360b159d875a.png](./img/l6VphSInjxz39AXO/1742830432627-2ae9601b-f2d8-4032-a96c-360b159d875a-974498.png)

Enter即可，云服务器root情况默认无密码，就算你写了密码，它还是会默认无密码。

![1742830481510-b73d0cba-d1b1-499a-bd99-4a509d8bfb73.png](./img/l6VphSInjxz39AXO/1742830481510-b73d0cba-d1b1-499a-bd99-4a509d8bfb73-970634.png)![1742830492063-4998b7f6-2d6c-4e05-9868-09c7f0e6cbf5.png](./img/l6VphSInjxz39AXO/1742830492063-4998b7f6-2d6c-4e05-9868-09c7f0e6cbf5-349339.png)

![1742830540651-0778eb8c-3e4d-420b-a652-0b0f7936aff7.png](./img/l6VphSInjxz39AXO/1742830540651-0778eb8c-3e4d-420b-a652-0b0f7936aff7-870032.png)

![1742830549760-76a9edf0-b3d8-4935-baee-79973a3d32a8.png](./img/l6VphSInjxz39AXO/1742830549760-76a9edf0-b3d8-4935-baee-79973a3d32a8-144995.png)

一路Enter跳过，不管。

![1742830586057-ce571c7b-e8ea-4db1-bb7e-2a565b8672d4.png](./img/l6VphSInjxz39AXO/1742830586057-ce571c7b-e8ea-4db1-bb7e-2a565b8672d4-678055.png)

Enter不管。

![1742830615814-87827488-3901-4c5a-a49c-0512590e0369.png](./img/l6VphSInjxz39AXO/1742830615814-87827488-3901-4c5a-a49c-0512590e0369-123660.png)

遇到这个就ESC即可。（先不管这个功能。它就是重启服务的，没有实质性意义）

![1742833826898-322712d1-f1d3-4280-ac63-972329b61781.png](./img/l6VphSInjxz39AXO/1742833826898-322712d1-f1d3-4280-ac63-972329b61781-118785.png)

可以重启脚本，这是网络连接失败，多试几次应该就可以。脚本添加了手动克隆仓库的功能。

![1742833960416-e8a62304-6aa9-4649-9544-13657532f684.png](./img/l6VphSInjxz39AXO/1742833960416-e8a62304-6aa9-4649-9544-13657532f684-503603.png)

这样就算成功了。

![1742834110757-9d24f8c5-2d34-4058-899a-cc016d5e317b.png](./img/l6VphSInjxz39AXO/1742834110757-9d24f8c5-2d34-4058-899a-cc016d5e317b-448003.png)

按照上面输入。一共要输2次，都一样。

接下来进入一个页面。![1742834283120-9fe44d27-14e1-48ec-b23e-812b9fdeedf3.png](./img/l6VphSInjxz39AXO/1742834283120-9fe44d27-14e1-48ec-b23e-812b9fdeedf3-839065.png)

```shell
authorityKeyIdentifier=keyid,issuer
basicConstraints=CA:FALSE
keyUsage = digitalSignature, nonRepudiation, keyEncipherment, dataEncipherment
subjectAltName = @alt_names
[alt_names]
IP.1 = xxxxxxxxx # 你的云服务器地址
```

把上面的内容粘贴进去，记得修改云服务器地址。

![1742834439429-1385e3bf-db1b-447a-b1ea-8d41c1698383.png](./img/l6VphSInjxz39AXO/1742834439429-1385e3bf-db1b-447a-b1ea-8d41c1698383-585296.png)

```shell
<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    # HTTP 到 HTTPS 重定向
    <IfModule mod_rewrite.c>
        RewriteEngine On
        RewriteCond %{HTTPS} off
        RewriteRule ^/?(.*) https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]
    </IfModule>
</VirtualHost>

<VirtualHost *:443>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    SSLEngine on
    SSLProxyEngine on

    SSLCertificateFile /etc/ssl/certs/server.crt
    SSLCertificateKeyFile /etc/ssl/private/server.key

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
```

把上面的内容粘贴进去。

![1742835703431-0c0597c9-7c0d-47ab-b22d-c4486ac55ec8.png](./img/l6VphSInjxz39AXO/1742835703431-0c0597c9-7c0d-47ab-b22d-c4486ac55ec8-306757.png)

```shell
listening-ip=0.0.0.0

external-ip=xxxxx # 外部 IP 地址（替换为你的服务器公网 IP）

listening-port=3478 # 监听端口

user=username:password # 用户名和密码（替换为你的用户名和密码）

tls-certificate=/etc/ssl/certs/server.crt # SSL 证书路径（如果需要加密通信）
tls-private-key=/etc/ssl/private/server.key
```

把上面内容粘贴上去，记得修改公网ip和用户名密码。

![1742835821001-5c2d638c-ae85-411e-ac6c-3fcba3a4230b.png](./img/l6VphSInjxz39AXO/1742835821001-5c2d638c-ae85-411e-ac6c-3fcba3a4230b-053088.png)

```shell
[Unit]
Description=kama_chat_backend
After=network.target

[Service]
User=root
WorkingDirectory=/root/project/KamaChat/cmd/kama_chat_server
ExecStart=/root/project/KamaChat/cmd/kama_chat_server/kama_chat_backend
Restart=always
RestartSec=10
Environment=GO_ENV=production
# 记得WorkingDirectory和ExecStart后面不要使用任何的注释，会错误检测路径


[Install]
WantedBy=multi-user.target
```

WorkingDirectory就是你存放main.go的路径，比如你git clone到/home/xxx路径下，那么该路径就是/home/xxx/KamaChat/cmd/kama\_chat\_server。

ExecStart 就是 /home/xxx/KamaChat/cmd/kama\_chat\_server/kama\_chat\_backend。

自己修改好，不然你的后端服务会正确部署。

![1742835900079-5aee1032-ae4f-4eb5-9d31-499ed0fb0bba.png](./img/l6VphSInjxz39AXO/1742835900079-5aee1032-ae4f-4eb5-9d31-499ed0fb0bba-918026.png)

当出现“Deployment complete！”说明部署好了。

![1742991798914-d02e05eb-8d1f-49f4-9b7b-891d724d59c9.png](./img/l6VphSInjxz39AXO/1742991798914-d02e05eb-8d1f-49f4-9b7b-891d724d59c9-601759.png)

此时通过netstat -tuln可以看到前端的443端口和后端的8000端口已经开始监听了。

但是现在如果你直接就去访问443端口的话，想必会出现证书不安全，然后所有功能都无法执行。

## 脚本源码

下面是脚本源码，大家可以看看执行了哪些操作。

```shell
#!/bin/bash

# 更新系统软件包
sudo apt update && sudo apt upgrade -y

# 安装 MySQL
echo "Installing MySQL..."
sudo apt install mysql-server -y

# 配置 MySQL 安全
sudo mysql_secure_installation

# 启动并启用 MySQL 服务
sudo systemctl start mysql
sudo systemctl enable mysql

# 自动创建数据库
echo "Creating database 'kama_chat_server'..."
sudo mysql -u root -p <<EOF
CREATE DATABASE kama_chat_server;
EOF

# 安装 Redis
echo "Installing Redis..."
sudo apt install redis-server -y

# 配置 Redis
# nano /etc/redis/redis.conf  # 修改 bind 127.0.0.1 改为 bind 0.0.0.0（如果需要外部访问）

# 启动并启用 Redis 服务
sudo systemctl start redis
sudo systemctl enable redis

# 卸载旧版本 Node.js 和 npm，如果不是纯净版的Ubuntu的话
echo "Uninstalling previous versions of Node.js and npm..."
sudo apt remove --purge -y nodejs npm

echo "Installing Node Version Manager (nvm)..."

apt install curl

curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -

apt install -y nodejs

# 安装 Go
echo "Installing Go..."
wget https://mirrors.aliyun.com/golang/go1.20.linux-amd64.tar.gz
tar -C /usr/local -xzf go1.20.linux-amd64.tar.gz

cp -r /usr/local/go/bin/* /usr/bin
# 设置 Go 环境变量
echo "Configuring Go environment..."

export PATH=$PATH:/usr/local/go/bin

 # 设置 Go 环境变量（避免重复写入 ~/.bashrc）
 if ! grep -q "export GOPATH=$HOME/go" ~/.bashrc; then
     echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
 fi
 source ~/.bashrc


# # 配置 Go 代理
echo "Configuring Go proxy..."
go env -w GOPROXY=https://goproxy.cn,direct


cd ~/project/KamaChat

# 安装 ssl 模块
echo "Installing ssl..."
apt-get install openssl
apt-get install libssl-dev

# # 创建根密钥，生成证书签名请求 (CSR)，创建根证书
openssl genrsa -out /etc/ssl/private/root.key 2048
openssl req -new -key /etc/ssl/private/root.key -out /etc/ssl/certs/root.csr
openssl x509 -req -in /etc/ssl/certs/root.csr -out /etc/ssl/certs/root.crt -signkey /etc/ssl/private/root.key -CAcreateserial -days 3650

# 生成服务器密钥，生成服务器证书签名请求 (CSR)，创建服务器证书扩展文件
openssl genrsa -out /etc/ssl/private/server.key 2048
openssl req -new -key /etc/ssl/private/server.key -out /etc/ssl/certs/server.csr
nano v3.ext

# # 使用根证书为服务器证书签名
openssl x509 -req -in /etc/ssl/certs/server.csr -CA /etc/ssl/certs/root.crt -CAkey /etc/ssl/private/root.key -CAcreateserial -out /etc/ssl/certs/server.crt -days 500 -sha256 -extfile v3.ext

# 安装 Apache2
apt update && apt install apache2

# 配置虚拟主机
mkdir -p /etc/apache2/sites-enabled
nano /etc/apache2/sites-enabled/000-default.conf


# 启用所需模块
a2enmod ssl
a2enmod rewrite

# 检查配置文件语法
apache2ctl configtest

# 重启 Apache 服务
systemctl start apache2
systemctl enable apache2



# 部署前端服务
cd ~/project/KamaChat

# 安装 Vue.js 开发环境
echo "Installing Vue.js development environment..."
apt install npm -y

# 将 Node.js 的安装路径加入环境变量
export PATH="/root/.nvm/versions/node/v16.20.2/bin:$PATH"

# 将 Node.js 的安装路径加入 .bashrc
echo "export PATH=\$PATH:/root/.nvm/versions/node/v16.20.2/bin" >> ~/.bashrc

# 刷新 .bashrc
source ~/.bashrc

# 方案1：使用 npm 安装 Yarn
# npm install -g yarn

# 方案2：使用cnpm 安装 Yarn
npm install -g cnpm --registry=https://registry.npmjs.org
cnpm install -g yarn

# 安装 Vue CLI
cnpm install -g @vue/cli

# 重新安装项目依赖
cd ~/project/KamaChat/web/chat-server

yarn cache clean
rm -rf node_modules

yarn install # 会把package.json中所有依赖配置好的

#打包项目成dist，放到/var/www/html/，此时就可以通过云服务器的公网ip看到前端页面了
mkdir -p /var/www/html
rm -rf /var/www/html/* 
rm -rf /root/project/KamaChat/web/chat-server/dist
yarn build
cp -r /root/project/KamaChat/web/chat-server/dist/* /var/www/html # 改成自己的项目路径
chmod -R 755 /var/www/html
chown -R www-data:www-data /var/www/html


# 配置turn服务器
echo "Installing coturn..."
apt install coturn
mkdir -p /etc/coturn
nano /etc/coturn/coturn.conf

systemctl start coturn
systemctl enable coturn


# 部署后端服务
cd /root/project/KamaChat/cmd/kama_chat_server
go build -o kama_chat_backend main.go


nano /etc/systemd/system/kama_chat_backend.service

systemctl daemon-reload
# 启动服务
systemctl start kama_chat_backend

# 启用服务（开机自启）
systemctl enable kama_chat_backend


# 输出完成信息
echo "Deployment complete!"
```

## 证书不安全？

自签名证书是不受信任的证书类型，如果要在自己电脑的浏览器上访问你的云服务器上的项目，就需要在自己的电脑上安装这个自签名证书，让该证书受你的电脑的信任。

以管理员身份运行命令提示符，输入下面指令。

```shell
scp root@你的云服务器ip地址:/etc/ssl/certs/root.crt C:/Users/HP/Desktop
```

![1743060468474-b0f8dd08-9a7c-41ce-8b45-fb483b6e8cea.png](./img/l6VphSInjxz39AXO/1743060468474-b0f8dd08-9a7c-41ce-8b45-fb483b6e8cea-141350.png)

如果出现上面的报错，说明你之前安装过这台云服务器机器的密钥，先删除一下就好了。

```shell
ssh-keygen -R xxxxx # 你的云服务器ip地址
```

再次执行scp指令。

![1742980953619-f18b03da-1f0b-45c5-b12f-07d26871f411.png](./img/l6VphSInjxz39AXO/1742980953619-f18b03da-1f0b-45c5-b12f-07d26871f411-901923.png)

![1742981005550-4d248ba8-2c0c-4b29-86f2-3934151cd6db.png](./img/l6VphSInjxz39AXO/1742981005550-4d248ba8-2c0c-4b29-86f2-3934151cd6db-042646.png)

这样可以在本地桌面看到这个CA证书。点开它。

![1742981026909-eb24d04d-51c8-4d06-a6e2-1e1773d66548.png](./img/l6VphSInjxz39AXO/1742981026909-eb24d04d-51c8-4d06-a6e2-1e1773d66548-965513.png)

点击安装证书。

![1742981042549-43433bc6-5dff-49fa-a739-8e2e270dcb91.png](./img/l6VphSInjxz39AXO/1742981042549-43433bc6-5dff-49fa-a739-8e2e270dcb91-284005.png)

选择当前用户。点击下一步。

![1742981068059-11165502-a16e-4f43-b6fa-2e7739d67852.png](./img/l6VphSInjxz39AXO/1742981068059-11165502-a16e-4f43-b6fa-2e7739d67852-825694.png)

选择证书存储：受信任的根证书颁发机构。点击下一步。

![1742981097865-47734bc7-fe5e-4d47-9faf-49fb06f06651.png](./img/l6VphSInjxz39AXO/1742981097865-47734bc7-fe5e-4d47-9faf-49fb06f06651-814049.png)

点击是。

![1742981105895-dfa6d727-cb64-4137-867b-5f015a849fde.png](./img/l6VphSInjxz39AXO/1742981105895-dfa6d727-cb64-4137-867b-5f015a849fde-032619.png)

此时本地浏览器信任了你在云服务器上自签名的证书。

## （强烈建议看）可能遇到的问题以及解决方案：

若是碰到了一些在此没有解决的方案的问题，可直接在群里卡哥助手-[@焕、心](undefined/huanxin-katrm)

有些问题可能在原作者写的后端开发/前端开发部分中有说明：这里建议先看下后面这两个部分，查看是否有对应解答（比如当前群聊可能不支持音视频通话等相关信息是在前端开发部分中有说明的）

### 0：登录的时候没登录成功？没反应？

我们需要查看这个是前端没有请求到的问题，还是后端没有响应成功的问题

所以，对于初看项目的小白来说，我们需要学会合理的定位

在你进入vue页面之后，鼠标右键点击检查（然后查看Network）

下面是一位录友的图片，可以发现他的请求地址是123.56....(原先写死在代码中的那个地址，可能是原作者之前的ECS的公网ip地址）我们需要通过在自己本身的服务器上查看服务器本身的ip地址，然后进行更替

![1750406765815-b23416d6-7ca4-4da9-9333-9c86d57bf38f.png](./img/l6VphSInjxz39AXO/1750406765815-b23416d6-7ca4-4da9-9333-9c86d57bf38f-338835.png)

看每次发送之后的请求是否有响应（response）如果没有，那么说明前端没发送出去，可能是ip地址错了，这里需要更改index.js这个文件

![1750406557857-80d43c91-6e4a-4867-bf58-85b493fcedf9.png](./img/l6VphSInjxz39AXO/1750406557857-80d43c91-6e4a-4867-bf58-85b493fcedf9-882321.png)

找到KamaChat/web/chat-server/src/store/index.js

将里面的ip地址更改成你云服务器的ip地址，然后重新yarn build，将生成的内容放入到指定位置后续再去查看是否有响应，一般来说就不会有问题了

(这部分截图是脚本源码部分的，可以前去查看输入即可）

![1750407071365-c1ae70d9-0c3b-40f6-9444-6f66ec89d8f7.png](./img/l6VphSInjxz39AXO/1750407071365-c1ae70d9-0c3b-40f6-9444-6f66ec89d8f7-038667.png)

![1750406664797-8ff298cd-d213-47ed-9bf0-2ee799852f9f.png](./img/l6VphSInjxz39AXO/1750406664797-8ff298cd-d213-47ed-9bf0-2ee799852f9f-636481.png)

### 1：证书还是解决不了怎么办？

那只能不用加密通信了，加密通信本身和非加密通信在代码层面改动也不大，本身从http改成https也只是锦上添花，没有什么太耀眼的东西在上面

我们只需要将https通信更改成http，将websocket的wss改成ws即可

将main.go部分这里更改为

```go
go func() {
    // Win10本地部署
    // if err := https_server.GE.RunTLS(fmt.Sprintf("%s:%d", host, port), "pkg/ssl/127.0.0.1+2.pem", "pkg/ssl/127.0.0.1+2-key.pem"); err != nil {
    // 	zlog.Fatal("server running fault")
    // 	return
    // }
    // Ubuntu22.04云服务器部署
    if err := https_server.GE.RunTLS(fmt.Sprintf("%s:%d", host, port), "/etc/ssl/certs/server.crt", "/etc/ssl/private/server.key"); err != nil {
        zlog.Fatal("server running fault")
        return
    }
}()

//改成如下，上面代码是原先的
	go func() {
		// Win10本地部署
		// if err := https_server.GE.RunTLS(fmt.Sprintf("%s:%d", host, port), "pkg/ssl/127.0.0.1+2.pem", "pkg/ssl/127.0.0.1+2-key.pem"); err != nil {
		// 	zlog.Fatal("server running fault")
		// 	return
		// }
		// Ubuntu22.04云服务器部署
		//fmt.Println("test host and port is  %s:%d", host, port)
		//if err := https_server.GE.RunTLS(fmt.Sprintf("%s:%d", host, port), "/etc/ssl/certs/server.crt", "/etc/ssl/private/server.key"); err != nil {
		//	zlog.Fatal("server running fault")
		//	return
		//}
		fmt.Printf("test host and port is %s:%d\n", host, port)
		if err := https_server.GE.Run(fmt.Sprintf("%s:%d", host, port)); err != nil {
			zlog.Fatal("server running fault")
			return
		}
	}()

```

更改KamaChat/web/chat-server/src/store目录下的index.js

```javascript
//将https->http以及wss->ws,其余保持你自己的ip地址和端口就行
backendUrl: 'http://192.168.15.129:8000',
wsUrl: 'ws://192.168.15.129:8000',
```

将剩下文件所有wss相关的全部改成ws如下（当然这里只是部分，全局搜一下wss相关的内容改成ws即可

```javascript
//https_server.go
GE.GET("/ws", v1.WsLogin)
//App.vue
const wsUrl = store.state.wsUrl + "/ws?client_id=" + store.state.userInfo.uuid;

```

### 2：更改为http后，webrtc部分可能出错：

webrtc本身在localhost或者https访问的时候，它才能正常获取到摄像头的一些信息

所以当更改成http协议后

web/chat-server/src/views/chat/contact/ContactChat.vue下可能出现

<font style="color:#080808;background-color:#ffffff;">getUserMedia is not supported!的日志输出，这个就表示当前浏览器不信任你</font>

<font style="color:#080808;background-color:#ffffff;">查阅相关资料后，找到了解决这个问题的办法：按照如图所示这么做即可</font>

![1749907426436-9f829bb1-b143-43e5-bf4d-4337903529f2.png](./img/l6VphSInjxz39AXO/1749907426436-9f829bb1-b143-43e5-bf4d-4337903529f2-972048.png)

启用并Relaunch之后，再次发起联系人通话就可以了

（注：在前端开发中有讲到，当前并不支持群聊视频，只支持两个联系人之间的音视频通信）

![1749907467226-c3ddea59-4616-4eb2-ab93-d303dbbb5267.png](./img/l6VphSInjxz39AXO/1749907467226-c3ddea59-4616-4eb2-ab93-d303dbbb5267-824707.png)

### 3：yarn build的时候可能会出现卡顿问题

* **方案A (临时解决)**: 指导用户在服务器上创建并启用 Swap 交换空间，为编译提供“虚拟内存”。

```bash
# 增加2GB的Swap空间
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

* **方案B **: 在文档中推荐**本地编译、上传产物**的专业部署流程。即在个人电脑上运行 `yarn build`，然后只将生成的 `dist` 文件夹上传到服务器的 `/var/www/html` 目录。
* 方案C：(直接在虚拟机上部署项目，不用ECS了，本身ECS和自己的虚拟机也差不多的

### 4：后端含有硬编码问题：

在KamaChat/internal/config/config.go中有这么一段代码,可以发现

项目作者这里硬编码了一段代码，所以我们还需要将config\_local.toml拷贝到这个目录下

否则将这行代码更改为你所对应的位置，也可以将硬编码方式去除

```cpp
func LoadConfig() error {
	// 本地部署
	// if _, err := toml.DecodeFile("F:\\go\\kama-chat-server\\configs\\config_local.toml", config); err != nil {
	// 	log.Fatal(err.Error())
	// 	return err
	// }
	// Ubuntu22.04云服务器部署
	if _, err := toml.DecodeFile("/root/project/KamaChat/configs/config_local.toml", config); err != nil {
		log.Fatal(err.Error())
		return err
	}
	return nil
}
```

### 5：阿里云 短信验证认证不了？

听不少录友说阿里云那个短信服务要35块钱才能买2000条次数，且买了之后还个人认证不了，这里可以先不用验证码相关的功能（如验证码登录，注册功能）

可以直接用暴力的方式插入一行数据到user\_infos表中

后续焕、心会将阿里云手机验证码更改成QQ邮箱验证码的方式

```sql
INSERT INTO user_info (
  uuid, nickname, telephone, email, avatar, gender,
  signature, password, birthday, created_at, deleted_at,
  last_online_at, last_offline_at, is_admin, status
) VALUES (
  'U2025061412347',
  '小红',
  '13700000002',
  'xiaohong@example.com',
  '/static/avatars/xh.png',
  0,
  '喜欢旅行，热爱生活',
  'e10adc3949ba59ab',
  '19980308',
  '2025-06-14 14:22:01',
  NULL,
  NULL,
  NULL,
  0,
  0
);

```

### 6:yarn install可能出现问题(可能网络出现问题）

* 方案一：换一个有用的源
* 方案二：换源后依旧出现网络问题（[@焕、心](undefined/huanxin-katrm)找我要 ) 然后yarn config set proxy <http://192.168.15.1:7890> 将虚拟机地址绑定到主机地址和映射端口，此时就可以解决网络问题了）

### 7：个人资料上传头像的时候并没有上传成功：

我在观看作者的源代码的时候，发现它其实只是上传了一个文件名，然后去寻找对应服务器下指定目录是否有这个文件，个人认为原作者的正确做法可以变成上传base64数据，然后服务端将其保存为图片，后续去请求相关图片就可以了

大家可以先自行更改这块逻辑，后续考虑我将原作者这块进行一个优化，如果真的想要换图片的话，可以上传指定图片到服务器上指定路径，后续上传相同名字的图片就可以请求到该图片了

![1749912374523-46976226-528c-4c9c-b1b4-c2d110004bc2.png](./img/l6VphSInjxz39AXO/1749912374523-46976226-528c-4c9c-b1b4-c2d110004bc2-831607.png)

### 8：其中一名录友遇到的问题以及解决方案

可以看看是否包含自身所遇到的问题，看看解决方案是否有帮助

[附件: kamachat.md](./attachments/l6VphSInjxz39AXO/kamachat.md)

### 9：sudo systemctl enable redis 失败？

试试这个命令

这个命令主要是在系统启动时自动启动你的redis，不用每次都start

```cpp
sudo systemctl enable redis-server
```

![1749979861520-8dfa47d6-4cf0-40f4-abd3-fc8b4a246df7.png](./img/l6VphSInjxz39AXO/1749979861520-8dfa47d6-4cf0-40f4-abd3-fc8b4a246df7-989201.png)

### 10：刷新页面的时候，出现404问题：

如果是自己配置的https的话，看到这里，如果是docker版本用的是http的话，那么在docker部分第五点已经讲过了，看那边即可

原因：阿帕奇配置文件我忘记对它做操作了，固然会请求一个静态资源目录，但是本身阿帕奇路径下没有login，固然会出现404的问题，那么如何解决呢？请看代码块部分，此时按照如下流程走就可以了

更新完毕之后：sudo systemctl restart apache2即可

![1751463841690-f37fe363-36b4-43ba-be4c-91c104ea5d56.png](./img/l6VphSInjxz39AXO/1751463841690-f37fe363-36b4-43ba-be4c-91c104ea5d56-674351.png)

```cpp
# 删除原先的阿帕奇配置文件
rm -f /etc/apache2/sites-enabled/000-default.conf
# 重新编辑并复制粘贴第二个代码块的内容
vim /etc/apache2/sites-enabled/000-default.conf
```

```cpp
<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined

    # HTTP 到 HTTPS 重定向
    RewriteEngine On
    RewriteCond %{HTTPS} off
    RewriteRule ^/?(.*) https://%{SERVER_NAME}%{REQUEST_URI} [R=301,L]
</VirtualHost>

# HTTPS 配置：监听 443，启用 SSL 和前端 SPA 支持
<VirtualHost *:443>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # SSL 证书配置（根据你实际路径替换）
    SSLEngine on
    SSLCertificateFile /etc/ssl/certs/server.crt
    SSLCertificateKeyFile /etc/ssl/private/server.key

    # 如果你确实有链证书，再取消注释：
    # SSLCertificateChainFile /etc/ssl/certs/chain.pem
    # SPA 路由重写：排除静态资源，其他都跳转到 index.html
    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/static/
    RewriteCond %{REQUEST_URI} !\.(js|css|png|jpg|jpeg|gif|ico|svg)$
    RewriteRule ^ /index.html [L]

    ErrorLog ${APACHE_LOG_DIR}/error_ssl.log
    CustomLog ${APACHE_LOG_DIR}/access_ssl.log combined
</VirtualHost>
```

## 总结

上面的操作都完成之后，就可以正式开始访问了。

目前整个部署加配置的流程已经是我能想到的最简化的方式了！！！因为本身整个项目的部署难度很大！！！

之后的访问可以通过https://xxxxx:443去访问。

## 注意

### 短信验证问题

短信验证功能，如果发送了验证码却没有收到，是因为我使用的是阿里云的测试短信模板。测试短信模板对应的绑定的测试手机号最多只有5个，请记得提前去阿里云短信服务中绑定测试手机号，这样使用短信验证功能，对应手机才会收到验证码，不然应该就会被阿里云拦截了。

![1742992248920-b964f2df-00a0-42b6-be79-83d952bb3d18.png](./img/l6VphSInjxz39AXO/1742992248920-b964f2df-00a0-42b6-be79-83d952bb3d18-841559.png)

在阿里云->短信服务中的“快速学习与测试”项中，有绑定测试手机号。\*\*<font style="color:#1DC0C9;">短信验证功能建议提前看“后端开发”中的“手机验证”部分。</font>\*\*这就不多赘述了。

![1742992370246-b917385d-f3f7-413c-8b69-3d58edc55db1.png](./img/l6VphSInjxz39AXO/1742992370246-b917385d-f3f7-413c-8b69-3d58edc55db1-380169.png)

### 脚本卡了就ctrl + c，重新执行！！！！

# docs

在/docs/业务逻辑.md中，介绍了具体的业务设计，对业务有问题的同学可以查看了解一下。

# todoList

* 多对多群聊
* nginx分布式部署


> 更新: 2026-04-08 13:30:12  
> 原文: <https://www.yuque.com/chengxuyuancarl/zbfw4l/nz9qx23vcizbvetd>