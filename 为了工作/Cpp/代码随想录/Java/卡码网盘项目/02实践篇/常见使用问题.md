# 常见使用问题

## AI相关功能

### 出现Failed to call the configured AI model provider报错

\*\*原因：\*\*阿里百炼平台的模型额度用完或转为收费模型、你的APIKey过期

**解决方案1：**

更换百炼平台其他免费的模型，选择模型名复制到下方即可

![1779356733335-883c8536-fde4-4a23-8e2b-0c7846e91d59.png](https://cdn.davidingplus.cn/images/2026/09/11/1779356733335-883c8536-fde4-4a23-8e2b-0c7846e91d59-888809.png)

\*\*解决方案2：\*\*本地chat模型运行较慢，可能超时

调用本地模型部署（采用ollama部署）

下载ollama后拉取chat模型与embedding模型

```plain
ollama pull nomic-embed-text
ollama pull qwen2.5:7b
```

更改配置如下，重启后端后可正常使用：

![1779357578504-e34935eb-81a1-415e-9bf3-a10366c5c303.png](https://cdn.davidingplus.cn/images/2026/09/11/1779357578504-e34935eb-81a1-415e-9bf3-a10366c5c303-352130.png)

\*\*解决方案3：\*\*比较复杂，性能较好

```
1. 更改部分代码，拆分chat模型与embedding模型配置
```

在`networkdisk-business/networkdisk-ai/src/main/java/com/disk/ai/infrastructure/config/AiProviderProperties.java`文件中添加：

```plain
private String embeddingBaseUrl;

private String embeddingApiKey;
```

原有的baseUrl/apiKey 继续给百炼 chat 用；embeddingBaseUrl/embeddingApiKey 专门给 Ollama 向量接口用

```
2. 修改`networkdisk-business/networkdisk-ai/src/main/java/com/disk/ai/infrastructure/provider/OpenAiCompatibleClient.java`
```

更改createEmbeddings()函数，同时新增validateEmbeddingConfiguration()

```plain
public List<float[]> createEmbeddings(List<String> texts) {
    validateEmbeddingConfiguration();
    if (texts == null || texts.isEmpty()) {
        return List.of();
    }

    String embeddingBaseUrl = StringUtils.defaultIfBlank(
            properties.getEmbeddingBaseUrl(),
            properties.getBaseUrl()
    );
    String embeddingApiKey = StringUtils.defaultIfBlank(
            properties.getEmbeddingApiKey(),
            properties.getApiKey()
    );

    EmbeddingRequest request = new EmbeddingRequest();
    request.setModel(properties.getEmbeddingModel());
    request.setInput(texts);
    request.setDimensions(properties.getEmbeddingDimension());
    request.setEncodingFormat("float");

    try {
        EmbeddingResponse response = restClient.post()
                .uri(buildUrl(embeddingBaseUrl, properties.getEmbeddingsPath()))
                .headers(headers -> {
                    headers.setBearerAuth(embeddingApiKey);
                    headers.setContentType(MediaType.APPLICATION_JSON);
                    headers.setAccept(List.of(MediaType.APPLICATION_JSON));
                })
                .body(request)
                .retrieve()
                .body(EmbeddingResponse.class);

        if (response == null || response.getData() == null || response.getData().isEmpty()) {
            throw new AiException("Empty embeddings response", AiErrorCode.MODEL_REQUEST_FAILED);
        }

        return response.getData().stream()
                .sorted(Comparator.comparingInt(value -> value.getIndex() == null ? 0 : value.getIndex()))
                .map(this::toFloatArray)
                .toList();
    } catch (AiException e) {
        throw e;
    } catch (Exception e) {
        throw new AiException("Failed to request embeddings from the configured AI provider", e, AiErrorCode.MODEL_REQUEST_FAILED);
    }
}

private void validateEmbeddingConfiguration() {
    String embeddingBaseUrl = StringUtils.defaultIfBlank(
            properties.getEmbeddingBaseUrl(),
            properties.getBaseUrl()
    );
    String embeddingApiKey = StringUtils.defaultIfBlank(
            properties.getEmbeddingApiKey(),
            properties.getApiKey()
    );

    if (StringUtils.isBlank(embeddingBaseUrl)) {
        throw new AiException("com.disk.ai.provider.embedding-base-url or base-url is required when using openai-compatible embedding mode", AiErrorCode.MODEL_REQUEST_FAILED);
    }
    if (StringUtils.isBlank(embeddingApiKey)) {
        throw new AiException("com.disk.ai.provider.embedding-api-key or api-key is required when using openai-compatible embedding mode", AiErrorCode.MODEL_REQUEST_FAILED);
    }
}
```

```
3. 修改`networkdisk-business/networkdisk-ai/src/main/resources/application.yml`
```

![1779361644869-ca6ea396-cbc6-4b79-a8f4-342f6785b4a8.png](https://cdn.davidingplus.cn/images/2026/09/11/1779361644869-ca6ea396-cbc6-4b79-a8f4-342f6785b4a8-429169.png)

## 搜索功能

### 文档无法搜索

是前后端分隔符约定不一致导致参数解析问题，前端规定了多个类型为文档，后端解析前端参数时使用的是特殊分隔符，如下图修改对应这行代码为按逗号进行分隔即可正确识别文件类型并搜索

![1779361849710-db966770-fc24-47a8-8911-f5eb49763738.png](https://cdn.davidingplus.cn/images/2026/09/11/1779361849710-db966770-fc24-47a8-8911-f5eb49763738-448977.png)


> 更新: 2026-05-21 19:10:56  
> 原文: <https://www.yuque.com/chengxuyuancarl/edybxb/nosrwdhvq02vah80>